## Repository published and teammate branches created — 2026-09-12
- Changed: initialized git and published the scaffold at https://github.com/alphabyalex/East-v-west-hackathon-2026 (main: a47a618). Created and pushed Alex, Kristian, and Tharun once from main; working branch is Alex. Future Codex commits will go only to Alex and main.
- New/changed interface: origin is https://github.com/alphabyalex/East-v-west-hackathon-2026.git; all four branches have matching origin upstreams. Frontend work is still in progress and is excluded from the scaffold commit.
- Needs review: gh is unavailable on PATH and standard install locations, so collaborator invitations for Tharun.ekam@gmail.com and skbridge04@gmail.com were not attempted; invite them manually through GitHub settings or provide their GitHub usernames once gh is available. Existing README claims about learning actual emergency/curtailment records need Claude/human review against the documented proxy-label fallback; unchanged by Codex.

## Frontend shell and exact mock API contract — 2026-09-12 (4569cb2)
- Changed: added the Vite + React + TypeScript + Tailwind /web workspace, Recharts annual fan, sticky site-exposure assumption, editable economics, conditional decision, reusable Sourced/SourceInfo/SourcedTick provenance, reset and local JSON export. No API calls, training, sampling, remote assets, or backend edits. Run `cd web`, `npm ci`, `npm run dev`.
- New/changed interface: complete valid response is `web/src/model/mock-response.json`; exact TypeScript types are `web/src/model/types.ts`; units/formulas/defaults are in `docs/frontend-api-contract.md`. Every numeric field is SV = {value, source_type: "data"|"clause"|"assumption", ref}. Top-level response = {schema_version:"1.0.0", grid_operator:"SPP", mode:"illustrative", locations:[{id,label,annual_series:[{year:SV,p10:SV,p50:SV,p90:SV,p99:SV}]}], defaults:{location_id:SV<string>,load_mw:SV,contract_years:SV,flexibility_percent:SV,site_exposure:SV,firm_wait_years:SV,gpu_per_mw:SV,gpu_hour_value_usd:SV,early_margin_usd_per_mw_year:SV}, decision_policy:{close_call_fraction:SV}}. Annual rows are baseline hours/year at site_exposure=1; years are relative contract years. Browser scales fixed quantiles and computes economics with documented arithmetic. Break-even results are null when interruption cost is zero.
- Validation: production build and TypeScript check passed; all 13 model/DOM interaction tests passed, covering decision transitions, zero-cost cases, invalid input normalization, source JSON, and synchronized annual data. The automated browser tool exposes no browser connection, so rendered desktop/mobile visual QA remains unverified.
- Needs review: mock data is explicitly authorized for this shell. All fixture values use assumption/mock refs; SPP-region selectors are illustrative, not verified node IDs. Replace fixture mode, refs, and location identifiers together for real data. Annual summaries average marginal quantile paths; economics are a median-path proxy, not a quantile of total losses. Human/Claude should review the new model-boundary copy and economic assumptions before any real-data demo. Illustrative term range is not a tariff claim. Existing AGENTS.md and FROM_CLAUDE.md were not edited by Codex.

## Institutional visual pass — 2026-09-12
- Changed: replaced the olive palette with neutral charcoal surfaces and gray rules; reduced the header/title to a compact workspace treatment; joined chart, ledger, and economic assumptions into contiguous square-edged regions; flattened the decision readout. Removed the chart and slider gradients, decorative shadows/glows, and nonzero corner radii. Icons remain SVG, with a restrained geometric mark. Essential labels/notes are at least 10px; metric/input layouts wrap at narrow widths. The site-exposure assumption remains sticky and prominent, with animated numeric changes and complete provenance.
- Why: studied official Coinbase Prime and Palantir Workshop product screenshots for dense numeric workspaces, aligned controls, persistent context, and restrained semantic color; reviewed Jane Street, Optiver, and Millennium for concise hierarchy. Reference links are in `web/README.md`. The visible AGENTS.md frontend section still contained its earlier shorter wording when re-read, so the user's newer explicit ban list governed this pass.
- New/changed interface: no mock API schema or economics-formula changes. Scenario exports now preserve the same edited/default input provenance shown on screen.
- Validation: source audit found no gradients, shadows, glass effects, or rounded-card treatment. Production/interaction verification is recorded in the subsequent completion entry.
- Needs review: browser visual inspection remains unavailable because the computer-use inventory exposes no browsers. DOM interaction tests are not a desktop/mobile screenshot review. AGENTS.md and FROM_CLAUDE.md were not edited by Codex.

## Frontend validation complete — 2026-09-12
- Changed: completed shell and institutional visual pass, ready to share on Alex and main at https://github.com/alphabyalex/East-v-west-hackathon-2026. Exact mock response and integration notes remain linked above.
- Validation: `npm run build` passed (TypeScript + Vite); `npm test` passed all 13 tests; local dev server returns HTTP 200; `git diff --check` passed. Vite reports the expected large Recharts-containing bundle warning; build succeeds and all runtime assets are local. Source scan confirms no banned visual effects.
- Needs review: visual browser QA remains unverified. Collaborator invitations remain manual because gh is not installed. Kristian and Tharun stay at their initial branch commit a47a618; subsequent publication targets only Alex and main.

## Generated cache excluded — 2026-09-12
- Changed: added `web/.gitignore` for Vite's local `.vite/` cache after final branch verification. Runtime code and the verified build are unchanged.
- New/changed interface: none.

## Teammate integration workflow — 2026-09-12
- Changed: Alex authorized checking Kristian/Tharun after major checkpoints, reviewing and merging ready teammate work into Alex, validating the combined changes, and pushing Alex. Teammate branches remain owned by their authors and will not receive Codex pushes.
- New/changed interface: coordination workflow only; no API changes. Current remote check found both teammate branches still at a47a618, so there is no new work to merge.
- Needs review: the expanded Frontend direction is now present in the local AGENTS.md and has been read in full. That file's existing uncommitted update was left untouched and unstaged.

## Shared build contract published directly to main — 2026-09-12 20:52 UTC
- Changed: published the user-supplied docs/BUILD_PLAN.md unchanged to main at https://github.com/alphabyalex/East-v-west-hackathon-2026/blob/main/docs/BUILD_PLAN.md, using an isolated checkout. This documentation publication is outside /web and is explicitly requested by Alex; frontend work remains on Alex.
- New/changed interface: BUILD_PLAN.md is now the canonical shared contract for pipeline parquet/function output, POST /api/estimate, and frontend consumption. Its section 2 supersedes the earlier illustrative frontend API shape; the existing shell has not yet been migrated to it.
- Needs review: coordinate reconciliation of docs/frontend-api-contract.md and the frontend adapter with Alex/Kristian/Tharun. The canonical response adds confidence and model provenance, uses term_years and fractional flexibility_split, and provides p50/p90/p99 rather than p10. Publication does not claim this endpoint is implemented.

## Updated team roles and main integration — 2026-09-12
- Changed: re-read the full updated AGENTS.md and FROM_CLAUDE.md. Codex is building the frontend alongside Tharun; Alex owns backend/API/wiring and Kristian owns ML. Role bleed is normal and changes outside /web will be logged here. Integrated the published main contract into Alex, retaining both branches' handoff entries.
- New/changed interface: current coordination follows the updated main-first integration policy: inspect main for shared teammate merges and integrate those updates into Alex; never push Kristian or Tharun. The remote checkpoint found no new teammate commits beyond a47a618.
- Needs review: existing user/Claude changes to AGENTS.md and root README.md remain untouched and unstaged.

## Data-driven exposure surface and canonical-contract reconciliation — 2026-09-12
- Changed: added a demand-rendered Three.js surface inside the exposure panel: relative year x percentile x modeled exposure hours/year, proportional percentile spacing, selectable supplied vertices, sourced axis labels and inspector, drag/button/keyboard rotation and zoom, fixed-scale slider morphing, and reduced-motion support. A single year is a cross-section with no invented time width. Recharts fan and the sourced annual table remain available; WebGL failure/context loss falls back to the fan. No decorative animation, gradients, remote assets, live sampling, or backend calls.
- New/changed interface: the current shell's derived annual_series now retains {year:SV,p10:SV,p50:SV,p90:SV,p99:SV}, where SV is {value,source_type,ref}; p99 was already in the fixture, so its input schema did not change. Faces interpolate display geometry only; four quantiles cannot define probability density. Three.js is loaded as a separate local bundle.
- Outside /web: updated docs/frontend-api-contract.md to identify BUILD_PLAN.md as canonical and document the pending adapter migration (term_years, fractional flexibility_split, already-scaled by_year, block provenance/model sources, confidence, and absence of p10). The older fixture is explicitly documented as an internal shell format, not the target FastAPI response. BUILD_PLAN.md itself was published unchanged to main in b3cbb21 at the user's request.
- Validation: all 69 tests passed, including real Three geometry/camera/controls with only WebGLRenderer stubbed, surface inspector provenance, zero exposure, single-year data, transitions/cancellation/disposal, and app fallback behavior. TypeScript and production build passed. Vite reports large chart bundles (surface approximately 141 KB gzip; main approximately 191 KB gzip), with no build failure. Source audit found no banned visual effects.
- Needs review: GPU pixels and desktop/mobile visual layout remain unverified because the browser inventory is empty. Human/Claude should review the new quantile/interpolation copy. Canonical POST /api/estimate and confidence are not wired yet; do not invent confidence or p10 during that migration, and do not scale API exposure twice. Economics remain illustrative until sourced ASSUMPTIONS.md is shared. Existing AGENTS.md, FROM_CLAUDE.md, and root README.md changes were not edited by Codex.

## Explicit mock economics pending team sourcing — 2026-09-12
- Changed: re-read AGENTS.md and FROM_CLAUDE.md, then fetched main at task start and completion. docs/ASSUMPTIONS.md is absent on main (b3cbb21). Added persistent MOCK ECONOMICS and demonstration-only copy to the economics panel, MOCK DECISION beside the readout, explicit mock input labels, and a mock break-even caption beside the site slider.
- New/changed interface: JSON shape unchanged. Round dummy defaults are gpu_per_mw=1000, gpu_hour_value_usd=2, early_margin_usd_per_mw_year=500000, firm_wait_years=3. Each default uses source_type=assumption and mock://illustrative/economics-placeholder/inputs/<key>?pending=docs/ASSUMPTIONS.md. Current dummy break-even is 125 h/year (site_exposure approximately 0.5695); demonstration decision states occur at 0.4 / 0.55 / 0.9. These are mock arithmetic, not sourced estimates.
- Outside /web: updated docs/frontend-api-contract.md's dummy values and sourcing handoff. Economics sourcing belongs to Kristian/Tharun and will arrive via main; Codex has not independently sourced prices or substituted pipeline outputs for economic assumptions. Check main at each integration checkpoint, replace supplied values/units/refs together as soon as ASSUMPTIONS.md arrives, and recompute break-even; retain mock labels for any remaining placeholder dependencies. No runtime external fetches were added.
- Validation: all 69 tests and the TypeScript/production build pass; git diff --check passes. Existing Vite bundle-size warnings remain. AGENTS.md, FROM_CLAUDE.md, and the user's root README.md changes were left untouched.
- Needs review: sourced economics remain pending the team's ASSUMPTIONS.md on main. Do not remove mock labels just because values are editable or the UI looks finished. Canonical API migration remains separately documented.

## Canonical estimate response and confidence frontend checkpoint — 2026-09-12
- Changed: the shell now consumes the BUILD_PLAN.md POST /api/estimate response through createMockEstimate -> adaptEstimateResponse, with a seven-year supported/default horizon. Surface, fan, and table show only supplied p50/p90/p99 (no invented p10). Added sourced confidence beside exposure readouts/inspection, annual cost scenarios per quantile, mock tariff evidence with source controls, and canonical request/response JSON export. All default execution remains local/offline.
- New/changed interface: canonical types are web/src/model/contract.ts and the complete response example is web/src/model/mock-response.json. Request has exactly {location_id,load_mw,term_years,flexibility_split,site_exposure}; response has {inputs_echo,modeled_exposure,confidence,economics,tariff}. UI adapters inherit block source metadata and append field paths to each Sourced ref. source_type now supports model alongside data/clause/assumption. API-scaled exposure is not scaled twice. Local export is {mode,request,response,local_assumptions,result}.
- Contract refinements: breakeven_exposure_hours_per_year permits null at zero per-hour cost; by_year must cover the complete ordered selected horizon. BUILD_PLAN.md remains unchanged; refinements are documented in docs/frontend-api-contract.md for the API/pipeline owners. Confidence remains an explicit fixed MOCK Medium/0.5 fixture, not a model-derived result. Tariff records are explicitly unextracted placeholders without fabricated citations.
- Decision rule: compare term_cost[q]=annual_cost_usd[q]*term_years with the total early-connection benefit. not_worth_it if p50 term cost>benefit*1.05; worth_it if p90 term cost<benefit*0.95; close_call otherwise. These are marginal percentile-path comparison proxies, not quantiles of total loss. The slider marker is labeled median cost crossover, not a decision boundary. Mock defaults still produce worth/close/not at exposure 0.4/0.55/0.9.
- Outside /web: rewrote docs/frontend-api-contract.md around the now-shipped canonical shape and explicit integration semantics. No pipeline, extraction, backend, AGENTS.md, FROM_CLAUDE.md, or root README.md edits. Economic controls are local mock-only overrides, not extra request fields; agree a supported override interface before enabling HTTP so the server never appears to use unsent assumptions.
- Validation: all 69 frontend/model/geometry/provenance tests passed and TypeScript/production build passed; no banned visual effects added. Browser inventory is still empty, so GPU pixels and desktop/mobile layout remain unverified. Large local chart bundle warnings remain non-fatal.
- Needs review: API/pipeline owners should confirm the nullable threshold, full-year horizon, comparison policy, and worst_contiguous_outage_hours semantics before real-data use. That last field is a round authored mock retained in the contract, not displayed as site-outage evidence. Sourced economics still await Kristian/Tharun's ASSUMPTIONS.md on main.

## Validated HTTP boundary and full team foundation publication — 2026-09-12
- Changed: completed web/src/api/client.ts with opt-in postEstimate(request,{signal?,fetchImpl?}) and validateEstimateResponse(raw,request?). It validates the canonical five-field request, complete ordered horizon, quantiles, finite numbers, confidence, provenance, and matching inputs_echo. It preserves cancellation/network errors and never silently repairs responses, rescales exposure, or falls back to mocks. The demo still uses the local mock provider; no backend is enabled.
- New/changed interface: EstimateClientError exposes code invalid_request|invalid_response|http_error with optional path/status. HTTP uses same-origin POST /api/estimate. Endpoint shapes remain those in model/contract.ts and BUILD_PLAN.md, with previously logged nullable break-even/full-horizon conventions. README and frontend integration notes describe the prepared transport and unresolved local-economic-override handoff.
- Outside /web: updated docs/frontend-api-contract.md and this outbox. At Alex's explicit request, the foundation publication includes all repository source/contracts/docs, the user's existing AGENTS.md and README.md updates, requirements/gitignore, BUILD_PLAN/DATA_NEEDED/FROM_CLAUDE/HANDOFF, unchanged pipeline/ingest.py and __init__.py, and the complete web implementation. Publishing directly to main without a PR; user/Claude-authored files were staged as supplied, not edited by Codex. Ignored secrets, dependencies, build output, and cached data remain excluded.
- Validation: all 145 frontend/model/geometry/provenance/client tests and the TypeScript/production build pass. The HTTP client has 76 focused validation/transport cases; tests inject fetch and do not contact a backend. Main is an ancestor of this checkpoint, so publication is a fast-forward with no force push. Final tree/diff checks cover all requested foundation paths.
- Needs review: browser visual QA remains unavailable; existing chart bundle warnings are non-fatal. Pipeline ingest's live SPP verification remains as documented by Claude. This publication does not claim a trained model, real confidence/economics, tariff extraction, or a running FastAPI endpoint. Sourced ASSUMPTIONS.md has not appeared on main yet.

## Four checkpoints ending at the final submission deadline — 2026-09-12
- Changed: re-read AGENTS.md, FROM_CLAUDE.md, and BUILD_PLAN.md section 4. The on-disk table still had CP5, so applied Alex's explicit correction: CP1/CP2/CP3/CP4 at hours 12/24/36/48; CP4, Monday 12:00pm ET, is FINAL SUBMISSION. Removed the fifth checkpoint, moved rehearsal/integration completion before that deadline, and corrected BUILD_PLAN's time-budget description to 48 hours. Future execution planning uses this 48-hour deadline.
- New/changed interface: schedule only; no API or pipeline schema changes. Publishing all current repository changes directly to main with the exact commit message requested by Alex. Previously committed foundation files and the full web app remain included.
- Outside /web: edited docs/BUILD_PLAN.md per the user's correction and appended this outbox entry. AGENTS.md and FROM_CLAUDE.md were read, not edited. AGENTS.md still contains legacy 72-hour wording; the user's corrected four-checkpoint/final-deadline instruction takes precedence.
- Validation: verified exactly four scheduled checkpoint rows and the final Monday 12:00pm ET deadline, all requested foundation paths, and the staged diff. No runtime code changed; the prior foundation's 145 tests and production build passed.
- Needs review: Claude may update the protected AGENTS.md's legacy 72-hour wording to match the corrected plan. No additional post-hour-48 build stage is assumed.

## Local development server ready for human review — 2026-09-12
- Changed: started npm run dev in /web and left Vite running in a persistent terminal at http://127.0.0.1:5174/. Port 5173 was already occupied. The foundation/schedule commit b7687ac is now published on main at https://github.com/alphabyalex/East-v-west-hackathon-2026/commit/b7687ac.
- New/changed interface: none; the app still uses the canonical local mock response, with no running backend required.
- Outside /web: appended this coordination entry only; no application or protected-file edits.
- Validation: the local app URL returns HTTP 200. This verifies serving, not rendered layout or GPU pixels.
- Needs review: Alex will inspect the app in a browser, including the surface, slider recomputation, provenance controls, and explicit mock labels.

## Corrected tooling cutoff and final submission publication — 2026-09-12
- Changed: publishing the supplied docs/BUILD_PLAN.md revision directly to main. Four checkpoints remain at hours 12/24/36/48. CP4 on Monday at 12:00pm ET is the tech-stack cutoff; the actual final submission is Monday at 11:59pm ET. This supersedes my earlier outbox entry calling noon the final submission deadline. Tool-dependent coding must finish by noon; the remaining window is for video, rehearsal, and manual cleanup/submission.
- New/changed interface: none; pipeline and POST /api/estimate contracts are unchanged.
- Outside /web: publishing BUILD_PLAN.md as supplied and appending this outbox entry. The separate pending AGENTS.md change remains untouched and unstaged.
- Needs review: team members should use the two distinct deadlines in BUILD_PLAN.md section 4 when planning remaining work.

## Item 0 — shared deadline verified on main — 2026-09-12
- Changed: fetched main and verified docs/BUILD_PLAN.md already contains the correction published in 282b4eb: Monday noon ET is the tooling cutoff, Monday 11:59pm ET is final submission. No duplicate plan change needed.
- New/changed interface: none.
- Needs review: none for publication; proceeding through the requested frontend/backend checkpoints in order.

## Item 1 — typography and color replacement — 2026-09-12
- Changed: replaced system typography with locally bundled Google Fonts: Fraunces for brand/headings, IBM Plex Sans for UI copy, IBM Plex Mono for numeric data with tabular alignment. Replaced the old blue-gray palette across CSS, Recharts, and Three.js with near-black panels, warm off-white text, amber active states, steel-teal series, and muted decision colors. Font assets and OFL licenses are local so offline demos keep the intended type.
- New/changed interface: no data shape changes. Sourced now identifies numeric/string values with data-value-type for exact numeric font styling.
- Validation: font assets serve successfully; TypeScript/production build checked. Browser inventory is empty, so rendered desktop/mobile appearance remains unverified here.
- Outside /web: this outbox entry only. AGENTS.md and FROM_CLAUDE.md remain untouched.
- Needs review: human visual review of the new type/palette; proceeding next to the reported surface freeze.

## Item 2 — surface scheduling and safe default — 2026-09-12
- Changed: fan chart is now the default; Surface remains an explicit opt-in lazy import. Found synchronous draws on OrbitControls changes (including duplicate rotate/reset draws), unthrottled hover raycasts, redundant React label updates, and geometry/material recreation on every slider update. Replaced these with one coalesced demand frame, reused buffers, dirty-label updates, hidden-tab suspension, and complete initialization/error/context-loss/disposal cleanup. Reduced unnecessary multisampling/pixel load.
- New/changed interface: none; quantiles, provenance, and surface controls remain available.
- Validation: all 153 frontend tests and TypeScript/production build pass. New regressions cover event bursts, geometry reuse, idle frames, hidden tabs, initialization/draw failure, and numeric-animation cancellation. No unbounded mount-effect loop was found.
- Needs review: the reported real-Chrome capture hang cannot be reproduced here because no browser is connected. GPU/driver behavior is not claimed fixed; keeping WebGL off initial load is the requested demo safeguard. Existing chart bundle-size warnings remain non-fatal.
- Outside /web: this outbox entry only.

## Item 3 — running FastAPI and frontend HTTP integration — 2026-09-12
- Changed: added /api with POST /api/estimate, strict request/response validation, cached canonical mock JSON, and cheap scenario arithmetic behind a replaceable precomputed-location provider. Frontend now calls the endpoint through Vite's /api proxy to 127.0.0.1:8000 by default. Both providers remain explicitly illustrative.
- New/changed interface: request remains exactly {location_id,load_mw,term_years,flexibility_split,site_exposure}; response remains {inputs_echo,modeled_exposure,confidence,economics,tariff}, including full ordered by_year and nullable zero-cost break-even. Invalid requests/overflow return 422; unknown locations return 404. VITE_ESTIMATE_MODE=api|local controls startup. Export now includes {mode,status,response_origin,request,response,local_assumptions,result}.
- Behavior: 50ms debounce, 3-second timeout, cancellation/stale-response protection; pending/error output is a clearly labeled current-input local mock preview/fallback with Retry. Economic edits explicitly switch to local mode; Use API defaults resets them before HTTP, so no unsupported economic override fields are sent. API-returned exposure is not scaled twice.
- Outside /web: added api source/tests/minimal requirements/README, updated root README run instructions and docs/frontend-api-contract.md, and ignored .tools/.pytest_cache. Installed portable uv/Python and dependencies into ignored .tools/.venv because Python was absent; system PATH was not changed. No pipeline, AGENTS.md, or FROM_CLAUDE.md edits.
- Validation: 49 backend tests, 164 frontend tests, and 8 live differential HTTP cases through Vite/client/API/adapter pass; TypeScript/production build passes. API remains running at http://127.0.0.1:8000 and UI at http://127.0.0.1:5174/. Unit runs skip the 8 server-dependent cases; they were run separately with HEADROOM_API_URL. Non-fatal dependency deprecations, a OneDrive pytest-cache warning, and chart bundle warnings remain.
- Needs review: Kristian's real parquet/provider, sourced economics, and Claude's tariff extraction are still pending. api/README.md describes the adapter boundary. Root README's earlier product/model narrative still describes intended capabilities, now explicitly distinguished from this mock checkpoint; Claude should review its emergency-label claim against FROM_CLAUDE.md's fallback-label update.

## Item 4 — inline transparency and evidence panel — 2026-09-12
- Changed: added Model & evidence beside modeled exposure. The expandable panel explains system stress versus site applicability, shows the current sourced site assumption/confidence, clearly labeled mock reliability diagnostics, and tariff records with provenance. Moved the existing tariff evidence into this panel. Close/Escape restores trigger focus; Escape first dismisses an open provenance popover.
- New/changed interface: canonical estimate JSON unchanged. Local-only diagnostics in web/src/model/transparency.ts are {status:'mock',target:'system_stress',brier_score:SV,naive_brier_score:SV,reliability_curve:[{mean_predicted:SV,observed_fraction:SV}]}, where SV={value,source_type:'assumption',ref:'mock://...'}. Scores/curve are authored placeholders, not a held-out evaluation; they do not move with site exposure. Exact handoff is in docs/frontend-api-contract.md.
- Validation: 178 frontend tests and TypeScript/production build pass. Tests cover sourced scores/bins/axes, mock and missing citations, safe supplied citation links, panel expansion, slider synchronization, and keyboard behavior. The 8 live HTTP cases passed in item 3; no API implementation changed here.
- Outside /web: updated docs/frontend-api-contract.md and this outbox only.
- Needs review: Claude/human should review the new honesty and reliability explanations. Kristian must provide evaluation target/split, Brier/baseline, bin counts, and source/model version before replacing mock diagnostics. Tariff extraction remains pending; no fabricated citations were added. Browser visual/GPU review remains unavailable here.

## Item 5 — team integration and collaborator hygiene — 2026-09-12
- Changed: fetched origin. No new teammate merges exist on main; Kristian and Tharun remain at a47a618 and were not pushed. Main's corrected deadline remains intact. docs/ASSUMPTIONS.md is still absent, so economics remain clearly mocked. Publishing the completed frontend/API checkpoints to main as well as Alex.
- GitHub tooling: gh was absent; installed checksum-verified official portable gh 2.100.0 at ignored .tools/gh/bin/gh.exe without changing PATH. Existing Git credentials authenticated the authorized API calls as alphabyalex in memory only; no credentials were logged or persisted. Both requested collaborator PUT calls using Tharun.ekam@gmail.com and skbridge04@gmail.com returned HTTP 404 Not Found. No invitation was created.
- New/changed interface: no application contract changes. API and Vite remain running locally; runtime/bootstrap folders remain gitignored. Protected AGENTS.md's existing user edit remains untouched and unstaged.
- Blocked on: collaborator invites require confirmed GitHub usernames or manual invitations. Automatic approval review rejected the proposed GitHub public email-to-user search because sending those addresses for that separate lookup purpose was not specifically authorized; the search was not executed or bypassed. User approval would be needed for that search. The historical manual-invite note and HANDOFF.md redirect are left as-is; this outbox contains the current attempt results.
- Needs review: complete invitations by username or in GitHub's repository access settings. No other requested item is blocked.

## Final source audit after items 1–5 — 2026-09-12
- Changed: no further UI edits needed. Audited current source against AGENTS.md's ban list: no gradients, shadows/glows, glass effects, emoji icons, rounded card grids, marketing heroes, banned fonts, or decorative 3D. Theme variables resolve; all font assets are local; numbers and chart ticks use Plex Mono/provenance. Mock diagnostics and tariff placeholders remain explicit. Fan chart stays default; surface work stops while idle/hidden.
- Validation: 178 frontend tests, 49 backend tests, and 8 separate live HTTP contract checks passed, as did TypeScript/production build. Verified both local server URLs still respond. No additional runtime changes since those checks.
- New/changed interface: none. Exact estimate and local diagnostic shapes are documented in docs/frontend-api-contract.md and the source types.
- Needs review: actual rendered desktop/mobile appearance and Chrome GPU capture still need human review; no browser is connected to this environment. Use http://127.0.0.1:5174/ for the UI and http://127.0.0.1:8000/openapi.json for the API schema.

## Numeric motion checkpoint — 2026-09-12 17:25 PDT
- Changed: repaired fast-slider stalls in useAnimatedNumber: elapsed time now starts when each target arrives, retargeting from the displayed value with bounded cubic easing. Numbers settle exactly; hidden tabs, reduced motion, superseded targets and unmount cancel pending frames.
- New/changed interface: no API changes; presentation duration defaults to 320ms. Provenance still exposes the exact supplied target, never an animated intermediate.
- Validation: 12 focused motion tests pass, including rapid drag, reversal, exact settlement, stale callbacks, reduced motion, visibility and cleanup.
- Needs review: browser motion review remains unavailable because no browser is connected. Full visual polish follows in the next checkpoint.

## Visual polish and quiet honesty labels — 2026-09-12 17:26 PDT
- Changed: stronger median/net-value hierarchy, visible upper-tail quantiles, refined section spacing, tactile range handle and eased segment selection. Fraunces/Plex Sans/Plex Mono and the charcoal/amber/steel-teal palette remain. Replaced the global illustrative pill and economics banners with small source-dependent labels beside affected results, inputs, confidence and tariff records. Permanent USER ASSUMPTION, location coverage and system/site caveats retain their prominence and substance.
- Motion: numeric changes use the repaired easing; chart recomputation, chart-view selection, confidence-level changes, surface inspection and decision changes have finite eased feedback. Native inputs and ordinal axes remain exact. Reduced motion and hidden-tab cancellation remain supported; fan chart stays default and Three.js stays opt-in with no idle render loop.
- New/changed interface: POST /api/estimate JSON is unchanged. Mock labels inspect each supplied block's mock:// reference independently; API-connected does not imply real data. The frontend-derived crossover now preserves both economics and exposure dependency refs, and stays mock if either dependency is mock. Diagnostics remain the documented local authored fixture, not an API field.
- Main/data check: fetched origin twice during this task. No new teammate merges; main still contains only pipeline/__init__.py and pipeline/ingest.py in pipeline. No exposure_by_location.parquet locally/on main and no docs/ASSUMPTIONS.md. Backend therefore still serves its internal mock; no data-source switch or real-data claim was made. When real economics arrives, replace the Context's local defaults/control provenance and mode note alongside the backend assumptions; result-label readiness alone is not economics integration.
- Validation: 194 frontend tests pass, plus 8 separate live client/Vite/FastAPI contract checks; TypeScript and production build pass. Mixed-source tests verify exposure/confidence can lose mock labels while economics/tariff/diagnostics retain theirs, and the inverse crossover dependency remains marked. Source audit found no gradients, shadows/glows, glass effects, emoji icons or banned fonts; CSS tokens resolve. Existing large chart bundle warnings remain.
- Outside /web: appended this outbox only; no edits to AGENTS.md, FROM_CLAUDE.md, pipeline or backend. Publishing to Alex and main; teammate branches untouched.
- Needs review: inspect desktop/mobile hierarchy and actual eased motion at http://127.0.0.1:5174/. Browser inventory is empty, so rendered layout and GPU behavior are not visually verified. Claude/human should review the quieter labeling; provenance and permanent honesty claims remain intact. API remains running at http://127.0.0.1:8000.

## Shared placeholder assumptions scaffold and branch availability audit — 2026-09-12 17:49 PDT
- Changed: created docs/ASSUMPTIONS.md with a clearly labeled placeholder JSON block and round fake values: GPU rental 2 USD/GPU-hour, industrial electricity 50 USD/MWh, density 1000 GPUs/MW, early access 3 years, net margin 500000 USD/MW-year, close-call fraction 0.05. None is researched; ranges are arbitrary development ranges. File presence on main MUST NOT trigger removal of mock labels. Electricity is informational until sourced gross/net margin policy is agreed.
- New/changed interface: marked headroom:economics-assumptions:v1 JSON block, schema_version=1 and status=placeholder|mixed|sourced; each named input carries {value,source_type,ref,unit,source_url,retrieved_on,low,high}. All scaffold sources are assumption/mock://placeholder refs, with null retrieval metadata. API economics reader follows in the backend checkpoint; POST body/response shape unchanged.
- Branch audit after fetch: no pipeline/simulate.py, implemented get_location_estimate(), or tracked data/processed/**/*.parquet on any of eight local/remote tips. Alex/main/origin Alex/origin main=6bc29b3; local Kristian/local Tharun/origin Kristian=a47a618; origin Tharun=e433d4c (evidence-panel copy only). No local data/processed directory, including ignored files. No ASSUMPTIONS.md existed on any tip before this scaffold. Cannot see teammates' unpushed or gitignored local artifacts; real pipeline data is not available to wire here yet. Did not merge or push teammate branches.
- Outside /web: this task is backend-only with Alex; visual/landing work stays with Tharun. Publishing this shared assumptions scaffold to main and Alex; backend wiring follows on Alex. Protected AGENTS.md and FROM_CLAUDE.md untouched.
- Needs review: team must replace fake assumptions with sourced values/units/ranges and update status plus references together. Pipeline reader/parquet and tariff extraction remain pending.
## Local repository synchronized - 2026-09-12 21:34:05 UTC
- Changed: fetched origin and fast-forwarded local main from b3cbb21 to 93207c5, bringing in seven commits. The working tree was clean before synchronization; no merge conflicts occurred.
- New/changed interface: inherited the latest committed GitHub contracts and frontend; no additional implementation changes or pushes.
- Validation: local HEAD matches origin/main. This local handoff entry is intentionally uncommitted.

## Kristian offline ML prototype and walkthrough - 2026-09-12 23:19:20 UTC
- Changed: added common/prepare/label/features/train/confidence/simulate/workflow modules, requirements-ml.txt, 21 pipeline tests, docs/ML_WALKTHROUGH.md and an explicit label-policy template. Added a cached SPP load access check with annual-archive fallback to ingest.py; original dataset download behavior is unchanged. Installed a local Python 3.12 venv.
- Real data: cached SPP 2024 hourly-load archive and normalized it to data/processed/ml_inputs/spp_2024_load_only.parquet. 17,562 raw rows; 8,779 exact duplicates removed; one conflicting hour and two absent hours remain unknown; 8,784 hourly slots, 8,781 observed loads. The aggregate ID is SPP_SYSTEM, not a validated hub or site.
- New/changed interface: python -m pipeline.workflow supports fetch-sample, prepare-load, join-evidence, inspect, train, simulate, estimate. Input requires interval-start timestamp_utc, location_id, load_mw, and explicit observed/proxy evidence. No default scarcity threshold or silent label fallback. get_location_estimate(location_id, path=optional) implements BUILD_PLAN.md's read-only shape and LocationNotFoundError; research outputs stay inside each run directory until reviewed promotion to the canonical processed path.
- Validation: all 21 tests passed, including temporary synthetic-fixture training, test-label isolation, deterministic probabilities, missing-data rules, seasonal simulation and API schema. Preparation tests passed again after removing a dependency deprecation warning. pip check passed. Actual SPP download and 2024 data-quality inspection succeeded. No real event-labeled model has been trained; no test-fixture predictions populate the UI.
- Needs review: per AGENTS.md, human review of new label.py, chronological sigmoid calibration in train.py and model-boundary copy. Also review the experimental seasonal joint probability/residual bootstrap: stationary conditions, 365-day years, annual tails unvalidated, annual confidence capped Low, numeric score measures classifier agreement only. worst_contiguous_hours is defined as p99 of annual longest modeled episodes, requiring agreement with Alex. At least a full held-out year/all months is required for simulation. Do not copy SPP_SYSTEM estimates under node IDs.
- Blocked on: real matching hourly event/proxy evidence and the team's label definition. Load data alone cannot train the stress classifier. No GitHub pushes were made; changes are local for review.

## GitHub updates pulled with local ML work preserved - 2026-09-12 23:46:49 UTC
- Changed: fetched origin and fast-forwarded main from 93207c5 to 8361bcd (nine commits). Restored all local tracked/untracked ML work; resolved README and FROM_CODEX overlaps by retaining both workflows and both handoff histories.
- Validation: all 13 local ML/guide files match their pre-pull content; local HEAD matches origin/main. The Python environment and real-data cache remain present. No ML implementation edits or GitHub pushes in this sync.
- Recovery: retained stash b5255e5461852323d90975f85d2f70f3f614301d (codex-pre-pull-ML-work-2026-09-12) as a backup. Local work remains uncommitted.

## Area input and historical temperature features - 2026-09-13 00:22 UTC
- Changed: added `pipeline.weather`, cached Open-Meteo/GeoNames area lookup and ERA5 temperature ingestion, prior-hour temperature features, model-card temperature provenance, and `docs/TEMPERATURE_DATA.md`. Updated the ML walkthrough and README. Labels and calibration logic were not changed in this task.
- New/changed interface: `python -m pipeline.workflow add-temperature --hourly INPUT --area "City, State" --out OUTPUT` resolves an unambiguous US city, records a sibling `.area.json`, and joins temperatures by UTC hour. Optional `--location-id` selects an existing grid ID. Advanced `--locations JSON` accepts explicit points/weights. Outputs add temperature_c, temperature_area_min_c, temperature_area_max_c, and temperature_source_ref; a sibling `.weather.json` records sources, hashes and the area association. API/frontend contract unchanged.
- Real data: verified Amarillo, Texas as an example, not an inferred user location. Coordinates 35.222/-101.8313; joined temperatures to all 8,784 hours in `data/processed/ml_inputs/spp_2024_amarillo_temperature.parquet`. Original input columns match exactly, including three unknown load hours. Completed 2024 and 2025 calendar years are cached because the grid input crosses the UTC year boundary. Verified those actual caches work with network access forbidden.
- Validation: all 33 pipeline tests pass. Added checks for ambiguous cities, invalid mappings, Celsius/UTC/calendar completeness, leap day, cache reuse, exact joins, missing values, provenance, feature timing, location isolation, and temporary synthetic heat/cold model training. Synthetic thresholds are test fixtures only; no real-data predictive skill is claimed. `git diff --check` passes.
- Needs review: the user's weather-area/grid association, optional regional weights, and retrospective evaluation. Reanalysis does not establish historical real-time availability; only completed past calendar years are currently supported. City weather does not create city/site grid-risk labels. All work remains local and uncommitted.
- Blocked on: matching reviewed hourly event/proxy evidence for real-data training. No production model was retrained, no site cutoff probabilities were invented, and the website still uses its existing mock results.

## Accessible local ML workspace - 2026-09-13 00:47 UTC
- Changed: added `pipeline/workbench.py`, its local HTML/CSS/JS assets, `Open ML Workspace.cmd`, `scripts/start-ml-workspace.ps1`, seven workspace tests and `docs/ML_WORKSPACE.md`. Updated README and the existing ML/temperature guides. No new Python/Node dependency was needed.
- New/changed interface: localhost-only research workspace at http://127.0.0.1:8765. Area entry invokes the existing offline weather CLI as an explicit background job. Reviewed-event CSV upload plus a source/coverage reference invokes the existing join and training commands. Read-only views show real cached temperature/load data, provenance, saved model reports, confidence and artifact downloads. One job runs at a time; immutable inputs/results live under ignored data/processed/workbench. Requests cannot select arbitrary files or launch shell commands.
- Architecture: this is a separate research authoring workspace, not the /api estimate endpoint or /web demo. Page loads only read cached files. Explicit preparation/training jobs run via subprocesses outside HTTP requests. The demo API remains read-only with its current mock contract. Label rules, calibration and simulation code were not changed.
- Validation: all 40 pipeline/workspace tests pass, including disposable synthetic training with saved-model downloads and provenance. A live HTTP weather job successfully reused real Amarillo caches and joined all 8,784 hourly rows to `data/processed/workbench/inputs/weather_20260913T004530_5518e405.parquet`. Verified service health, static assets, data endpoints, and idempotent launcher behavior. `git diff --check` passes. Browser loaded the app/fonts/data successfully; visual inspection was unavailable because CUA reported no connected browsers/apps.
- Runtime: launched the service in the background and opened its URL using the launcher. It remains running. Reopen with Open ML Workspace.cmd; no terminal commands are needed for normal use. Source changes remain local and uncommitted.
- Needs review: the new research UI wording preserves the existing system-exposure/site distinction and explicitly shows the missing real-label state. Human label/calibration and rendered visual review remain appropriate before demo promotion.
- Blocked on: real matching reviewed hourly event labels for actual training. No real-data model or predictions were fabricated. The workspace is usable now for area/weather preparation and data inspection.

## Requested exposure-hours prediction: current-data audit - 2026-09-13 00:54 UTC
- Checked: all prepared hourly inputs and processed model artifacts. The three datasets represent the same 8,784-hour period, with 8,781 known loads and up to 8,784 Amarillo temperatures; none contains event labels, reserve-shortfall evidence, constraint counts or prices. No model.joblib, model_card.json, test_predictions.parquet or exposure_by_location.parquet exists.
- Output: saved `data/processed/prediction_readiness.json` with dataset hashes, coverage, artifact inventory and null exposure/site-cutoff estimates. Null means unavailable, not zero event hours. No model logic or UI changed.
- Blocked on: independently sourced event/proxy evidence and a fitted/evaluated model. Current load and weather alone do not identify a cutoff frequency. No unsupported numerical prediction was generated.

## SPP historical evidence and publication to Kristian - 2026-09-13 UTC
- Changed: cached official SPP reports and CHILLS filings; added docs/SPP_EVENT_AND_CHILLS_ANALYSIS.md, source-hashed docs/spp-event-evidence.json, pipeline/events.py and seven evidence/cache tests. Workspace includes evidence provenance. Prepared the local 2024 Amarillo/event join: 150 confirmed EEA1 minutes across three hourly buckets; preserves all input hours and missing loads.
- Interface: python -m pipeline.events --hourly INPUT --out NEW_OUTPUT adds observational EEA minutes/IDs and evidence JSON/CSV companions. It creates no training labels, preserves unknown coverage, unions duplicate overlaps and excludes local/other-region/date-only events. Label and calibration logic were not changed by this research task.
- Findings: exact August 26, 2024 EEA1 joins existing load/weather; five 2024 Conservative Operations dates have no verified intraday intervals; April 26, 2025 Shreveport local load shed stays geographically separate. July 1, 2026 accepted CHILLS tariff and September 2 proposal targeting Q2 2027 are distinguished. Source pages, hashes, observations and limitations are in the report/catalog.
- Validation: all 47 pipeline/workspace/evidence tests pass; seven evidence tests repeated after timestamp cleanup. A non-fatal pandas/NumPy timedelta deprecation warning remains. Actual real-data join succeeded; downloaded summer-report page visually checked. Git whitespace and scoped credential/large-file audit passed.
- Needs review: historical evidence catalog, region mapping, CHILLS interpretation and original prototype label/calibration code before production use. Complete event/negative coverage and site operating terms remain absent; no real-data trained model or site cutoff prediction is claimed. OATI archive TLS validation failed; certificate checks remain enabled.
- Git: user explicitly requested all changes on Kristian and no push to main, overriding AGENTS.md default branch ownership for this publication. Created local Kristian from current work; origin/Kristian is an ancestor. Publishing all source/docs/tests/launcher changes together; ignored raw/processed caches, environments and runtime files remain local. No force push or main update is authorized.

## Backend reader wiring, file-backed economics, and direct CORS — 2026-09-12 17:53 PDT
- Changed: POST /api/estimate now defaults to api/pipeline_provider.py, importing pipeline.simulate and calling get_location_estimate(location_id) when the callable and expected parquet exist. It validates section 1's location/year/quantile/confidence/model-version shape, retains model provenance, and applies site_exposure once in api/estimate.py. Missing module/function/precomputed file yields the same response shape with assumption sources and obvious mock:// refs including 'placeholder, pipeline not wired yet'; no silent real-looking fallback. Broken imports/dependencies/readers, malformed output or insufficient horizons return 503; unknown locations 404; invalid requests/arithmetic overflow 422. Tariffs remain explicitly unextracted placeholders.
- New/changed interface: section 2 JSON unchanged: request {location_id,load_mw,term_years,flexibility_split,site_exposure}, response {inputs_echo,modeled_exposure,confidence,economics,tariff}. All numeric descendants inherit their block's source; echoed inputs remain user assumptions. Added response header X-Headroom-Exposure-Source=pipeline|placeholder (exposure only). CORS allows exactly http://127.0.0.1:5174, POST and Content-Type, without credentials; direct calls to port 8000 need no proxy. Existing frontend proxy is unchanged.
- Economics: api/economics.py reads/validates the shared marked JSON on every request; no hardcoded numeric fallback. It records all input/dependency refs in the response. Industrial electricity is read but informational; current annual costs are gross lost rental value and early benefit uses independent net margin, avoiding a second power-cost subtraction. The tolerance remains 5% around early benefit compared to full-term p50/p90 paths; zero interruptible cost has null break-even. Missing/invalid assumptions are explicit 503 errors.
- Final branch/data recheck: fetched again after scaffold publication. No simulate.py or any tracked data/processed files on all eight tips: Alex/main/origin Alex/origin main=6cfdde3, local Kristian/local Tharun/origin Kristian=a47a618, origin Tharun=e433d4c. Local simulate absent and processed parquet list empty, including ignored files. Therefore the running endpoint still returns placeholders; real reader behavior was verified with injected test records only. This cannot inspect teammates' unpushed/ignored files on their machines.
- Validation: full repository pytest discovery passes 152 tests; complete frontend suite passes 194 tests with --maxWorkers=2, plus 8 separate direct HTTP/client contract checks. Real OPTIONS preflight and POST to port 8000 both returned 200 with the exact allowed origin and explicit placeholder metadata. TypeScript/production build passes. Initial Python temp setup failed on Windows permissions/missing parent, resolved with a new verified .tools/test-runs path and cache disabled; initial concurrently built frontend run had one lazy-load timeout, full bounded-worker rerun passed without changing frontend code. Existing dependency deprecations and chart bundle-size warnings remain.
- Outside /web: api source/tests/README, root README, docs/frontend-api-contract.md and this outbox; shared assumptions scaffold already pushed to main and Alex as 6cfdde3. Backend implementation is pushed to Alex. No /web, pipeline, AGENTS.md, FROM_CLAUDE.md, or teammate branch edits.
- Needs review: Kristian must supply the reader/parquet and export LocationNotFoundError; restart API after changing imported pipeline code. Tharun must keep labels until actual per-source status is sourced, and align local economic controls if reviewed file defaults change. Electricity/gross-net economics policy and tariff extraction remain unfinished sourcing work. API is running at http://127.0.0.1:8000; UI remains at http://127.0.0.1:5174/.

## Teammate push status check — 2026-09-12 18:06 PDT
- Changed: fetched origin; Tharun advanced from e433d4c to 7669f42. Five Tharun commits remain outside main: confidence overlap fix (71f9884), favicon (05527b9), local reliability trigger copy (e433d4c), landing page (b59c2a6), and economics sourcing document (7669f42). Kristian remains at initial a47a618. Main remains 6cfdde3 with nothing new to merge into Alex.
- New/changed interface: no local implementation change or merge. Tharun's ASSUMPTIONS.md is prose/tables without the backend's marked JSON block; it also explicitly flags gross revenue versus net margin and inconsistent grid/IT power bases. Reconcile these before adopting it; do not overwrite the loader-compatible scaffold or put gross revenue into the net-margin input unchanged. The proposed real/derived and real/cited source types in that doc are not canonical API enums.
- Needs review: Tharun's work is available on his branch but not yet main; branch policy still routes integration through main. Neither teammate branch contains simulate.py or tracked processed parquet. No teammate branches or UI files were modified. Citation accuracy was not audited during this status check.

## Kristian push confirmed — 2026-09-12 18:29 PDT
- Changed: fetched origin/Kristian at 2e4d9d2, adding ML preparation/features/labels/training/confidence/simulation, weather/event evidence, a local workbench, and tests/docs. pipeline/simulate.py now exports get_location_estimate(location_id, *, path=None) and LocationNotFoundError; missing precomputed output raises FileNotFoundError. No merge performed.
- New/changed interface: the reader now exists remotely, but no data/processed parquet is tracked on that branch. The reader expects data/processed/exposure_by_location.parquet. Main has no new commits to merge into Alex yet; backend still uses placeholders locally.
- Needs review: inspect the new reader's full return shape, ML tests, human-sensitive labeling/calibration, and offline output availability before integration through main. This status check did not run the new pipeline or establish that a trained model/parquet exists on Kristian's machine.

## Integration merges and real pipeline attempt — 2026-09-12 18:48 PDT
- Changed: merged Kristian 2e4d9d2 into main (c3d3e30), then current Tharun 59cbdda (2375f7b), then Alex's existing backend (2c11c96). Resolved outbox collisions by retaining both complete histories. Kept Tharun's landing page, navigation, favicon, confidence layout, and evidence copy; preserved source-dependent quiet honesty labels during dashboard conflicts. No pipeline label or calibration code was changed.
- Real pipeline attempt: installed requirements-ml.txt in the ignored local venv. `python -m pipeline.workflow fetch-sample` succeeded against https://portal.spp.org/file-browser-api/download/hourly-load?path=/2024/2024.zip (retrieved 2026-09-13T01:42:28Z). Cached 17,562 real rows. `prepare-load --raw data/raw/spp/access_check/2024_hourly_load.parquet --out data/processed/ml_inputs/spp_2024_load_only.parquet` succeeded: 8,779 exact duplicates removed; one conflicting hour and two absent hours remain unknown; 8,784 hourly slots, 8,781 known loads. Raw parquet SHA256: 9142040c00166449449683682778a87bcbb1e2f450355d23e6b4aac57180ecd4. Location is SPP_SYSTEM, a system sum, not a pricing node or site.
- Exact blockers: calling `label_hours` on that input with docs/ml-policy.example.json raised `ValueError: observed_event needs ['event_active']; do not substitute a different label silently.` The workflow `train --hourly data/processed/ml_inputs/spp_2024_load_only.parquet --policy docs/ml-policy.example.json --run-dir data/processed/integration_attempt` exited with `Cannot continue: Policy requires a nonempty label_ref.` The downstream `simulate --run-dir data/processed/integration_attempt` found no model_card.json. No model was trained and no exposure_by_location.parquet was created. An initial PowerShell quoting failure was corrected before these actual stage checks.
- Availability: pipeline/simulate.py and get_location_estimate now exist on main and origin/Kristian. No branch has a tracked data/processed parquet; our newly downloaded/normalized load caches are local and gitignored. Teammates' ignored caches cannot be pulled through Git. Selected official event episodes do not establish full negative-hour coverage. The simulator also requires at least a full year of held-out reference data; the current one-year load archive cannot meet that gate under the chronological training split.
- New/changed interface: canonical POST request/response unchanged. Integrating a read-only GET /api/economics-assumptions for the frontend to consume the exact file-backed defaults/provenance. The API will accept SPP_SYSTEM as an explicitly marked placeholder until matching precomputed output exists, without aliasing it to a site node.
- Needs review: real matching hourly event/proxy evidence, an explicit reviewed label definition, sufficient historical coverage, label/calibration logic, and experimental annual-tail methodology remain Kristian/team work. Exposure/confidence and unextracted tariff data remain visibly assumptions; a successful real load download does not make exposure real. Economics reconciliation and integration tests follow.

## Backend artifact wiring and economics reconciliation — 2026-09-13 01:52 UTC
- Changed: FastAPI calls Kristian's actual get_location_estimate(location_id, path=canonical_path). Matching model_card.json and simulation_metadata.json must accompany the parquet; the adapter checks source policy/hash/version/settings and confidence consistency. Model refs retain experimental annual-tail, Low-cap, classifier-agreement-only, and p99-longest-episode caveats. Missing pieces return explicit assumption placeholders; broken/mismatched data returns 503. Added parquet reader dependencies to api/requirements.txt. HTTP never fetches grid data or trains/simulates.
- New/changed interface: canonical POST /api/estimate unchanged. New GET /api/economics-assumptions returns the exact marked JSON block: schema_version=1, status=placeholder|mixed|sourced, and gpu_rental_price_usd_per_hour, industrial_electricity_price_usd_per_mwh, gpus_per_mw, early_connection_years, early_margin_usd_per_mw_year, close_call_fraction. Each is {value,source_type,ref,unit,source_url,retrieved_on,low,high}. GET/POST allow direct CORS from http://127.0.0.1:5174. BUILD_PLAN sections 1/3 and consumption documentation updated; section 2 body unchanged.
- Economics: kept Tharun59cbdda defaults of 575 GPUs/grid-MW, $3/GPU-hour, four-year early-access gap, and $317,000/MW-year. $10,577,700/MW-year is scenario GROSS REVENUE. $317,000 is his UNVERIFIED 3% operating-margin assumption applied to that revenue, rounded; not sourced net margin/net profit. No new haircut invented. Removed claims that this was a genuine margin or validated by a decision flip. Margin and 5% tolerance remain mock:// placeholders; status mixed. Other selected planning inputs remain assumptions attributed to Tharun's immutable research commit, without claiming independent external verification. Kansas $82.1/MWh is informational, not a site tariff. Cost uses gross rental value while benefit uses assumed operating margin; docs and result refs state the different bases.
- Validation: full Python suite passed 249 tests plus 19 subtests, including all merged ML/workspace/evidence tests. Adapter tests include a real temporary-parquet reader HTTP round-trip with training/simulation/network methods prohibited; disposable synthetic fixtures never populate demo data. Non-fatal upstream Starlette/httpx and NumPy timedelta deprecation warnings remain. Frontend/live HTTP checks follow.
- Outside /web: backend/API, dependencies, shared contracts and this outbox changed as requested. AGENTS.md and FROM_CLAUDE.md contents were not edited. The pre-existing user-authored AGENTS.md update is included unchanged in the requested complete publication.
- Blocked on: reviewed matching event/proxy evidence and sufficient history. No real-run exposure parquet exists; exposure/confidence/tariff placeholders remain honest.

## Tharun frontend HTTP integration — 2026-09-13 01:55 UTC
- Changed: retained Tharun's landing/navigation/design, with technical wiring only. Default API origin is http://127.0.0.1:8000 (VITE_API_BASE_URL override, explicitly empty for same-origin proxy). Each request also fetches GET /api/economics-assumptions; validated metadata drives economic controls, policy, and provenance. Cross-response arithmetic checks reject a mismatched file revision. Local mode/failure fallback remains usable, and local economic overrides are never sent as unsupported POST fields.
- New/changed interface: SPP_SYSTEM is the initial location and is labeled system aggregate with no site-specific coverage. City examples remain illustrative. The bundled offline assumptions snapshot exactly matches docs/ASSUMPTIONS.md's marked JSON. Estimate JSON remains canonical. Local derived economics retains mock provenance while its exposure or margin is placeholder. No landing, styling, favicon or decorative work added.
- Validation: 212 frontend tests passed; production TypeScript/Vite build passed with existing large-chunk advisory. Eight live GET/POST contract checks passed against the restarted API, including current defaults, slider changes, zero exposure/flexibility and different terms/locations. The real cached SPP load also reads successfully with network calls forbidden. Installed Python packages pass dependency compatibility checking.
- Team update: origin/Tharun advanced to d3b2770 during integration, fixing mixed economics provenance and stale tests. Its intent matches this integration; merging that latest commit next so the team history is included. origin/Kristian remains 2e4d9d2; no new main commits were present at the final fetch.
- Needs review: remaining ML evidence/model gaps are documented above; no demo exposure numbers were relabeled as real. API is running at http://127.0.0.1:8000 and Vite remains at http://127.0.0.1:5174.

## Sourced economics wired in and reconciled with the placeholder scaffold — Tharun, 2026-09-12/13
- Changed: independently sourced real economics inputs (docs/ASSUMPTIONS.md) in parallel with Alex's schema/pipeline-wiring work below, not knowing about it in advance. Reconciled on merge: kept Alex's machine-readable JSON schema and api/economics.py/pipeline_provider.py wholesale (strictly more robust than my own earlier direct-constant approach in api/estimate.py, which I discarded), and ported my sourced citations into that schema's six fields. Also shipped a landing page at `/` (workspace moves to `/app`), a mobile confidence-badge overlap fix, a favicon, and a local-reliability-issue tariff disclosure - all on `/web`, no conflict with backend work.
- New/changed interface: `docs/ASSUMPTIONS.md` status is now `mixed` - `gpu_rental_price_usd_per_hour` (3.00, H100 composite), `industrial_electricity_price_usd_per_mwh` (82.10, EIA Kansas), `gpus_per_mw` (575, grid-interconnection basis), and `early_connection_years` (4, LBNL Queued Up gap) are `source_type: "data"` with real HTTPS citations and a 2026-09-12 retrieval date. `early_margin_usd_per_mw_year` (317,000) and `close_call_fraction` (0.05, unchanged) stay `source_type: "assumption"`/`mock://` - no public opex/capex margin figure exists for AI-GPU-neocloud infra, so an explicit 3% assumed-margin-on-sourced-revenue derivation is documented in the file rather than treating the raw $10.58M/MW/yr gross revenue figure as a margin (that number breaks the decision math entirely - see the file's derivation section). `economics.source` still correctly stays `mock://` end-to-end per `is_mock = status != "sourced" or placeholder_exposure` - real sourcing here does not and should not flip that until exposure is also real.
- Validation: updated every test in api/test_economics.py and api/test_estimate.py that hardcoded the old scaffold's placeholder values (2/50/1000/3yr/500,000) or the old direct `mock://illustrative/economics-placeholder` ref format, retargeting the placeholder-provenance-validation tests at `close_call_fraction` (still genuinely placeholder) instead of `gpu_rental_price_usd_per_hour` (now genuinely sourced). All 152 backend tests, 178+ frontend tests, and both production builds pass. Verified in a real browser that the site_exposure slider still demonstrates all three decision states (0.4/0.55/0.9 → worth_it/close_call/not_worth_it) with the new real numbers, and that the landing page's live demo (same deriveScenario()) picked them up automatically.
- Needs review: the 3% margin assumption is mine, not cited - if anyone sources a real opex/capex-amortization figure for GPU-neocloud operating margins, replace it directly; the sourced $10,577,700 gross-revenue figure underneath does not need to change. Also flagging for Alex/whoever owns `/api` next: I did not add a `restart_overhead_minutes`, `checkpoint_overhead_pct`, `utilization_pct`, or SLA-tier field even though I sourced real data for them (see docs/ASSUMPTIONS.md's "Not yet wired into the calculation" table) - the six-field schema doesn't have room for them yet.

## Final concurrent Tharun merge — 2026-09-13 02:00 UTC
- Changed: included origin/Tharun d3b2770 and its intermediate Alex/Kristian merges. Its mixed-economics mock-label and test fixes are already represented by the tested integration; conflict resolution retains the direct HTTP/metadata wiring and matching offline snapshot. Preserved Tharun's additional outbox entry without duplicating the existing Alex history. No teammate branch was pushed.
- Reconciliation: selected density/rental/timing defaults stay assumptions rather than data because they are scenario choices/derivations; the research citations remain in full. The Kansas reference is attributed as reported, without new verification. The 3% margin remains explicitly unverified with mock provenance. This supersedes the historical Tharun outbox entry's data classifications and any claim that a decision flip validates the economics. No model output or UI design changed in this final merge.
- Validation: the final merge changes only the outbox relative to the tested integration; 249 Python tests plus 19 subtests, 212 frontend tests, 8 live HTTP checks, and production build have passed. Prepared real load remains in ignored local caches; no exposure model artifact is available.
- Publication: publishing the full integrated history to main. Frontend http://127.0.0.1:5174/app and API http://127.0.0.1:8000 remain running. Exact ML blockers and artifact handoff requirements are documented above and in BUILD_PLAN.md.

## Product name: Fluxline — 2026-09-13 02:03 UTC
- Changed: replaced the working product title with Fluxline in landing/workspace navigation and accessible names, both browser-tab titles, root/frontend README titles, economics-doc heading, scenario download filename, API documentation title, and ML workspace header/tab/launch message. Private frontend package name is fluxline-web. Text and name metadata only; no styling, layout, icons or graphics changed.
- New/changed interface: downloaded scenarios are named fluxline-scenario.json. JSON schemas, provenance markers, transport headers, environment variables and health identifiers remain compatible. Ordinary electrical transmission-headroom wording and historical outbox entries remain unchanged.
- Validation: production TypeScript/Vite build passed (existing chunk-size advisory only). Source audit found no remaining old product labels; live frontend HTML and OpenAPI titles display Fluxline. Package and lockfile names agree.
- Outside /web: README.md, docs/ASSUMPTIONS.md heading, api/main.py title, pipeline/workbench.py launch text and its HTML labels, plus this outbox, per the requested complete text rename. AGENTS.md and FROM_CLAUDE.md untouched. Publishing to main.
- Needs review: no design change; brand-mark/favicon artwork remains Tharun's design.

## Branch refresh and Tharun accessibility/presentation merge — 2026-09-13 03:15 UTC
- Changed: fetched all origin branches. Tharun advanced from d3b2770 to 042ece5: 1ed82eb aligns provenance-button accessible names with formatted visible labels, b800be4 incorporates prior main, and 042ece5 refines landing color discipline/closing-line spacing. Merged into main without conflicts as 9a83c72. These are Tharun's design changes; Fluxline branding is preserved. Kristian remains 2e4d9d2 and Alex remains ec9ae7f, both already integrated; origin/main had no incoming commits beyond our existing 595dcbb.
- New/changed interface: provenance primitives now compute accessible names from formatted/plain visible text; exact underlying values remain in provenance JSON. No API, model, schema, or runtime wiring changes.
- Validation: 212 frontend tests passed; 8 opt-in live tests skipped in the ordinary suite. TypeScript/production build passed with the existing large-chunk advisory. No backend changes required a Python rerun. Publishing merge and this log to main; teammate branches untouched.
- Needs review: nonblocking primitive API detail for Tharun: displayText is present in exported prop types but not forwarded as a caller override by Sourced/SourceInfo; current callers do not use it. No integration blocker found. No files outside /web changed except this outbox.

## Break-even sensitivity tornado chart — 2026-09-12 PDT
- Changed: added a Recharts horizontal interval chart on the results view, sorted by absolute dollar swing, with visible low/high-input decisions, exact-value provenance, keyboard-accessible axis sources, an endpoint table, bounded eased recomputation, and reduced-motion support. Tharun's existing landing/design remains the base; styles are scoped to the new panel.
- Accounting decision: user explicitly answered "Keep current formulas; mark electricity and utilization not modeled." GPU rental, flexibility split and site exposure vary independently; early contribution remains fixed. Rental affects gross interruption cost only, not the independent early operating-margin input. Electricity/utilization have documented sourced ranges but `status: "not_modeled"`, null outcomes and no zero-effect bars. Full flexibility/site ranges are user-assumption domains, not empirical confidence intervals. Bars show the change in the p50 annual-cost-path contract comparison, not a probability or a contract-value quantile; endpoint decisions retain the canonical p50/p90 rule.
- New/changed interface: POST /api/estimate and GET /api/economics-assumptions are unchanged. Scenario export adds `sensitivity: {baseline, rows, source, notes}`. Modeled rows have `{key,label,unit,baseline_input,status,low,high,swing_usd,source}`; endpoints contain `{input,delta_value_usd,snapshot,source}`. Unmodeled rows add `reason`, set swing/delta/snapshot to null, and retain sourced input ranges. Snapshot includes sourced p50/p90/p99 annual costs and comparison values, fixed early value, decision and nullable break-even hours. Exact TypeScript/JSON shape is in docs/frontend-api-contract.md#break-even-sensitivity-handoff; range/accounting policy in docs/ASSUMPTIONS.md#sensitivity-policy.
- Wiring: positive site factors recover unscaled exposure by inverse canonical arithmetic, without an extra HTTP request. At zero, a second canonical estimate with site_exposure=1 is necessary; source/model-version, confidence, scenario, exposure and economics consistency are checked before accepting it. Existing timeout/abort/stale-response handling covers that read. API failures stay explicit local mocks; the chart never combines API estimates with mock exposure. Strict-threshold rounding regression was fixed by matching canonical multiplication grouping and retaining received exposure for rental/flexibility sweeps.
- Validation: 264 frontend tests passed cleanly (256 unit/UI plus eight live FastAPI cases; live cases also compare every flexibility/site endpoint), production TypeScript/Vite build passed, and the full Python suite passed 249 tests plus 19 subtests. Existing large-chunk advisory and upstream Python deprecation warnings remain. Source audit found no banned styling in the new panel. Computer-use inventory returned no connected apps/browsers, so actual browser visual inspection remains for Tharun/human review at http://127.0.0.1:5174/app. Local API/Vite servers remain running.
- Outside /web: added policy/export documentation in docs/ASSUMPTIONS.md, docs/frontend-api-contract.md and a CP4 pointer in docs/BUILD_PLAN.md; no canonical API fields, machine-readable economic defaults, deadlines, pipeline code, AGENTS.md or FROM_CLAUDE.md changed. No standalone roadmap file was present, so implementation follows the explicit task and the available CP4 contract. Main was re-fetched with no incoming commits before publication. Pushing to main; teammate branches untouched.
- Needs review: economic margin/tolerance and exposure placeholders retain their existing mock status; this chart does not validate those inputs or introduce independent electricity/utilization effects. Team review can expand the accounting model later through a separately agreed contract.

## Ink and Signal visual restyle - 2026-09-12 22:48 PDT (uncommitted)
- Changed: replaced the amber/steel-teal frontend identity with the requested near-black surfaces, #212325 hairlines, #F2F1ED text, #8A8D88 muted text, and #7FD8CF active-state signal. Landing, workspace, fan/tornado/reliability charts, evidence panel, source popovers and surface colors now share the system. Tighter internal spacing, clearer section gaps, square controls, no gradients or shadows. Mock/model series remain neutral; mock and permanent site/confidence caveats retain their exact text and behavior.
- Brand/type: used the supplied octahedron and lowercase fluxline wordmark in both headers, vertically centered with the artwork's aspect ratio preserved inside its padded SVG canvas. Wired the supplied ICO, 32px PNG and Apple touch icon. Supplied SVG/favicon/brand masters were not regenerated or edited and remain untracked. Self-hosted Archivo 600 and Roboto Mono variable fonts plus OFL licenses from google/fonts (ofl/archivo and ofl/robotomono); retained Plex Sans body faces. Removed obsolete Fraunces and Plex Mono binaries/licenses. Heading/body font faces exclude ASCII digits so inline numbers also fall through to Roboto Mono; numeric components/axes/tables use tabular figures.
- New/changed interface: none. Canonical requests/responses, source objects, economics and sensitivity math, scenario state, slider semantics, animation hooks, confidence framing, and provenance mechanics are unchanged. No tests needed assertion edits and no tests were removed.
- Validation before -> after: frontend 264 -> 264 passed (256 unit/UI plus 8 live API cases, 16 files); Python 249 -> 249 passed plus 19 -> 19 subtests. Production TypeScript/Vite build passed. Existing large-chunk advisory and 17 upstream Python deprecation warnings remain. Source audit found no old palette/font references, gradients, or shadows in active /web source; protected AGENTS.md and historical outbox descriptions intentionally remain untouched.
- Browser review: isolated headless Edge checked landing/workspace at 1440px and 390px, including evidence panel and pinned source JSON. Actual loaded fonts are Archivo SemiBold, Roboto Mono and Plex Sans; favicon/preload requests return 200. No document horizontal overflow; the detailed sensitivity table retains its intentional internal horizontal scroll. Review screenshots are local/ignored under .tools/ink-and-signal. Vite remains at http://127.0.0.1:5174/app and the API at http://127.0.0.1:8000.
- Outside /web: repaired only the internal ML workbench's font allowlist and CSS font URL/alias in pipeline/workbench.py and pipeline/workbench_assets/style.css so deleting Plex Mono does not break that tool. Its broader visual identity is unchanged; no ML or API behavior changed. This outbox is the only documentation change outside /web.
- Needs review / limits: no unresolved /web restyling blocker. Hardware-rendered interactive WebGL appearance was not visually verified in the headless GPU-disabled review; only renderer colors changed, and existing render-loop/geometry/fallback tests pass. Existing landing claims about a trained ensemble and its unsourced preview/static figures predate this pass and need a separate content/provenance review; preserved them under the explicit visual-only restriction. AGENTS.md still describes the old identity and was not edited; this task follows the user's newer brief. Nothing staged, committed, or pushed; waiting for explicit publication instructions.


## Multi-factor high-demand exposure model and hours results - 2026-09-13 UTC
- Changed: added pipeline/history.py, generation.py, stress.py and hours.py; expanded prior-observation features with temperature/load interaction, load ramps and net demand. Added scored-period probability sums, monthly CSV, joint contract totals and local workspace hours/annual controls with an explicit site-exposure slider. Added safe explicit server restart, reproduction guide and a small version-controlled result snapshot.
- User scope: clarified that stress need not be an emergency and requested temperature plus load and other factors. The starting target is an explicit high-demand proxy (reference 95th percentile), not an inferred physical overload. Threshold 42,064.0228 MW was fitted on 2019 through 2021 only. No change to label.py or train.py calibration.
- Real data/model: cached 2019-2024 SPP load and generation plus Amarillo ERA5; 52,608 slots, 67 unknown loads and 1,029 missing complete hourly wind/solar observations. Fifteen-member model spp_lgbm_20260913T041851_30b0029de7 uses load, temperature, actual wind/solar, calendar and their past interactions. Expected held-out proxy hours 819.3906 versus 835 observed, across 10,426 scored hours; Brier 0.003016 versus prevalence 0.074131 and prior-status persistence 0.018415. These evaluate the demand proxy only.
- Annual research output: 2,000 seven-year seasonal trials, first-year system P50/P90/P99 834/950.10/1033.01 hours; joint seven-year totals 5876.5/6161.1/6387.0. Confidence Low. No growth/climate projection, no actual site cutoff forecast. Site assumption applied only in the workspace view, never baked into pipeline outputs.
- Interface: train now saves hours_summary.json/monthly_expected_hours.csv; workflow hours summarizes older runs, simulate additionally writes contract_hours.json. Workspace GET /api/run adds hours and annual; explicit POST kind=hours accepts an existing run and years=0 for scored-period summary or 1-7 for simulation. Read-only page loads never fetch/train. Model downloads and controls verified at http://127.0.0.1:8765. Existing /api and /web provider contracts unchanged.
- Validation: all 53 tests pass (new probability-sum/gap/duplicate/timezone/ensemble checks, joint-trial quantiles, past-only multi-factor features, generation gaps/conflicts and threshold train/test separation). Live hours job succeeded, live model/annual/download endpoints verified and workspace JavaScript syntax compiled successfully. Existing non-fatal pandas/NumPy warning in events.py remains. No connected browser visual verification is claimed.
- Needs review: demand-proxy validity, threshold choice, regional relevance of Amarillo, inherited calibration, confidence density and unvalidated annual tails. Reserves, constraints, outages, imports and prices remain unavailable in this fitted model and are disclosed. Private model/data caches remain local/ignored; docs/multifactor-model-summary.json records provenance/results.
- Publication: continuing the user-authorized Kristian branch workflow; do not push or merge main. The local workspace is running with the real model.
## Arbitrary location search and automated exposure reports - 2026-09-13 UTC
- User scope: accepts user-chosen cities/state or parcel coordinates, without a curated city list. Added local workspace search/candidate selection and an explicit scan-and-report job. PNNL's historical HIFLD utility polygons screen SPP applicability; other-grid matches do not calculate hours, and unverified coverage needs a saved explicit user assertion. This does not verify current utility service or post-2024 SPP expansion.
- Changed: pipeline/site.py orchestrates cached location-specific ERA5 retrieval, removes the original Amarillo weather, pairs the chosen point with shared 2019-2024 SPP load/wind/solar, fits the existing high-demand proxy ensemble, and simulates seasonal annual/term exposure. Cache keys include coordinates, source data and pipeline hashes/settings; completed model/simulation artifacts are hash-checked on reuse. No label.py or train.py/calibration changes.
- Report: annual system and assumed-site P50/P90/P99 clock hours, joint term totals, conditional MWh, exact prior-hour historical conditions, monthly and temperature comparisons, held-out evaluation, input coverage, grid/weather source URLs and hashes, and limitations. Facility MW and conditional share affect energy only. Site exposure remains an explicit downstream slider. Research confidence is Low; these are high-demand proxy scenarios, not actual site cutoffs, all forms of network stress, tariff-trigger predictions or future dates.
- Interface: POST /api/jobs kinds site-scan and site-report invoke the offline workflow; GET /api/site-scan, /api/site-report and allowlisted /api/site-download read saved results. Only server-registered search candidates can start a report. Existing host/token/origin protection and one-job limit apply. Demo /api and /web contracts unchanged. Docs/LOCATION_REPORTS.md explains the click-by-click workflow; local workspace remains running at http://127.0.0.1:8765.
- Real validation: Wichita, KS resolved to 37.69224,-97.33754 with historical SPP territory matches. Real weather downloaded/cached for 2019-2025 (2025 supplies the final UTC boundary of the 2019-2024 SPP archive). Full location model/report and HTML/Markdown/JSON downloads succeeded. First-year system P50/P90/P99 833/949.10/1034.02 h, expected held-out proxy hours 815.4402 over 10,426 scored hours (75 unscored), Brier 0.003053. All annual confidence Low. docs/location-model-summary.json records the example and provenance.
- Reuse validation: entered the same point directly as coordinates and requested 200 MW, 50% conditional load, 25% site exposure and three years. The exact fitted model/simulation was reused; first-year assumed site P50 208.25 h and conditional energy 20,825 MWh verified, with no new fit or simulation.
- Tests: full 61-test suite passed before final report polish; all 16 affected site/workspace tests passed afterward (9 new site tests; 62 total tests now). Checked JS syntax, UI IDs, live job/status/report/download interfaces, download traversal rejection and artifact provenance. No connected browser was available for visual verification. Existing events.py NumPy timedelta warning remains unrelated.
- Publication: follows the user's existing authorization to publish to Kristian only. No main push or merge. Raw downloads, private data/model caches and runtime files remain ignored; only code, tests, docs and a small example result snapshot are included. Human review of proxy suitability, applicability, inherited calibration and annual tails remains necessary before production use.



## GitHub updates merged into local Kristian - 2026-09-13 05:06 UTC
- Changed: fetched origin; Kristian already matched origin/Kristian at b67e767. Merged 27 shared-main commits through b15fb9a into local Kristian, preserving the location-report and multi-factor ML work. The only conflict was this handoff log; both complete histories were retained. Received AGENTS.md updates unchanged from main.
- Validation: all 62 ML/workspace tests pass. The running local workspace responds successfully, displays Fluxline branding and retains its location controls and saved real-data reports. No remaining conflict markers or whitespace errors.
- Scope: local synchronization only; no GitHub push and no model retraining. Frontend/API suites were not run because Node/frontend dependencies and FastAPI are absent from this machine environment. Incoming implementation outside the conflict was retained without extra edits.

## Learned warning signs and regional transfer - 2026-09-13 UTC
- Changed: added pipeline/signals.py for exact calibrated tree-contribution explanations, signed input groups, training-defined parameter comparisons evaluated on later observations, and similar historical hours. Added pipeline/regional.py to train on separate SPS, OKGE, LES and OPPD load-area high-demand proxies with representative local ERA5 weather and shared SPP demand/wind/solar histories. New query locations have unknown outcomes, preserved as missing. Existing train.py, label.py and their calibration are unchanged.
- Interface: the local workspace offers Learn stress signs across areas and Explain this saved model's warning signs. POST job kinds site-transfer and site-signals preserve registered-input, Host/Origin/token and one-job protections. Reports show parameter thresholds, missing inputs, direction agreement, historical comparisons, and validation on completely excluded areas. Inference hours use monthly probability expectations; no transferred P50/P90/P99 outcome quantiles are manufactured. Site exposure remains a visible downstream assumption. Demo /api and /web are unchanged.
- Real validation: model spp_regional_cc7d60f6ff429cb1e0cb completed all three excluded-area folds and the pooled fit. Every evaluation starts after that fold's calibration, including unequal-history cases. All four areas beat their training-prevalence Brier baseline, but hour calibration varies substantially: OPPD estimated 302.5 proxy hours versus 1,181 observed. Confidence remains Low; this does not validate actual cutoff behavior or new-site annual totals.
- Wichita example: 10,430 query hours scored, 72 unscored; stationary annual expected regional high-demand exposure 631.0057 h. At 100 MW, full conditional load and site exposure 1, conditional energy is 63,100.57 MWh/year. The highest-scored historical hour used prior temperature 34.5 C and SPP demand 50,887.832 MW, with temperature/demand interaction the largest positive explanation. Sources, input hashes, separate area thresholds and validation results are recorded in docs/regional-warning-summary.json; full raw inputs and models remain ignored local caches.
- Reuse verification: a second explicit report reused the exact model without fitting, with 200 MW, 50% conditional share, 25% site exposure and three years. Regional expected hours stayed unchanged; assumed-site annual hours 157.7514 and conditional energy 15,775.14 MWh verified. Existing Wichita system-model explanation also completed without retraining.
- Tests: all 71 ML/workspace tests pass, including calibrated contribution reconstruction, negative calibration slopes, unlabeled inference, missing observations, separate area labels, time/area exclusion, uneven-history leakage regression, annual coverage and invalid probabilities. Live report/status and all three downloads passed, traversal was rejected, and workspace JavaScript syntax and referenced HTML IDs passed. No connected browser visual verification is claimed. Existing events.py NumPy warning remains unrelated.
- Publication and limits: user-authorized Kristian branch only; no push to main or teammate branches. AGENTS.md and FROM_CLAUDE.md were not edited. The workspace remains at http://127.0.0.1:8765. Actual local constraints, reserves, outages, site interruption logs and operating-rule triggers are absent; explanations concern a research high-demand proxy, not a future cutoff schedule. See docs/WARNING_SIGNS.md for workflow and interpretation.

## Simplified location inputs and hours output - 2026-09-13 UTC
- Changed: replaced the local ML workspace's dense research screen with a responsive location/facility form and a prominent annual site-hours result. Inputs are location, MW, flexible share, visible site-exposure assumption and years. Supporting outputs show full-term site hours, annual MWh and regional high-demand hours. Removed warning-sign displays, model selection, training controls and research charts from the main screen at the user's request; underlying model/evidence and research API/CLI remain available.
- Flow: Estimate hours explicitly starts a location search and automatically continues to site-transfer only for a single match and unchanged captured inputs. Ambiguous matches require selection. Historical SPP confirmation is never automatic and resets when location changes. Saved results restore their original assumptions; editing inputs flags the existing result until recalculated. Late responses cannot replace a newer selection. No automatic jobs on page load or refresh.
- Output: all numbers remain modeled exposure with Low confidence, clear units and source access. Earlier saved simulations retain their P50 meaning and joint-term totals. Added a readable text export from saved values plus the full original JSON download. No warning-sign explanations are displayed or included in the plain-text summary. Updated README and local workflow/model guides.
- Validation: nine JavaScript behavior checks passed in the Node runtime, plus all seven workspace and nine site Python tests. Verified real saved Wichita full/quarter-exposure and legacy-simulation values, export units and source references. Live HTML/JS/CSS/fonts and report endpoints pass; all referenced HTML IDs exist. Computer-use inventory had no connected browser, so visual browser inspection was unavailable.
- Scope: static ML workspace assets, UI tests, documentation and this outbox only. No changes to models, labels, calibration, cached inputs, demo /api or /web, AGENTS.md or FROM_CLAUDE.md. Local app remains at http://127.0.0.1:8765; publishing to Kristian only under the user's standing instruction.

## Main synchronized into Kristian - 2026-09-13 UTC
- Changed: after publishing the simplified estimator as db0255e, fetched origin and merged main through ad01055 into Kristian as 83d1053 without conflicts. Received five main-side commits covering frontend comparison/climate/history features, 21-zone artifacts, optional XGBoost ensemble members and relaxed simulation-history requirements. Preserved the simplified local estimator.
- Validation: all 71 Python pipeline/workspace tests pass. XGBoost and FastAPI are absent from this local environment, so the optional XGBoost branch and frontend/API suites were not exercised. No model retraining or artifact regeneration was performed during synchronization.
- Needs review: incoming simulation now substitutes other available blocks for missing months; upstream annual outputs retain experimental status. If XGBoost is enabled later, pipeline/signals.py currently uses LightGBM-specific contribution methods and needs a compatible XGBoost adapter. These upstream changes were retained as requested; this sync does not certify their scientific validity.
- Publication: push the merged history and this handoff to Kristian only. No push to main, no changes to AGENTS.md or FROM_CLAUDE.md.

## Broader SPP locations and nearby weather - 2026-09-13 UTC
- Changed: added pipeline/location_data.py for city/state parsing, exact-name GeoNames selection with a cached Census fallback, historical SPP polygon checks and nearby western participant/member-utility territory checks. Added pipeline/location_weather.py for bounded nearby ERA5 fallback. Integrated these with the explicit site-transfer job and a short data-match note in the simple input/output UI and text export. No warning-sign panels were added.
- Policy: the user's request authorizes approximate regional comparisons. Accept historical utility matches, points inside/within 100 km of the historical SPP outline, or points/100-km neighborhoods matching documented western utility regions. Whole retail territories are regional proxies, not verified RTO/parcel membership. Sources and the assumed radius travel with each result. Western results explicitly use historical SPP East patterns as an analogy; no claim of training on the April 2026 expansion. See docs/SPP_LOCATION_COVERAGE.md for current primary membership references and the historical GIS mirror's limitations.
- Weather: requested coordinates first; on unavailable/incomplete data, try up to three nearest complete cached points then three nearby grid requests, within 100 km. Require at least 99% temperature coverage in every input month and retain one weather location across the whole history. Original site coordinates, provider grid coordinates, offsets, failed attempts, raw source references and hashes remain in the result. Unknown query load/outcomes are not replaced with reference-area values.
- Real verification: accepted representative cities in all 17 SPP states, including Havre/Lewistown MT, Durango/Colorado Springs/Fort Collins CO, Page AZ, Roosevelt UT and rural Plains cities. Houston and Seattle remain outside automatic comparison eligibility. Complete reports succeeded without a manual override for Colorado Springs (388.7336 annual assumed-site h), Page (800.0084 h), and Wamego (601.8216 h), each at 100% site exposure, using the existing trained regional model. A subsequent Fort Collins report also succeeded and was verified through the live API. These are Low-confidence high-demand-proxy exposures, not actual interruptions.
- Fallback verification: simulated provider failure near Wichita selected a real cached weather request 0.9 km from the original parcel, with an actual ERA5 cell 9.7 km away and complete monthly temperatures. Census fallback used the downloaded 32,350-place national directory. All external pulls remain ignored parquet caches; no invented weather or replacement local grid measurements.
- Checks: all 80 Python tests and 10 JavaScript behavior checks pass. Live HTML/JS/CSS, all four completed report responses, JSON/HTML/Markdown downloads and query input hashes verified. Existing events.py NumPy deprecation warning is unrelated. No connected browser visual inspection claimed. No changes to labels, calibration, regional model implementation, demo /api or /web, AGENTS.md or FROM_CLAUDE.md. Publish to Kristian only under the user's standing instruction.

## Saved before switching projects - 2026-09-13 UTC
- User requested preserving this project before starting separate work in EastVSWestOptica2. Verified implementation commit 5dfadfc is saved on GitHub's Kristian branch, with a clean local tree before adding this handoff. The latest workspace job succeeded and 17 completed reports plus three regional model files are present locally.
- Added docs/PROJECT_CHECKPOINT.md with launch instructions, artifact locations, publication constraints, current behavior, assumptions and validation references. Ignored local models/data/reports remain in place; they are not represented as GitHub backups. No changes to application behavior or models. Publish this documentation checkpoint to Kristian only, then await the new project's requirements.

## Team integration paused by user - frontend checkpoint 656e4fc
- Changed: fetched main ec8d203, Kristian b67e767 and Tharun 0adc04a. Saved the approved Ink and Signal frontend/assets in local main commit 656e4fc. Prepared merges only in isolated .tools/integration-ink/checkout on integration/ink-signal-ml (7bd8017). Its /web and brand Git trees exactly match the frontend checkpoint. Root app files and running servers remain on the preserved frontend; nothing was pushed.
- ML finding: main's published artifact is the separate 2024 observed-event run spp_lgbm_20260913T055001_b63ee2577e, with only October 20-December 31 held-out history. Its missing-season substitution cannot support the claimed seasonal annual output. Retained strict simulator guards in the isolated merge. Kristian's newer 2019-2024 multifactor code/summaries are included there, but its matching actual run bundle remains absent.
- New/changed interface: no published change. Isolated uncommitted API readiness guard/tests and API/BUILD_PLAN documentation require a full held-out year and 8760 scored hours per location before serving annual model provenance; insufficient evidence retains explicit placeholders. Focused API tests passed 127; focused merged ML checks passed 23 plus 16 subtests. Full combined suite, live round-trip, final build, bringing integration into root main and push are still pending.
- Paused: user explicitly asked to return only after Kristian pushes the previously ignored files. Resume from the saved integration checkout, re-read team instructions/updates, and re-fetch then. Required newer-run bundle: exposure_by_location.parquet, model_card.json and simulation_metadata.json together; for model iteration also model.joblib, test_predictions.parquet, simulation_trials.parquet and input/policy manifests. Do not reconstruct artifacts from rounded JSON summaries or overwrite the preserved frontend.

## Landing preview label shortened - 2026-09-12 (uncommitted)
- Changed: removed the requested "— SAME MATH AS THE FULL TOOL" suffix from web/src/Landing.tsx; label now reads "TRY IT". No styling, calculation or provenance changes.
- New/changed interface: none. Scoped diff/search checked; no new tests needed for this text-only deletion. No commit or push.
- Needs review: integration remains paused. Preserve this newer root frontend edit when resuming the isolated integration checkout; it intentionally postdates frontend checkpoint 656e4fc.

## Client-facing scenario copy - 2026-09-12 23:49 PDT
- Changed: removed TRY IT entirely. Replaced visible illustrative/mock/developer wording with scenario locations, assumed exposure/economics/sensitivity, supplied defaults, evaluation unavailable and tariff evidence not supplied. The landing preview explicitly remains an assumed scenario. Source-dependent labels still disappear only when their data earns it; unavailable diagnostics and unverified economics remain disclosed.
- New/changed interface: no request/response, data, calculation or source-object changes. Exact original references, confidence basis and tariff records remain available in provenance inspection/export, including historical mock://illustrative identifiers. Ink and Signal styling, fonts, assets and layout are unchanged.
- Validation: all 264 frontend tests passed, including eight live cases against the merged API on temporary port 8001. Updated only affected copy assertions; no tests removed. This frontend supersedes the earlier TRY IT-only edit and will be retained in the resumed integration.

## Latest Kristian integration with frontend preserved - 2026-09-12 23:51 PDT
- Changed: resumed at user request; fetched Kristian c53ac3c, main ad01055 and Tharun 0adc04a. Integrated Kristian's multifactor, regional transfer, warning-sign and simplified ML-workspace changes, including inherited main history. Tharun's history is included through main; incoming /web comparison/telemetry/router changes were excluded to preserve the user's chosen frontend. Final /web and brand trees exactly match frontend checkpoint 389d121, including all requested client-facing copy edits. No teammate branch was pushed.
- Artifact status: only the three canonical files were published, now model spp_lgbm_20260913T060709_b63ee2577e. They still use 2024 observed-event labels with 1742 held-out clock hours and 1717 scored hours per location, not Kristian's newer six-year multifactor/regional model. The newer actual run files, trained model and caches remain absent; summaries are not reconstructed into artifacts. The published run's held-out Brier 0.005033 is worse than its 0.002905 baseline; annual confidence remains Low.
- Backend protection: retained strict one-year/all-season simulator guards and the one-year rejection test removed upstream. Added an API readiness check using inclusive split endpoints and per-location scored hours; missing/insufficient annual evidence yields the existing explicit assumption placeholder with model/version/reason, while malformed/mismatched artifacts still fail. A live POST against the pulled canonical artifact returned 200 with X-Headroom-Exposure-Source=placeholder and the exact 1742/1717-hour reason. No silent substitution of missing seasons or node aliases enters the frontend.
- New/changed interface: canonical POST/GET shapes, economics math and source objects unchanged. BUILD_PLAN.md and api/README.md document annual readiness and the exact three-file handoff. New regional inference supplies expected hours only, so it is not substituted into p50/p90/p99 fields. All heavy operations remain explicit offline ML-workspace jobs; the estimate API only reads saved output.
- Validation: full Python suite 299 passed plus 30 subtests; all nine standalone estimator JavaScript checks passed; full frontend 264 passed including eight live cases against the merged API; production build passed. Existing bundle-size/deprecation warnings remain. Protected AGENTS.md/FROM_CLAUDE.md and label.py are unchanged. Restored strict simulation test rather than accepting a weakened test.
- Needs review: incoming optional XGBoost training path was retained from teammate code but XGBoost is absent in this environment and undeclared in requirements-ml.txt; passing tests exercise LightGBM. pipeline/signals.py still assumes LightGBM contribution methods and needs an adapter before mixed-ensemble explanations can run. Calibration/model-family settings and all target/annual research assumptions still need ML-owner review. No new model fit or external grid-data fetch was performed. Obtain the newer matching parquet/model-card/simulation-metadata bundle (and model.joblib, predictions/trials/input manifests for iteration) before activating it.
- Publication: completing the user's authorized main integration and push; existing Ink and Signal assets/design and the latest copy are preserved. Local API will be restarted with the readiness guard; Vite remains at http://127.0.0.1:5174/app.

## Scenario location catalog wired to parquet - 2026-09-13 20:56 UTC
- Changed: `/app` now fetches selectable IDs from new `GET /api/locations`, reading only `location_id` from the configured exposure parquet. Chose a dynamic endpoint so future artifact IDs appear without a frontend release. Current catalog contains 17 load zones, SPP_SYSTEM, and the three existing demos: 21 options total. Zones are labeled `<ID> · SPP load zone`; demos retain `· scenario`. No styling, model, economics, carbon, existing estimate contract or artifact changes.
- New interface: `{"locations":[{"id":"SPP_SYSTEM","label":"SPP system aggregate","kind":"system"},{"id":"CSWS","label":"CSWS · SPP load zone","kind":"zone"},{"id":"spp-wichita-demo","label":"Wichita, KS · scenario","kind":"scenario"}]}` (example entries, not the complete catalog). `kind` is `system|zone|scenario`; IDs are unique. System comes first, zones sort by ID, demos come last. Missing parquet retains the original four choices; a present malformed/unreadable catalog returns 503. The endpoint reads local precomputed data only and does not claim annual estimate readiness.
- Frontend: selecting a zone posts its exact ID through the existing estimate transport. Bounded/cancellable catalog loading retains usable choices on failure and does not reset a selected ID. New-zone pending/offline rendering reuses the unchanged system mock formula with explicit assumption/mock provenance identifying the requested zone and generic fixture; this also prevents the results/chart-axis path crashing on a previously unknown ID. The existing fallback status remains visible. Original four-location arithmetic and all HTTP response source objects remain unchanged.
- Real-data verification / blocker: live GET returns all 21 choices. Live POSTs for **CSWS, OKGE and LES each return 404**, not real modeled-exposure values. Direct `get_location_estimate()` can read these IDs, but the existing provider rejects their 1742-hour held-out span / 1717 local scored hours against its 8760-hour annual requirement. Its existing placeholder set has no real-zone entries. Model `spp_lgbm_20260913T060709_b63ee2577e`; parquet SHA256 `ffe0e5e3f633fd19065611cae868f6066bb77dfc1803a271d955f4213671ce35`. No guard was bypassed and no model provenance was invented. Actual three-zone real-output acceptance remains blocked until the ML owner supplies a valid matching annual artifact bundle; fixing that is outside this dropdown-only task.
- Tests distinguish catalog membership from model readiness: authored full-year software fixtures for CSWS/OKGE/LES pass through the actual parquet reader/provider/POST with model-sourced output, while a short-history fixture still fails. Frontend tests cover all 21 IDs, exact three-zone requests, future IDs, catalog errors, pending/404 fallback, and the complete results view. These fixtures are explicitly test data, not claims about the production artifact. Live contract coverage also checks the current catalog.
- Validation before changes: full Python 1,624 passed + 30 subtests; frontend 267 passed; production build passed. The initial concurrent baseline frontend run had 266 passes / one existing 5-second App-test timeout while Python ran; the unchanged serial rerun passed all 267. Final suites ran serially: Python **1,643 passed + 30 subtests**, frontend **287 passed** including nine live API tests, TypeScript/production build passed. Same 17 Python deprecation warnings and existing Vite chunk-size warning; no tests removed. Independent review found and corrected a malformed array-valued catalog-kind validation gap.
- Scope / publication: API catalog plumbing and this outbox are the authorized changes outside `/web`; protected AGENTS.md/FROM_CLAUDE.md and all three canonical data artifacts retain their hashes. Restarted the existing local API at 127.0.0.1:8000; Vite remains at 127.0.0.1:5174/app. Committing/pushing this task to **Alex only**, with no merge or push to main or teammate branches. Overnight wind-classifier/carbon/standalone-grid-impact work and its integration limits are recorded separately in `docs/FROM_ALEX.md`.

## Serve precompiled grid-impact snapshots over HTTP - 2026-09-13 21:16 UTC
- Changed: added `GET /api/grid-impact/{location_id}` in `api/main.py`, calling `read_live_grid_impact` -> `read_grid_impact_snapshot` with a retained, bounded validation cache. Requests only read precompiled files; no raw composition, fetching or training. Existing API handler ASTs, including POST /api/estimate, are unchanged; no frontend, economics or pipeline files changed.
- Interface: unchanged `grid-impact-v1` result with `location_id`, `schema_version`, `boundary`, `units`, `basis`, `coverage` (wind/shift), `evidence_context`, `evidence`, and six sourced quantities: `wind_absorption_mwh_in_observed_hours`, `wind_absorption_mwh_per_year`, `carbon_absorbed_tonnes_in_observed_hours`, `carbon_absorbed_tonnes_per_year`, `carbon_shifted_tonnes_in_observed_hours`, `carbon_shifted_tonnes_per_year`. Every quantity is `{value: number|null, source_type, ref}`; local evidence pointers resolve within the returned dictionary. This GET serves a fixed offline capacity scenario, independent of /api/estimate inputs. Valid partial/unavailable snapshots return 200; missing files/IDs 404; invalid/unreadable files 503; unsafe IDs 422. Success/404/503 carry `Cache-Control: no-store`; existing CORS permits 127.0.0.1:5174.
- Offline bundle: explicitly included `data/processed/grid_impact/live_v1/` in this commit despite the general processed-data ignore. Compiled 21 unavailable catalog snapshots with `compile_grid_impact_snapshot(id, path)` and copied eight verified 2025 point snapshots byte for byte; 29 snapshots + manifest, 3,418,702 bytes. No original artifacts were overwritten. Default serving uses that directory; `FLUXLINE_GRID_IMPACT_DIR` can select another compiled bundle. Manifest records exact IDs and source/file hashes; staged Git blobs match all 29 snapshot hashes.
- Verified observations: AEPM_CSWS, LES_LES, OKGE_OKGE, OPPD_OPPD, SPS_SPS, WFEC_WFEC, SPPNORTH_HUB and SPPSOUTH_HUB return existing real-observation-based wind capacity scenarios. Examples: LES_LES 38,200 MWh, OKGE_OKGE 15,250 MWh, AEPM_CSWS 1,400 MWh in evaluable 2025 hours, under explicit 100 MW / 0.5 upward-availability assumptions. Output provenance remains assumption; these are not measured recoverable energy. All eight have 8,687 evaluable / 73 unknown hours; annual quantities and carbon shifted stay null. Associated wind operational CO2 is zero, not a carbon offset or atmospheric removal.
- Unavailable / needs review: all 18 catalog aggregate/zone IDs (CSWS, EDE, GRDA, INDN, KACY, KCPL, LES, MPS, NPPD, OKGE, OPPD, SECI, SPP_SYSTEM, SPRM, SPS, WAUE, WFEC, WR), plus the three demo scenarios, return six sourced nulls and unavailable coverage. Exact point identifiers are not load-zone aliases; no reviewed mapping or complete exact-zone input/supported shift schedule exists in the supplied bundle. No real location has complete annual/shifted-carbon output here. The complete-data HTTP branch is verified with explicitly authored software fixtures, not substituted production numbers.
- Validation: Python before **1,643** -> after **1,673 passing**, both +30 subtests and the same 17 deprecation warnings; frontend **287 -> 287 passing**, including nine existing live API cases; production build passed with the existing chunk-size warning. Added 30 tests covering real published snapshots, complete/partial/unknown authored cases, source preservation, no composition on requests, corruption, missing files, cache deletion, CORS and path handling. All 29 actual live HTTP responses equal their validated snapshots; `http://127.0.0.1:8000/api/grid-impact/LES_LES` is running. Verification `.tools/grid_impact_live_http_verification.json` SHA256 `207d500476520756d1fd0349807605e41c27bfce930486c46ca8f26e9bf59fce`.
- Publication: source edits confined to api/grid_impact.py and api/main.py, plus the expressly requested tests, compiled snapshots and this log. Protected team files, canonical artifacts and model files are unchanged. Commit/push to **Alex only**; no main or teammate-branch push. Frontend consumption can now use the additive endpoint without a proxy; UI integration remains with Tharun.

## Six documented zone references enabled - 2026-09-13 21:47 UTC
- Changed: `GET /api/grid-impact/{location_id}` now exposes existing settlement-point scenarios for **CSWS -> AEPM_CSWS, LES -> LES_LES, OKGE -> OKGE_OKGE, OPPD -> OPPD_OPPD, SPS -> SPS_SPS, WFEC -> WFEC_WFEC**. This is a within-area network-load price reference, not an assertion that a point measures the whole zone. All original snapshot bytes and quantities remain unchanged; no model, economics, frontend or POST /api/estimate change.
- Justification: `pipeline/prepare.py` defines the component load-area IDs; `pipeline/regional.py` has city/area references but no settlement crosswalk, and `site.py` supplies none. [SPP Protocols v38, section 6.7.3(1), printed p679](https://www.spp.org/documents/39052/integrated%20marketplace%20protocols%2038.pdf#page=679) documents the repeated-area network-load name. [SPP Markets UI Guide v21, Query Settlement Locations, printed p36](https://www.spp.org/documents/66792/integrated%20marketplace%20market%20user%20interface%20guide%20v21.0%2020230612.pdf) identifies aggregate CSWS_LA with AEPM_CSWS. All twelve cached official 2025 monthly price files independently confirm the six exact Location/Pnode pairs; no suffix-only guessing, city proximity, or hub assignment was used. The code whitelist is limited to the reviewed 2025 observation period.
- Interface: mapped live responses keep `location_id` equal to the requested zone and add `location_mapping: {requested_location_id, source_location_id, pnode, relationship, scope, source: {source_type: "data", ref}}`. Its relationship is `network_load_settlement_location_within_settlement_area`; scope is `point_reference_scenario_not_zone_total_or_site_deliverability`. Each scalar retains its original point datum in the evidence graph plus mapping evidence and an explicit reference-scenario assumption; numerical values and existing assumption source types are unchanged. This optional metadata extends only the live response; exact-point responses and the strict stored snapshot schema are unchanged. Frontend consumers should retain the named source point and scope when displaying a mapped result.
- Availability: **6 of 21 dropdown choices now return sourced observed-period wind scenarios; 15 remain unavailable**. Remaining IDs: EDE, GRDA, INDN, KACY, KCPL, MPS, NPPD, SECI, SPRM, WAUE, WR, SPP_SYSTEM, and the three demo scenarios. There is no reviewed association from those entries to the eight supplied snapshots; the two trading hubs cannot stand for a whole system/zone. All annual and shifted-carbon totals still remain null. The six values still use the published 100 MW / 0.5 upward-availability assumptions, with associated wind operational CO2 zero; they are not measured recoverable wind or carbon offsets.
- Verification: all 21 live endpoint requests passed, with exact point quantities and final evidence references preserved; six mapped and fifteen sourced-null results. Added 14 regressions, including direct-zone-data priority, missing/corrupt/deleted references, reviewed-year bounds, no arbitrary name/hub mapping, immutable originals and complete source retention. Full Python **1,687 passed + 30 subtests** (14 more than the previous checkpoint), frontend **287 passed** including live API cases, production build passed; existing 17 Python warnings and Vite size warning remain. Final 44 HTTP tests also passed after evidence encoding was made independent of Windows line-ending conversion.
- Evidence / publication: committed `data/processed/grid_impact/live_v1/zone_mapping_evidence.json`, SHA256 `daf3fc27f105421a0c992f1a45e02a67f2e4566be4d4514415b5a0042eea5bed`, records the primary sources, exact pairs and all twelve source/cache hashes. The serving manifest documents the new resolver behavior without rewriting old snapshots. Local API is running at 127.0.0.1:8000 with the final mapping. Push this code, tests, evidence and log to **Alex only**.

## Grid Impact on /app: wind, carbon framing, cheap-power scenario and surface — 2026-09-13
- Changed: the scenario results now request `GET /api/grid-impact/{location_id}` independently of estimate readiness whenever the selection changes. Added an Ink & Signal panel with wind-consumption scenario, cheap-power value, carbon shifted, associated wind operating emissions, sourced monthly chart/table, evidence download and visible unavailable/error/retry states. Load changes recompute locally; old-zone replies are aborted/ignored. No estimate contract, economics, exposure math, ML, ranking, landing page, AGENTS.md or FROM_CLAUDE.md changes.
- Formula: `USD = selected_load_mw * upward_available_fraction * SUM(-LMP_usd_mwh * 1 hour)` across evaluable hours where system wind/load >= 0.5 AND exact-point day-ahead LMP <= 0. The reference is explicitly **$0/MWh**, avoiding an invented normal/retail tariff. Zero-price hours add MWh but no dollar premium. `MWh = selected_load_mw * upward_available_fraction * qualifying_hour_count`. Upward availability starts at the published 0.5 and has its own visible adjustable control; it is not site_exposure or interruption flexibility. All resulting numbers are `source_type: assumption`, with exact load, fraction, formula and original evidence in their refs. This is conditional wholesale energy value, not measured savings, guaranteed retail pass-through, local deliverability or verified curtailed-wind capture.
- Offline evidence: new `api/grid_power.py` compiles the pinned `data/processed/wind_audits/proxy_ver_2025_v1/hourly.parquet` (SHA256 54f0692fdbf0fca2fd724e0fed699bf350dad873cb009afea387e3faafe4b2f2). It independently reproduces every published count and creates 12 x 24 UTC month/hour bins per point. Committed `data/processed/grid_impact/live_v1/cheap_power_v1.json` is 1,979,415 bytes, SHA256 28809ff68bbc7605f47f676b7442d6e0d2bbfb76204b8472a3e5dea01a6e3249, identified by the serving manifest. Serving reads only compiled JSON, validates integrity/identity/counts, and binds price evidence to the exact wind snapshot; missing/stale prices stay unavailable, malformed evidence follows existing 503 handling. No raw hourly reads, external fetching or classification in HTTP. Old point snapshots remain byte-for-byte unchanged.
- Additive live interface: `cheap_power` is either `{status: "unavailable", reason: string, bins: []}` or `{status: "observed_hours_only", source_location_id: string, snapshot_sha256: string, basis: string, input_source: {source_type, ref}, reference_price_usd_mwh: S, proxy_hours: S, unknown_hours: S, usd_per_available_mw: S, bins: [{month: S, hour_utc: S, proxy_hours: S, unknown_hours: S, usd_per_available_mw: S}]}`. `S = {value: number, source_type: "assumption", ref: string}`; bins are ordered by month 1–12 then UTC hour 0–23. Month/hour coordinates are calendar values; per-MW dollar quantities integrate one-hour LMP observations. `grid-power://...` bin refs resolve through returned `cheap_power.input_source` and `basis`; upstream wind/carbon DAG references are retained in the complete downloadable response. Existing six grid-impact quantities, source graph, location_mapping and `/api/estimate` shape/math remain unchanged. Frontend validates transport before any multiplication and treats older servers without cheap_power as price-unavailable.
- Visualization: Monthly / Surface toggle; the surface base is **month x UTC hour**, height **observed wholesale scenario USD**. This makes the timing and dollar concentration visible without inventing contract-year forecasts or percentiles. The inspector shows the supplied bin's qualifying hours, MWh, dollars and excluded unknown hours. Reused the existing Three.js demand-driven renderer, shared rotation/zoom/reset controls, nearest-supplied-point picking and keyboard interaction. Monthly chart is default, lazy 3D is opt-in, and WebGL/lazy-load failure returns to the chart/table. Faces are display interpolation, not new observations.
- Real checks: LES (LES_LES): 764 qualifying hours, $7,940.537 per available MW; at 100 MW and 0.5 availability = 38,200 MWh and **$397,026.85**. OKGE (OKGE_OKGE): 305 hours, $2,061.7952/MW = 15,250 MWh and **$103,089.76**. Live CSWS also verified: 28 hours, 1,400 MWh, **$6,307.885** at the same controls. Live EDE remains unavailable. All six mapped zones (CSWS/LES/OKGE/OPPD/SPS/WFEC) have price evidence; the other fifteen catalog entries still have no defensible mapping and show no dollar figure. First/warm local endpoint timings were approximately 172/95 ms for available responses, 7 ms for EDE.
- Carbon/coverage limits: each supplied point has 8,687 evaluable and 73 unknown hours over the 2025 UTC observation period. No missing-hour fill or annual/contract extrapolation. Carbon shifted remains **null/unavailable** without supported shift schedules and intensities. The existing associated-wind operating-emissions value is zero, explicitly **not** carbon absorbed, removed, avoided, or an offset. The frontend does not turn that zero into a sustainability-benefit claim.
- Verification: BEFORE full Python **1,687 passed + 30 subtests**, frontend **287 passed**. AFTER full Python **1,717 passed + 30 subtests**, frontend **310 passed**, including 12 live HTTP checks; TypeScript and production build pass. Added real-artifact/HTTP tests for six mapped plus fifteen unavailable choices, exact offline reproduction, corrupt/stale evidence, source validation, dollar/MWh arithmetic, selection races, load/availability updates, camera/point inspection and idle-loop regression. Existing 17 Python deprecation warnings and Vite >500 kB warning remain; no tests deleted. An initial targeted pytest run lacked a writable basetemp and hit Windows temp-directory setup errors; all required runs used the normal explicit writable basetemp and passed. Browser automation reported no connected browsers, so screenshot/GPU visual verification remains for a human; jsdom controls/geometry/fallback were verified. An auto-review rejected an attempted whole-file HEAD-based encoding repair; a small preserving patch fixed only the affected glyphs instead, with no lost edits.
- Publication: current branch **Alex** only. Local API restarted at 127.0.0.1:8000 with the new attachment; existing Vite remains running at 127.0.0.1:5174/app. Out-of-/web changes are the explicitly requested additive API/price compilation, serving evidence, tests and this log. No main or teammate-branch push.

## Remove standalone source info icons — 2026-09-13
- Changed: removed all shared SourceInfo "i" buttons and their popup triggers. Their source objects remain as nonvisual companion metadata; displayed Sourced values and chart ticks still support click/keyboard inspection. Tariff provenance now opens from the existing clause text, with its wrapping/type preserved. Updated the footer hint. No data, math, API or honesty copy changed.
- New/changed interface: none. Updated existing icon-based tests to assert icon absence, retained input metadata and direct-value inspection; no tests deleted.
- Verification: full frontend run after assertion updates passed 308/310, with two existing App timing failures (async retry lookup / 5-second test budget). Isolated App rerun passed all 14 with a 15-second budget; all 310 cases passed across these runs. Production build checked; existing Vite chunk-size warning remains. Python was not rerun for this presentation-only change. Push scoped to Alex.
- Publication update: commit/push was rejected by automatic approval review, which considers the earlier no-commit/no-push instruction still active. No commit or push occurred for this cleanup; changes remain local pending explicit publication approval.

## Diagnose all zone estimates and correct misleading errors - 2026-09-13
- Directly posted all 21 parquet IDs to the live endpoint before and after. Exact per-ID status/error bodies are in docs/ZONE_ESTIMATE_DIAGNOSTIC.md. These are 17 load zones, SPP_SYSTEM and three demo aliases. All 21 underlying reader lookups work; no missing columns or IDs were found.
- Root cause: the existing annual-readiness guard rejects the current bundle's 1742 held-out clock hours / 1717 local scored hours against its 8760-hour requirement. The fallback has only four authored locations, so the other 17 incorrectly became 404. Those 17 now return explicit 503 coverage errors identifying the existing location and missing evidence. SPP_SYSTEM and three demos still return 200 with assumption/placeholder exposure. Zero current responses contain real modeled exposure; the requested all-real-200 acceptance remains blocked, not completed.
- Fetched and inspected upstream Kristian through 6c19dc5 and main through 9e8a6a0 without merging. Confidence regeneration 54c7571 changes confidence_score, not location IDs, schema or exposure values. Follow-up evidence validation retains annual readiness. The longer system-only high-demand-proxy experiment is a different target and is not a replacement 21-location artifact. No ready replacement bundle was found locally or on the inspected branches. Asked whether to retain the guard or explicitly serve experimental results with visible limitations; no answer received, so the guard remains intact. No model, training, confidence, economics or artifact files changed.
- Corrected the real-zone caption to 'SPP load zone · zone-specific model data; site exposure is your assumption'. System/demos retain their separate site-data caveat. No API response schema changes. API plumbing/tests and this diagnostic/outbox are the authorized changes outside /web.
- Validation: full Python baseline 1717 passed + 30 subtests; after 1752 passed + 30 subtests (17 existing warnings). Expanded authored annual-ready software fixtures to all 21 IDs and added all 17 real-artifact readiness regressions; fixture success is not evidence that production artifacts pass. Full frontend 310 passed including 12 live checks. Production build passed with the existing chunk-size warning; initial sandbox build failed on directory access, then the authorized rerun passed. No tests deleted.
- Publication scope: this zone diagnostic/error/caption change to Alex only under the latest explicit push instruction. Earlier source-icon cleanup remains local and is excluded from this commit; its existing outbox history is preserved. Main and teammate branches are unchanged.


## Publish Alex checkpoint to main - 2026-09-13
- Changed: merged committed Alex through 6d8fd44 with origin/main 9e8a6a0 in an isolated worktree. Preserved main's national-stack/leaderboard, VPP, comparison and telemetry work alongside Alex's location catalog, grid-impact serving/panel, wind/carbon evidence and zone diagnostics. Retained Ink and Signal typography, colors, brand, quiet source presentation and baseline provenance. The original Alex checkout and its seven uncommitted source-icon cleanup files were not changed or included.
- Merge integration: resolved four frontend conflicts, combined companion endpoint test routing, aligned saved-scenario result typing, and retained main's existing vpp_solar_homes request field in all-zone assertions. No new API contract beyond the interfaces already committed on the two branches. Leaderboard success tests now use explicit authored fixtures instead of requiring an ignored teammate artifact; added an actual missing-manifest 503 regression. No generated rankings or model evidence fabricated.
- Needs review / blocked on: annual-ready zone estimates remain unavailable as documented in ZONE_ESTIMATE_DIAGNOSTIC.md. The national_stack/zone_rankings.json serving artifact is absent in this checkout and the original workspace; its existing endpoint honestly returns 503. Publishing the combined code does not claim those data blockers are solved. Kristian's newly fetched 77015a7 was not merged as part of this Alex-to-main publication.
- Validation: full merged Python suite 1754 passed + 30 subtests, one skipped, 17 existing warnings. Production build passed with the existing large-chunk warning. Initial integration checks caught and resolved old request-field assertions and the ignored leaderboard test dependency. Final frontend result recorded below. Publishing to main is explicitly authorized by this task; no push to teammate branches.
- Final frontend validation: all 316 tests passed across 22 files, including 12 live HTTP checks against the isolated merged API on port 8011. Build passed. Original demo servers were left untouched; the temporary test API is stopped after verification.

## Pull main into the local Kristian checkout - 2026-09-13 20:43 UTC
- Changed: merged origin/main through 9e8a6a0 at the user's request, preserving Kristian's 5dfadfc location/weather changes and 2d22db1 checkpoint. Resolved the sole conflict in this append-only log by retaining both histories. Incoming application code merged automatically.
- Validation: all 80 Python pipeline/workspace tests and 10 estimator JavaScript checks pass; API/pipeline syntax compilation and merge-local whitespace/conflict checks pass. Upstream trailing whitespace and existing NumPy timedelta deprecation warnings are retained. Full API and React suites were not run: this checkout has no FastAPI/pytest/XGBoost installation or web node_modules.
- Scope: local synchronization only; no push, model retraining, artifact regeneration or server restart.

## Merge Alex into Kristian - 2026-09-13 UTC
- Changed: merged origin/Alex e24eee8 into Kristian after the main merge fe6c440, bringing in wind/carbon/grid-impact modules and tests. Resolved three frontend conflicts by retaining Alex's click-to-inspect source behavior alongside main's climate telemetry, VPP, scenario comparison and zone leaderboard. Kristian's location/weather changes remain intact.
- Interface: retained upstream API contracts and provenance metadata; updated merged UI copy and tests to match Alex's source presentation. Rankings tests now use explicit temporary contract fixtures, with missing/malformed-file coverage, instead of relying on an ignored local model output.
- Validation: full Python run passed 1,633 tests and 30 subtests; its two rankings failures exposed the missing ignored artifact. After the fixture correction, all five rankings tests passed. All 273 frontend tests passed, including eight live API checks, and the production build passed. Existing dependency/bundle warnings remain.
- Scope and publication: user explicitly requested pulling Alex and pushing Kristian. Publish the merged history to Kristian only. No model retraining or research-artifact generation; absent production rankings still return 503. Temporary verification API is stopped after checks.

## Open the merged app locally - 2026-09-13 UTC
- Started the merged Fluxline frontend at http://127.0.0.1:5174/app and API at http://127.0.0.1:8000 as hidden local processes; both HTTP checks returned 200. The existing location estimator at http://127.0.0.1:8765 also returned 200. No application code or API interface changes; startup logs are ignored under .tools/app-launch/.

## Location selection in Fluxline - 2026-09-13 UTC
- Changed: added city/state or coordinate selection to the existing Fluxline input bar. All current facility, site-exposure and economic inputs go to a hidden location adapter; results reuse Fluxline's typography, controls, exposure/economics panels, export and comparison ledger. No second estimator screen or duplicate facility controls. Ambiguous locations require a choice; changed inputs hide stale results and require Estimate location again.
- Interface: new local-only /api/location-estimator search, estimate, job-status and cached-suggestion routes. Completed regional reports support immediate arithmetic without rewriting artifacts. Explicit new searches/estimates use the existing workspace's token, registered inputs and single-job lock. Full inputs are echoed, report coordinates are checked, and Low-confidence expected hours remain distinct from unavailable P50/P90/P99 outcomes. The canonical /api/estimate contract is unchanged.
- Validation: all 251 API tests and 280 frontend tests pass, including eight live canonical API checks; production build passes. Full-page UI checks use two workers and a 15-second test budget; the retry DOM wait was made tolerant of dashboard rendering while transport deadlines retain separate tests. Fixed a telemetry-drawer regression found during integration. Existing dependency/bundle warnings remain. No connected browser was available for visual inspection.
- Live evidence: verified Wichita, Colorado Springs and Page against their original saved reports, including echoed inputs, single site scaling, annual energy, full-term hours and browser-origin headers. A new coordinate search for the existing Wichita point completed through the real hidden workspace job and returned its cached model result. No model training or weather/grid fetch was needed for these checks.
- Local launch: Open Fluxline.cmd starts/reuses hidden services and opens http://127.0.0.1:5174/app. API restarted with the adapter; the original estimator remains on 8765. Protected instructions, model labels and calibration are unchanged. Implementation saved locally on Kristian; this task does not publish another branch update.

## Confidence handoff and ranking validation - 2026-09-13
- Changed: API checks both sidecars against confidence policy v2 and recomputes saved scores/components/classifier levels from ensemble spread, same-location precedent, and limitations. Shared arithmetic moved to dependency-free pipeline/confidence_policy.py and remains re-exported by pipeline/confidence.py. Original scoring behavior and artifacts are unchanged.
- Interface: estimate JSON shape unchanged; model confidence.basis now reads ensemble_agreement_and_historical_support. Its source ref includes policy version, component scores, precedent count, and limitations. Stale/missing policies or inconsistent evidence return 503. Annual confidence stays Low and annual readiness is unchanged.
- Rankings: reject duplicate locations, empty/unordered lists, invalid ranks, out-of-range component/composite scores, and unordered/excessive risk hours. Artifact JSON rejects duplicate keys and non-finite/overflow numbers; unreadable rankings produce 503 without exposing filesystem details. No rankings generated or placeholder evidence promoted.
- Validation: full Python suite 1,729 passed plus 30 subtests; 108 frontend API/contract checks passed, including eight live cases against an isolated updated API. Both saved bundles pass confidence validation: 21 canonical zones still fail pre-existing annual readiness (1,717 local held-out hours); the separate six-year SPP_SYSTEM run passes. Fresh-interpreter regression confirms confidence validation does not import the ML training stack.
- Scope: no frontend or location-estimator edits; no new datasets/training. Sync main and publish to Kristian only under the user's explicit instruction.
- Local runtime: automatic approval review blocked restarting the existing API with the reason 'blocked by policy' and no additional detail. Isolated temporary validation server shut down normally; the user's existing API still needs a restart to load these changes.

## Additional 2025 SPP data and frozen-target model comparison - 2026-09-13
- Changed: history preparation now consumes all twelve reviewed 2025 monthly archives, checks complete Central operating-year timelines and prepared-cache raw hashes, and reports the six excluded post-2025 UTC boundary hours. Generation preparation supports the reviewed 2025 annual archive. Existing readers retain unknown/conflicting observations.
- Data: cached thirteen public SPP files with source hashes (twelve load months and generation mix). Existing ERA5 observations reused. Prepared rows grow from 52,608 to 61,362; usable features from 52,250 to 60,979. The original 21-zone published artifacts remain unchanged because their target differs from this explicit system-demand proxy.
- Model: trained fifteen 100-tree members and generated seven experimental annual rows in data/processed/workbench/runs/history_2025_20260913. Both models retain the exact 42,064.0228 MW demand threshold from the pre-2022 reference. On identical 8,729 new hours with 874 positives, Brier falls 0.0039015551 -> 0.0036610444 (6.16%); log loss falls 0.0141813806 -> 0.0131984266. Paired weekly bootstrap interval crosses zero, so the improvement is modest and not established as statistically clear. Confidence remains 0/Low with zero median neighbor support.
- New interface: offline python -m pipeline.backtest compares repeated --run-dir values against one --hourly input and an explicit UTC window. It rejects changed targets, fitting/calibration overlap, existing labels, model/card/schema mismatches, and invalid probabilities. It does not train, fetch, or promote models.
- Evidence: docs/MODEL_EVALUATION_2025.md contains exact reproduction commands; docs/model-evaluation-2025.json contains per-model metrics, coverage, the paired interval, source URLs/retrieval dates/hashes, and candidate artifact hashes. Large raw/model outputs remain in ignored local directories.
- Validation: full Python suite 1,753 passed plus 30 subtests; actual training/simulation, API artifact provenance/annual readiness, and network-disabled cache reuse passed. No frontend, hidden-estimator wiring, label.py, or calibration changes. Sync main and push Kristian only, as instructed.

## Confirmed emergency evidence and source validation ? 2026-09-13
- Changed: added 14 source-verified Uri/Elliott EEA intervals in docs/spp-event-evidence.json; joined the 2019?2025 system history offline. Confirmed evidence grew from 150 to 5,908 minutes, across 102 hourly buckets; 61,260 rows remain unknown, with no negative labels invented.
- Finding: the frozen 42,064.0228 MW demand proxy covers only 600/5,485 documented Uri minutes (10.94%), versus all 273 Elliott minutes and 150 August-2024 minutes. This is target-validity evidence, not held-out emergency accuracy. See docs/EMERGENCY_EVIDENCE_AUDIT.md and its JSON with exact rows/hashes.
- Backend: catalog source/ID validation; correct positional joins for repeated pandas index labels; hourly identity validation; cached public documents now reject altered bytes, missing provenance, and ambiguous multirow caches without refetching or overwriting them.
- Validation: 1,759 tests + 42 subtests pass; two existing dependency warnings. Actual seven-year join passed. No frontend, location-estimator wiring, label policy, calibration, or published model changes.
- Needs review: exact emergency transition transcription (2021 PDF page 26; 2022 PDF pages 9/40) before using it in a new training-label policy. Newly cached 2021 generation-outage archive has 365 daily outlooks; next work will verify publication timing and assess supply-side inputs.

## Outage outlook importer and model comparison ? 2026-09-13; paused at user request
- Changed: pipeline/outages.py imports 2019?2025 dated SPP outage outlooks; 2,557 daily reports / 429,555 overlapping forecast rows cached. 55,240 hourly predictors retained; 6,098 hours quarantined for fuel-total mismatch and 24 lack an eligible vintage. Source hashes, missing report hours, and conservative next-day eligibility are recorded.
- New interface: four outage outlook inputs add 20 lagged/rolling features through pipeline/common.py and the existing feature builder. workflow.py records the outlook timing assumption in model cards. backtest.py compares models with distinct recorded feature subsets on identical hours.
- Actual comparison: 8,729 common 2025 hours, Brier 0.0036610444 -> 0.0036697464 (0.238% worse); paired 95% interval includes zero. Candidate not promoted. Frozen target and split rules unchanged. See docs/OUTAGE_OUTLOOK_EVALUATION.md and docs/outage-outlook-evaluation.json.
- Validation: 1,786 tests + 42 subtests pass; actual preparation/training/seven-year simulation/API artifact checks passed. Confidence remains Low / 0.0 with zero median neighbor support. No frontend or location-estimator wiring edits.
- Needs review: historical report publication/revision times remain unverified; recurring fuel-total discrepancies need source clarification. Outage outlooks include planned outages and are not emergency labels. Work paused by user after this tested checkpoint; no further research or training until resumed.

## Resumed: confidence coverage gate and capacity source collection - 2026-09-13
- Changed: confidence now requires 8,760 distinct local scored hours for above-Low ratings and measures inclusive hourly span. A synthetic 30-hour / 406-day case changes High 0.909091 -> Low 0.69; exactly 8,760 consecutive hours changes Low 0.69 -> High 0.909091 when all other gates pass. Duplicate/unknown/non-hourly evidence is rejected; DST fold hours remain distinct.
- New interface: new training model cards include confidence_coverage and confidence_coverage_policy. Existing version-2 arithmetic and API contract are preserved. Three saved real runs were rescored without fitting; all scores stay Low/0 and API validation passes. Canonical 147-row artifacts remain unchanged.
- Validation: final full suite 1,798 tests + 42 subtests pass; two existing dependency warnings. Exact evidence in docs/CONFIDENCE_COVERAGE_AUDIT.md and docs/confidence-coverage-audit.json.
- Data: collected 2,557 SPP hourly-capacity daily reports across 2019-2025. 61,314 distinct hours, 54 missing clock hours, four identical duplicate timestamps, and 264 valid thousands-formatted numeric cells recovered during audit. Sources/hashes in docs/capacity-data-sources.json; details in docs/CAPACITY_DATA_COLLECTION.md.
- Next: implement and test the capacity importer, then assess availability before training. EcoMax capacity is not a guarantee of available/deliverable reserves. These new records have not been used as model inputs. Frontend and hidden location-estimator wiring remain untouched.

## Capacity importer and source-preserving research data - 2026-09-13
- Changed: pipeline/capacity.py normalizes all 2,557 cached SPP generation-capacity reports with strict schema, numeric, calendar/DST, and duplicate checks. Identical duplicate observations retain both report URLs/dates/hashes; conflicting values stop import. Source GMT timestamps are preserved without inventing interval or publication semantics.
- Data: actual network-disabled import yields 61,362 pre-2026 clock rows: 61,308 observed, 54 explicitly missing, four identical duplicate timestamps, six post-2025 UTC observations excluded. Exact counts, missing hours, output hash and reproduction command are in docs/capacity-normalization-audit.json.
- Publication: include the new cleaned research parquet and matching source manifest explicitly under data/processed/research/ at the user's request to push everything. Raw source byte caches and older generated experiments remain local under the existing ignore rules; source URLs and fingerprints are published for reproduction.
- Validation: complete Python test coverage in two invocations (tests/ and api/): 1,855 tests plus 42 subtests passed; two existing API dependency warnings. The actual output was reloaded with pandas and verified against its manifest. No frontend or hidden-estimator changes.
- Model scope: research observations only, not new predictors or labels. Historical publication/revision timing still needs review; EcoMax does not establish deliverable reserves. No trained model or confidence change in this checkpoint. Pull main before pushing Kristian only.

## Repository published and teammate branches created — 2026-09-12
- Changed: initialized git and published the scaffold at https://github.com/alphabyalex/East-v-west-hackathon-2026 (main: a47a618). Created and pushed Alex, Kristian, and Tharun once from main; working branch is Alex. Future Codex commits will go only to Alex and main.
- New/changed interface: origin is https://github.com/alphabyalex/East-v-west-hackathon-2026.git; all four branches have matching origin upstreams. Frontend work is still in progress and is excluded from the scaffold commit.
- Needs review: gh is unavailable on PATH and standard install locations, so collaborator invitations for Tharun.ekam@gmail.com and skbridge04@gmail.com were not attempted; invite them manually through GitHub settings or provide their GitHub usernames once gh is available. Existing README claims about learning actual emergency/curtailment records need Claude/human review against the documented proxy-label fallback; unchanged by Codex.

## Frontend shell and exact mock API contract — 2026-09-12 (4569cb2)
- Changed: added the Vite + React + TypeScript + Tailwind /web workspace, Recharts annual fan, sticky site-exposure assumption, editable economics, conditional decision, reusable Sourced/SourceInfo/SourcedTick provenance, reset and local JSON export. No API calls, training, sampling, remote assets, or backend edits. Run `cd web`, `npm ci`, `npm run dev`.
- New/changed interface: complete valid response is `web/src/model/mock-response.json`; exact TypeScript types are `web/src/model/types.ts`; units/formulas/defaults are in `docs/frontend-api-contract.md`. Every numeric field is SV = {value, source_type: "data"|"clause"|"assumption", ref}. Top-level response = {schema_version:"1.0.0", grid_operator:"SPP", mode:"illustrative", locations:[{id,label,annual_series:[{year:SV,p10:SV,p50:SV,p90:SV,p99:SV}]}], defaults:{location_id:SV<string>,load_mw:SV,contract_years:SV,flexibility_percent:SV,site_exposure:SV,firm_wait_years:SV,gpu_per_mw:SV,gpu_hour_value_usd:SV,early_margin_usd_per_mw_year:SV}, decision_policy:{close_call_fraction:SV}}. Annual rows are baseline hours/year at site_exposure=1; years are relative contract years. Browser scales fixed quantiles and computes economics with documented arithmetic. Break-even results are null when interruption cost is zero.
- Validation: production build and TypeScript check passed; all 13 model/DOM interaction tests passed, covering decision transitions, zero-cost cases, invalid input normalization, source JSON, and synchronized annual data. The automated browser tool exposes no browser connection, so rendered desktop/mobile visual QA remains unverified.
- Needs review: mock data is explicitly authorized for this shell. All fixture values use assumption/mock refs; SPP-region selectors are illustrative, not verified node IDs. Replace fixture mode, refs, and location identifiers together for real data. Annual summaries average marginal quantile paths; economics are a median-path proxy, not a quantile of total losses. Human/Claude should review the new model-boundary copy and economic assumptions before any real-data demo. Illustrative term range is not a tariff claim. Existing AGENTS.md and FROM_CLAUDE.md were not edited by Codex.

## Institutional visual pass — 2026-09-12
- Changed: replaced the olive palette with neutral charcoal surfaces and gray rules; reduced the header/title to a compact workspace treatment; joined chart, ledger, and economic assumptions into contiguous square-edged regions; flattened the decision readout. Removed the chart and slider gradients, decorative shadows/glows, and nonzero corner radii. Icons remain SVG, with a restrained geometric mark. Essential labels/notes are at least 10px; metric/input layouts wrap at narrow widths. The site-exposure assumption remains sticky and prominent, with animated numeric changes and complete provenance.
- Why: studied official Coinbase Prime and Palantir Workshop product screenshots for dense numeric workspaces, aligned controls, persistent context, and restrained semantic color; reviewed Jane Street, Optiver, and Millennium for concise hierarchy. Reference links are in `web/README.md`. The visible AGENTS.md frontend section still contained its earlier shorter wording when re-read, so the user's newer explicit ban list governed this pass.
- New/changed interface: no mock API schema or economics-formula changes. Scenario exports now preserve the same edited/default input provenance shown on screen.
- Validation: source audit found no gradients, shadows, glass effects, or rounded-card treatment. Production/interaction verification is recorded in the subsequent completion entry.
- Needs review: browser visual inspection remains unavailable because the computer-use inventory exposes no browsers. DOM interaction tests are not a desktop/mobile screenshot review. AGENTS.md and FROM_CLAUDE.md were not edited by Codex.

## Frontend validation complete — 2026-09-12
- Changed: completed shell and institutional visual pass, ready to share on Alex and main at https://github.com/alphabyalex/East-v-west-hackathon-2026. Exact mock response and integration notes remain linked above.
- Validation: `npm run build` passed (TypeScript + Vite); `npm test` passed all 13 tests; local dev server returns HTTP 200; `git diff --check` passed. Vite reports the expected large Recharts-containing bundle warning; build succeeds and all runtime assets are local. Source scan confirms no banned visual effects.
- Needs review: visual browser QA remains unverified. Collaborator invitations remain manual because gh is not installed. Kristian and Tharun stay at their initial branch commit a47a618; subsequent publication targets only Alex and main.

## Generated cache excluded — 2026-09-12
- Changed: added `web/.gitignore` for Vite's local `.vite/` cache after final branch verification. Runtime code and the verified build are unchanged.
- New/changed interface: none.

## Teammate integration workflow — 2026-09-12
- Changed: Alex authorized checking Kristian/Tharun after major checkpoints, reviewing and merging ready teammate work into Alex, validating the combined changes, and pushing Alex. Teammate branches remain owned by their authors and will not receive Codex pushes.
- New/changed interface: coordination workflow only; no API changes. Current remote check found both teammate branches still at a47a618, so there is no new work to merge.
- Needs review: the expanded Frontend direction is now present in the local AGENTS.md and has been read in full. That file's existing uncommitted update was left untouched and unstaged.

## Shared build contract published directly to main — 2026-09-12 20:52 UTC
- Changed: published the user-supplied docs/BUILD_PLAN.md unchanged to main at https://github.com/alphabyalex/East-v-west-hackathon-2026/blob/main/docs/BUILD_PLAN.md, using an isolated checkout. This documentation publication is outside /web and is explicitly requested by Alex; frontend work remains on Alex.
- New/changed interface: BUILD_PLAN.md is now the canonical shared contract for pipeline parquet/function output, POST /api/estimate, and frontend consumption. Its section 2 supersedes the earlier illustrative frontend API shape; the existing shell has not yet been migrated to it.
- Needs review: coordinate reconciliation of docs/frontend-api-contract.md and the frontend adapter with Alex/Kristian/Tharun. The canonical response adds confidence and model provenance, uses term_years and fractional flexibility_split, and provides p50/p90/p99 rather than p10. Publication does not claim this endpoint is implemented.

## Updated team roles and main integration — 2026-09-12
- Changed: re-read the full updated AGENTS.md and FROM_CLAUDE.md. Codex is building the frontend alongside Tharun; Alex owns backend/API/wiring and Kristian owns ML. Role bleed is normal and changes outside /web will be logged here. Integrated the published main contract into Alex, retaining both branches' handoff entries.
- New/changed interface: current coordination follows the updated main-first integration policy: inspect main for shared teammate merges and integrate those updates into Alex; never push Kristian or Tharun. The remote checkpoint found no new teammate commits beyond a47a618.
- Needs review: existing user/Claude changes to AGENTS.md and root README.md remain untouched and unstaged.

## Data-driven exposure surface and canonical-contract reconciliation — 2026-09-12
- Changed: added a demand-rendered Three.js surface inside the exposure panel: relative year x percentile x modeled exposure hours/year, proportional percentile spacing, selectable supplied vertices, sourced axis labels and inspector, drag/button/keyboard rotation and zoom, fixed-scale slider morphing, and reduced-motion support. A single year is a cross-section with no invented time width. Recharts fan and the sourced annual table remain available; WebGL failure/context loss falls back to the fan. No decorative animation, gradients, remote assets, live sampling, or backend calls.
- New/changed interface: the current shell's derived annual_series now retains {year:SV,p10:SV,p50:SV,p90:SV,p99:SV}, where SV is {value,source_type,ref}; p99 was already in the fixture, so its input schema did not change. Faces interpolate display geometry only; four quantiles cannot define probability density. Three.js is loaded as a separate local bundle.
- Outside /web: updated docs/frontend-api-contract.md to identify BUILD_PLAN.md as canonical and document the pending adapter migration (term_years, fractional flexibility_split, already-scaled by_year, block provenance/model sources, confidence, and absence of p10). The older fixture is explicitly documented as an internal shell format, not the target FastAPI response. BUILD_PLAN.md itself was published unchanged to main in b3cbb21 at the user's request.
- Validation: all 69 tests passed, including real Three geometry/camera/controls with only WebGLRenderer stubbed, surface inspector provenance, zero exposure, single-year data, transitions/cancellation/disposal, and app fallback behavior. TypeScript and production build passed. Vite reports large chart bundles (surface approximately 141 KB gzip; main approximately 191 KB gzip), with no build failure. Source audit found no banned visual effects.
- Needs review: GPU pixels and desktop/mobile visual layout remain unverified because the browser inventory is empty. Human/Claude should review the new quantile/interpolation copy. Canonical POST /api/estimate and confidence are not wired yet; do not invent confidence or p10 during that migration, and do not scale API exposure twice. Economics remain illustrative until sourced ASSUMPTIONS.md is shared. Existing AGENTS.md, FROM_CLAUDE.md, and root README.md changes were not edited by Codex.

## Explicit mock economics pending team sourcing — 2026-09-12
- Changed: re-read AGENTS.md and FROM_CLAUDE.md, then fetched main at task start and completion. docs/ASSUMPTIONS.md is absent on main (b3cbb21). Added persistent MOCK ECONOMICS and demonstration-only copy to the economics panel, MOCK DECISION beside the readout, explicit mock input labels, and a mock break-even caption beside the site slider.
- New/changed interface: JSON shape unchanged. Round dummy defaults are gpu_per_mw=1000, gpu_hour_value_usd=2, early_margin_usd_per_mw_year=500000, firm_wait_years=3. Each default uses source_type=assumption and mock://illustrative/economics-placeholder/inputs/<key>?pending=docs/ASSUMPTIONS.md. Current dummy break-even is 125 h/year (site_exposure approximately 0.5695); demonstration decision states occur at 0.4 / 0.55 / 0.9. These are mock arithmetic, not sourced estimates.
- Outside /web: updated docs/frontend-api-contract.md's dummy values and sourcing handoff. Economics sourcing belongs to Kristian/Tharun and will arrive via main; Codex has not independently sourced prices or substituted pipeline outputs for economic assumptions. Check main at each integration checkpoint, replace supplied values/units/refs together as soon as ASSUMPTIONS.md arrives, and recompute break-even; retain mock labels for any remaining placeholder dependencies. No runtime external fetches were added.
- Validation: all 69 tests and the TypeScript/production build pass; git diff --check passes. Existing Vite bundle-size warnings remain. AGENTS.md, FROM_CLAUDE.md, and the user's root README.md changes were left untouched.
- Needs review: sourced economics remain pending the team's ASSUMPTIONS.md on main. Do not remove mock labels just because values are editable or the UI looks finished. Canonical API migration remains separately documented.

## Canonical estimate response and confidence frontend checkpoint — 2026-09-12
- Changed: the shell now consumes the BUILD_PLAN.md POST /api/estimate response through createMockEstimate -> adaptEstimateResponse, with a seven-year supported/default horizon. Surface, fan, and table show only supplied p50/p90/p99 (no invented p10). Added sourced confidence beside exposure readouts/inspection, annual cost scenarios per quantile, mock tariff evidence with source controls, and canonical request/response JSON export. All default execution remains local/offline.
- New/changed interface: canonical types are web/src/model/contract.ts and the complete response example is web/src/model/mock-response.json. Request has exactly {location_id,load_mw,term_years,flexibility_split,site_exposure}; response has {inputs_echo,modeled_exposure,confidence,economics,tariff}. UI adapters inherit block source metadata and append field paths to each Sourced ref. source_type now supports model alongside data/clause/assumption. API-scaled exposure is not scaled twice. Local export is {mode,request,response,local_assumptions,result}.
- Contract refinements: breakeven_exposure_hours_per_year permits null at zero per-hour cost; by_year must cover the complete ordered selected horizon. BUILD_PLAN.md remains unchanged; refinements are documented in docs/frontend-api-contract.md for the API/pipeline owners. Confidence remains an explicit fixed MOCK Medium/0.5 fixture, not a model-derived result. Tariff records are explicitly unextracted placeholders without fabricated citations.
- Decision rule: compare term_cost[q]=annual_cost_usd[q]*term_years with the total early-connection benefit. not_worth_it if p50 term cost>benefit*1.05; worth_it if p90 term cost<benefit*0.95; close_call otherwise. These are marginal percentile-path comparison proxies, not quantiles of total loss. The slider marker is labeled median cost crossover, not a decision boundary. Mock defaults still produce worth/close/not at exposure 0.4/0.55/0.9.
- Outside /web: rewrote docs/frontend-api-contract.md around the now-shipped canonical shape and explicit integration semantics. No pipeline, extraction, backend, AGENTS.md, FROM_CLAUDE.md, or root README.md edits. Economic controls are local mock-only overrides, not extra request fields; agree a supported override interface before enabling HTTP so the server never appears to use unsent assumptions.
- Validation: all 69 frontend/model/geometry/provenance tests passed and TypeScript/production build passed; no banned visual effects added. Browser inventory is still empty, so GPU pixels and desktop/mobile layout remain unverified. Large local chart bundle warnings remain non-fatal.
- Needs review: API/pipeline owners should confirm the nullable threshold, full-year horizon, comparison policy, and worst_contiguous_outage_hours semantics before real-data use. That last field is a round authored mock retained in the contract, not displayed as site-outage evidence. Sourced economics still await Kristian/Tharun's ASSUMPTIONS.md on main.

## Validated HTTP boundary and full team foundation publication — 2026-09-12
- Changed: completed web/src/api/client.ts with opt-in postEstimate(request,{signal?,fetchImpl?}) and validateEstimateResponse(raw,request?). It validates the canonical five-field request, complete ordered horizon, quantiles, finite numbers, confidence, provenance, and matching inputs_echo. It preserves cancellation/network errors and never silently repairs responses, rescales exposure, or falls back to mocks. The demo still uses the local mock provider; no backend is enabled.
- New/changed interface: EstimateClientError exposes code invalid_request|invalid_response|http_error with optional path/status. HTTP uses same-origin POST /api/estimate. Endpoint shapes remain those in model/contract.ts and BUILD_PLAN.md, with previously logged nullable break-even/full-horizon conventions. README and frontend integration notes describe the prepared transport and unresolved local-economic-override handoff.
- Outside /web: updated docs/frontend-api-contract.md and this outbox. At Alex's explicit request, the foundation publication includes all repository source/contracts/docs, the user's existing AGENTS.md and README.md updates, requirements/gitignore, BUILD_PLAN/DATA_NEEDED/FROM_CLAUDE/HANDOFF, unchanged pipeline/ingest.py and __init__.py, and the complete web implementation. Publishing directly to main without a PR; user/Claude-authored files were staged as supplied, not edited by Codex. Ignored secrets, dependencies, build output, and cached data remain excluded.
- Validation: all 145 frontend/model/geometry/provenance/client tests and the TypeScript/production build pass. The HTTP client has 76 focused validation/transport cases; tests inject fetch and do not contact a backend. Main is an ancestor of this checkpoint, so publication is a fast-forward with no force push. Final tree/diff checks cover all requested foundation paths.
- Needs review: browser visual QA remains unavailable; existing chart bundle warnings are non-fatal. Pipeline ingest's live SPP verification remains as documented by Claude. This publication does not claim a trained model, real confidence/economics, tariff extraction, or a running FastAPI endpoint. Sourced ASSUMPTIONS.md has not appeared on main yet.

## Four checkpoints ending at the final submission deadline — 2026-09-12
- Changed: re-read AGENTS.md, FROM_CLAUDE.md, and BUILD_PLAN.md section 4. The on-disk table still had CP5, so applied Alex's explicit correction: CP1/CP2/CP3/CP4 at hours 12/24/36/48; CP4, Monday 12:00pm ET, is FINAL SUBMISSION. Removed the fifth checkpoint, moved rehearsal/integration completion before that deadline, and corrected BUILD_PLAN's time-budget description to 48 hours. Future execution planning uses this 48-hour deadline.
- New/changed interface: schedule only; no API or pipeline schema changes. Publishing all current repository changes directly to main with the exact commit message requested by Alex. Previously committed foundation files and the full web app remain included.
- Outside /web: edited docs/BUILD_PLAN.md per the user's correction and appended this outbox entry. AGENTS.md and FROM_CLAUDE.md were read, not edited. AGENTS.md still contains legacy 72-hour wording; the user's corrected four-checkpoint/final-deadline instruction takes precedence.
- Validation: verified exactly four scheduled checkpoint rows and the final Monday 12:00pm ET deadline, all requested foundation paths, and the staged diff. No runtime code changed; the prior foundation's 145 tests and production build passed.
- Needs review: Claude may update the protected AGENTS.md's legacy 72-hour wording to match the corrected plan. No additional post-hour-48 build stage is assumed.

## Local development server ready for human review — 2026-09-12
- Changed: started npm run dev in /web and left Vite running in a persistent terminal at http://127.0.0.1:5174/. Port 5173 was already occupied. The foundation/schedule commit b7687ac is now published on main at https://github.com/alphabyalex/East-v-west-hackathon-2026/commit/b7687ac.
- New/changed interface: none; the app still uses the canonical local mock response, with no running backend required.
- Outside /web: appended this coordination entry only; no application or protected-file edits.
- Validation: the local app URL returns HTTP 200. This verifies serving, not rendered layout or GPU pixels.
- Needs review: Alex will inspect the app in a browser, including the surface, slider recomputation, provenance controls, and explicit mock labels.

## Corrected tooling cutoff and final submission publication — 2026-09-12
- Changed: publishing the supplied docs/BUILD_PLAN.md revision directly to main. Four checkpoints remain at hours 12/24/36/48. CP4 on Monday at 12:00pm ET is the tech-stack cutoff; the actual final submission is Monday at 11:59pm ET. This supersedes my earlier outbox entry calling noon the final submission deadline. Tool-dependent coding must finish by noon; the remaining window is for video, rehearsal, and manual cleanup/submission.
- New/changed interface: none; pipeline and POST /api/estimate contracts are unchanged.
- Outside /web: publishing BUILD_PLAN.md as supplied and appending this outbox entry. The separate pending AGENTS.md change remains untouched and unstaged.
- Needs review: team members should use the two distinct deadlines in BUILD_PLAN.md section 4 when planning remaining work.

## Item 0 — shared deadline verified on main — 2026-09-12
- Changed: fetched main and verified docs/BUILD_PLAN.md already contains the correction published in 282b4eb: Monday noon ET is the tooling cutoff, Monday 11:59pm ET is final submission. No duplicate plan change needed.
- New/changed interface: none.
- Needs review: none for publication; proceeding through the requested frontend/backend checkpoints in order.

## Item 1 — typography and color replacement — 2026-09-12
- Changed: replaced system typography with locally bundled Google Fonts: Fraunces for brand/headings, IBM Plex Sans for UI copy, IBM Plex Mono for numeric data with tabular alignment. Replaced the old blue-gray palette across CSS, Recharts, and Three.js with near-black panels, warm off-white text, amber active states, steel-teal series, and muted decision colors. Font assets and OFL licenses are local so offline demos keep the intended type.
- New/changed interface: no data shape changes. Sourced now identifies numeric/string values with data-value-type for exact numeric font styling.
- Validation: font assets serve successfully; TypeScript/production build checked. Browser inventory is empty, so rendered desktop/mobile appearance remains unverified here.
- Outside /web: this outbox entry only. AGENTS.md and FROM_CLAUDE.md remain untouched.
- Needs review: human visual review of the new type/palette; proceeding next to the reported surface freeze.

## Item 2 — surface scheduling and safe default — 2026-09-12
- Changed: fan chart is now the default; Surface remains an explicit opt-in lazy import. Found synchronous draws on OrbitControls changes (including duplicate rotate/reset draws), unthrottled hover raycasts, redundant React label updates, and geometry/material recreation on every slider update. Replaced these with one coalesced demand frame, reused buffers, dirty-label updates, hidden-tab suspension, and complete initialization/error/context-loss/disposal cleanup. Reduced unnecessary multisampling/pixel load.
- New/changed interface: none; quantiles, provenance, and surface controls remain available.
- Validation: all 153 frontend tests and TypeScript/production build pass. New regressions cover event bursts, geometry reuse, idle frames, hidden tabs, initialization/draw failure, and numeric-animation cancellation. No unbounded mount-effect loop was found.
- Needs review: the reported real-Chrome capture hang cannot be reproduced here because no browser is connected. GPU/driver behavior is not claimed fixed; keeping WebGL off initial load is the requested demo safeguard. Existing chart bundle-size warnings remain non-fatal.
- Outside /web: this outbox entry only.

## Item 3 — running FastAPI and frontend HTTP integration — 2026-09-12
- Changed: added /api with POST /api/estimate, strict request/response validation, cached canonical mock JSON, and cheap scenario arithmetic behind a replaceable precomputed-location provider. Frontend now calls the endpoint through Vite's /api proxy to 127.0.0.1:8000 by default. Both providers remain explicitly illustrative.
- New/changed interface: request remains exactly {location_id,load_mw,term_years,flexibility_split,site_exposure}; response remains {inputs_echo,modeled_exposure,confidence,economics,tariff}, including full ordered by_year and nullable zero-cost break-even. Invalid requests/overflow return 422; unknown locations return 404. VITE_ESTIMATE_MODE=api|local controls startup. Export now includes {mode,status,response_origin,request,response,local_assumptions,result}.
- Behavior: 50ms debounce, 3-second timeout, cancellation/stale-response protection; pending/error output is a clearly labeled current-input local mock preview/fallback with Retry. Economic edits explicitly switch to local mode; Use API defaults resets them before HTTP, so no unsupported economic override fields are sent. API-returned exposure is not scaled twice.
- Outside /web: added api source/tests/minimal requirements/README, updated root README run instructions and docs/frontend-api-contract.md, and ignored .tools/.pytest_cache. Installed portable uv/Python and dependencies into ignored .tools/.venv because Python was absent; system PATH was not changed. No pipeline, AGENTS.md, or FROM_CLAUDE.md edits.
- Validation: 49 backend tests, 164 frontend tests, and 8 live differential HTTP cases through Vite/client/API/adapter pass; TypeScript/production build passes. API remains running at http://127.0.0.1:8000 and UI at http://127.0.0.1:5174/. Unit runs skip the 8 server-dependent cases; they were run separately with HEADROOM_API_URL. Non-fatal dependency deprecations, a OneDrive pytest-cache warning, and chart bundle warnings remain.
- Needs review: Kristian's real parquet/provider, sourced economics, and Claude's tariff extraction are still pending. api/README.md describes the adapter boundary. Root README's earlier product/model narrative still describes intended capabilities, now explicitly distinguished from this mock checkpoint; Claude should review its emergency-label claim against FROM_CLAUDE.md's fallback-label update.

## Item 4 — inline transparency and evidence panel — 2026-09-12
- Changed: added Model & evidence beside modeled exposure. The expandable panel explains system stress versus site applicability, shows the current sourced site assumption/confidence, clearly labeled mock reliability diagnostics, and tariff records with provenance. Moved the existing tariff evidence into this panel. Close/Escape restores trigger focus; Escape first dismisses an open provenance popover.
- New/changed interface: canonical estimate JSON unchanged. Local-only diagnostics in web/src/model/transparency.ts are {status:'mock',target:'system_stress',brier_score:SV,naive_brier_score:SV,reliability_curve:[{mean_predicted:SV,observed_fraction:SV}]}, where SV={value,source_type:'assumption',ref:'mock://...'}. Scores/curve are authored placeholders, not a held-out evaluation; they do not move with site exposure. Exact handoff is in docs/frontend-api-contract.md.
- Validation: 178 frontend tests and TypeScript/production build pass. Tests cover sourced scores/bins/axes, mock and missing citations, safe supplied citation links, panel expansion, slider synchronization, and keyboard behavior. The 8 live HTTP cases passed in item 3; no API implementation changed here.
- Outside /web: updated docs/frontend-api-contract.md and this outbox only.
- Needs review: Claude/human should review the new honesty and reliability explanations. Kristian must provide evaluation target/split, Brier/baseline, bin counts, and source/model version before replacing mock diagnostics. Tariff extraction remains pending; no fabricated citations were added. Browser visual/GPU review remains unavailable here.

## Item 5 — team integration and collaborator hygiene — 2026-09-12
- Changed: fetched origin. No new teammate merges exist on main; Kristian and Tharun remain at a47a618 and were not pushed. Main's corrected deadline remains intact. docs/ASSUMPTIONS.md is still absent, so economics remain clearly mocked. Publishing the completed frontend/API checkpoints to main as well as Alex.
- GitHub tooling: gh was absent; installed checksum-verified official portable gh 2.100.0 at ignored .tools/gh/bin/gh.exe without changing PATH. Existing Git credentials authenticated the authorized API calls as alphabyalex in memory only; no credentials were logged or persisted. Both requested collaborator PUT calls using Tharun.ekam@gmail.com and skbridge04@gmail.com returned HTTP 404 Not Found. No invitation was created.
- New/changed interface: no application contract changes. API and Vite remain running locally; runtime/bootstrap folders remain gitignored. Protected AGENTS.md's existing user edit remains untouched and unstaged.
- Blocked on: collaborator invites require confirmed GitHub usernames or manual invitations. Automatic approval review rejected the proposed GitHub public email-to-user search because sending those addresses for that separate lookup purpose was not specifically authorized; the search was not executed or bypassed. User approval would be needed for that search. The historical manual-invite note and HANDOFF.md redirect are left as-is; this outbox contains the current attempt results.
- Needs review: complete invitations by username or in GitHub's repository access settings. No other requested item is blocked.

## Final source audit after items 1–5 — 2026-09-12
- Changed: no further UI edits needed. Audited current source against AGENTS.md's ban list: no gradients, shadows/glows, glass effects, emoji icons, rounded card grids, marketing heroes, banned fonts, or decorative 3D. Theme variables resolve; all font assets are local; numbers and chart ticks use Plex Mono/provenance. Mock diagnostics and tariff placeholders remain explicit. Fan chart stays default; surface work stops while idle/hidden.
- Validation: 178 frontend tests, 49 backend tests, and 8 separate live HTTP contract checks passed, as did TypeScript/production build. Verified both local server URLs still respond. No additional runtime changes since those checks.
- New/changed interface: none. Exact estimate and local diagnostic shapes are documented in docs/frontend-api-contract.md and the source types.
- Needs review: actual rendered desktop/mobile appearance and Chrome GPU capture still need human review; no browser is connected to this environment. Use http://127.0.0.1:5174/ for the UI and http://127.0.0.1:8000/openapi.json for the API schema.

## Numeric motion checkpoint — 2026-09-12 17:25 PDT
- Changed: repaired fast-slider stalls in useAnimatedNumber: elapsed time now starts when each target arrives, retargeting from the displayed value with bounded cubic easing. Numbers settle exactly; hidden tabs, reduced motion, superseded targets and unmount cancel pending frames.
- New/changed interface: no API changes; presentation duration defaults to 320ms. Provenance still exposes the exact supplied target, never an animated intermediate.
- Validation: 12 focused motion tests pass, including rapid drag, reversal, exact settlement, stale callbacks, reduced motion, visibility and cleanup.
- Needs review: browser motion review remains unavailable because no browser is connected. Full visual polish follows in the next checkpoint.

## Visual polish and quiet honesty labels — 2026-09-12 17:26 PDT
- Changed: stronger median/net-value hierarchy, visible upper-tail quantiles, refined section spacing, tactile range handle and eased segment selection. Fraunces/Plex Sans/Plex Mono and the charcoal/amber/steel-teal palette remain. Replaced the global illustrative pill and economics banners with small source-dependent labels beside affected results, inputs, confidence and tariff records. Permanent USER ASSUMPTION, location coverage and system/site caveats retain their prominence and substance.
- Motion: numeric changes use the repaired easing; chart recomputation, chart-view selection, confidence-level changes, surface inspection and decision changes have finite eased feedback. Native inputs and ordinal axes remain exact. Reduced motion and hidden-tab cancellation remain supported; fan chart stays default and Three.js stays opt-in with no idle render loop.
- New/changed interface: POST /api/estimate JSON is unchanged. Mock labels inspect each supplied block's mock:// reference independently; API-connected does not imply real data. The frontend-derived crossover now preserves both economics and exposure dependency refs, and stays mock if either dependency is mock. Diagnostics remain the documented local authored fixture, not an API field.
- Main/data check: fetched origin twice during this task. No new teammate merges; main still contains only pipeline/__init__.py and pipeline/ingest.py in pipeline. No exposure_by_location.parquet locally/on main and no docs/ASSUMPTIONS.md. Backend therefore still serves its internal mock; no data-source switch or real-data claim was made. When real economics arrives, replace the Context's local defaults/control provenance and mode note alongside the backend assumptions; result-label readiness alone is not economics integration.
- Validation: 194 frontend tests pass, plus 8 separate live client/Vite/FastAPI contract checks; TypeScript and production build pass. Mixed-source tests verify exposure/confidence can lose mock labels while economics/tariff/diagnostics retain theirs, and the inverse crossover dependency remains marked. Source audit found no gradients, shadows/glows, glass effects, emoji icons or banned fonts; CSS tokens resolve. Existing large chart bundle warnings remain.
- Outside /web: appended this outbox only; no edits to AGENTS.md, FROM_CLAUDE.md, pipeline or backend. Publishing to Alex and main; teammate branches untouched.
- Needs review: inspect desktop/mobile hierarchy and actual eased motion at http://127.0.0.1:5174/. Browser inventory is empty, so rendered layout and GPU behavior are not visually verified. Claude/human should review the quieter labeling; provenance and permanent honesty claims remain intact. API remains running at http://127.0.0.1:8000.

## Shared placeholder assumptions scaffold and branch availability audit — 2026-09-12 17:49 PDT
- Changed: created docs/ASSUMPTIONS.md with a clearly labeled placeholder JSON block and round fake values: GPU rental 2 USD/GPU-hour, industrial electricity 50 USD/MWh, density 1000 GPUs/MW, early access 3 years, net margin 500000 USD/MW-year, close-call fraction 0.05. None is researched; ranges are arbitrary development ranges. File presence on main MUST NOT trigger removal of mock labels. Electricity is informational until sourced gross/net margin policy is agreed.
- New/changed interface: marked headroom:economics-assumptions:v1 JSON block, schema_version=1 and status=placeholder|mixed|sourced; each named input carries {value,source_type,ref,unit,source_url,retrieved_on,low,high}. All scaffold sources are assumption/mock://placeholder refs, with null retrieval metadata. API economics reader follows in the backend checkpoint; POST body/response shape unchanged.
- Branch audit after fetch: no pipeline/simulate.py, implemented get_location_estimate(), or tracked data/processed/**/*.parquet on any of eight local/remote tips. Alex/main/origin Alex/origin main=6bc29b3; local Kristian/local Tharun/origin Kristian=a47a618; origin Tharun=e433d4c (evidence-panel copy only). No local data/processed directory, including ignored files. No ASSUMPTIONS.md existed on any tip before this scaffold. Cannot see teammates' unpushed or gitignored local artifacts; real pipeline data is not available to wire here yet. Did not merge or push teammate branches.
- Outside /web: this task is backend-only with Alex; visual/landing work stays with Tharun. Publishing this shared assumptions scaffold to main and Alex; backend wiring follows on Alex. Protected AGENTS.md and FROM_CLAUDE.md untouched.
- Needs review: team must replace fake assumptions with sourced values/units/ranges and update status plus references together. Pipeline reader/parquet and tariff extraction remain pending.
## Local repository synchronized - 2026-09-12 21:34:05 UTC
- Changed: fetched origin and fast-forwarded local main from b3cbb21 to 93207c5, bringing in seven commits. The working tree was clean before synchronization; no merge conflicts occurred.
- New/changed interface: inherited the latest committed GitHub contracts and frontend; no additional implementation changes or pushes.
- Validation: local HEAD matches origin/main. This local handoff entry is intentionally uncommitted.

## Kristian offline ML prototype and walkthrough - 2026-09-12 23:19:20 UTC
- Changed: added common/prepare/label/features/train/confidence/simulate/workflow modules, requirements-ml.txt, 21 pipeline tests, docs/ML_WALKTHROUGH.md and an explicit label-policy template. Added a cached SPP load access check with annual-archive fallback to ingest.py; original dataset download behavior is unchanged. Installed a local Python 3.12 venv.
- Real data: cached SPP 2024 hourly-load archive and normalized it to data/processed/ml_inputs/spp_2024_load_only.parquet. 17,562 raw rows; 8,779 exact duplicates removed; one conflicting hour and two absent hours remain unknown; 8,784 hourly slots, 8,781 observed loads. The aggregate ID is SPP_SYSTEM, not a validated hub or site.
- New/changed interface: python -m pipeline.workflow supports fetch-sample, prepare-load, join-evidence, inspect, train, simulate, estimate. Input requires interval-start timestamp_utc, location_id, load_mw, and explicit observed/proxy evidence. No default scarcity threshold or silent label fallback. get_location_estimate(location_id, path=optional) implements BUILD_PLAN.md's read-only shape and LocationNotFoundError; research outputs stay inside each run directory until reviewed promotion to the canonical processed path.
- Validation: all 21 tests passed, including temporary synthetic-fixture training, test-label isolation, deterministic probabilities, missing-data rules, seasonal simulation and API schema. Preparation tests passed again after removing a dependency deprecation warning. pip check passed. Actual SPP download and 2024 data-quality inspection succeeded. No real event-labeled model has been trained; no test-fixture predictions populate the UI.
- Needs review: per AGENTS.md, human review of new label.py, chronological sigmoid calibration in train.py and model-boundary copy. Also review the experimental seasonal joint probability/residual bootstrap: stationary conditions, 365-day years, annual tails unvalidated, annual confidence capped Low, numeric score measures classifier agreement only. worst_contiguous_hours is defined as p99 of annual longest modeled episodes, requiring agreement with Alex. At least a full held-out year/all months is required for simulation. Do not copy SPP_SYSTEM estimates under node IDs.
- Blocked on: real matching hourly event/proxy evidence and the team's label definition. Load data alone cannot train the stress classifier. No GitHub pushes were made; changes are local for review.

## GitHub updates pulled with local ML work preserved - 2026-09-12 23:46:49 UTC
- Changed: fetched origin and fast-forwarded main from 93207c5 to 8361bcd (nine commits). Restored all local tracked/untracked ML work; resolved README and FROM_CODEX overlaps by retaining both workflows and both handoff histories.
- Validation: all 13 local ML/guide files match their pre-pull content; local HEAD matches origin/main. The Python environment and real-data cache remain present. No ML implementation edits or GitHub pushes in this sync.
- Recovery: retained stash b5255e5461852323d90975f85d2f70f3f614301d (codex-pre-pull-ML-work-2026-09-12) as a backup. Local work remains uncommitted.

## Area input and historical temperature features - 2026-09-13 00:22 UTC
- Changed: added `pipeline.weather`, cached Open-Meteo/GeoNames area lookup and ERA5 temperature ingestion, prior-hour temperature features, model-card temperature provenance, and `docs/TEMPERATURE_DATA.md`. Updated the ML walkthrough and README. Labels and calibration logic were not changed in this task.
- New/changed interface: `python -m pipeline.workflow add-temperature --hourly INPUT --area "City, State" --out OUTPUT` resolves an unambiguous US city, records a sibling `.area.json`, and joins temperatures by UTC hour. Optional `--location-id` selects an existing grid ID. Advanced `--locations JSON` accepts explicit points/weights. Outputs add temperature_c, temperature_area_min_c, temperature_area_max_c, and temperature_source_ref; a sibling `.weather.json` records sources, hashes and the area association. API/frontend contract unchanged.
- Real data: verified Amarillo, Texas as an example, not an inferred user location. Coordinates 35.222/-101.8313; joined temperatures to all 8,784 hours in `data/processed/ml_inputs/spp_2024_amarillo_temperature.parquet`. Original input columns match exactly, including three unknown load hours. Completed 2024 and 2025 calendar years are cached because the grid input crosses the UTC year boundary. Verified those actual caches work with network access forbidden.
- Validation: all 33 pipeline tests pass. Added checks for ambiguous cities, invalid mappings, Celsius/UTC/calendar completeness, leap day, cache reuse, exact joins, missing values, provenance, feature timing, location isolation, and temporary synthetic heat/cold model training. Synthetic thresholds are test fixtures only; no real-data predictive skill is claimed. `git diff --check` passes.
- Needs review: the user's weather-area/grid association, optional regional weights, and retrospective evaluation. Reanalysis does not establish historical real-time availability; only completed past calendar years are currently supported. City weather does not create city/site grid-risk labels. All work remains local and uncommitted.
- Blocked on: matching reviewed hourly event/proxy evidence for real-data training. No production model was retrained, no site cutoff probabilities were invented, and the website still uses its existing mock results.

## Accessible local ML workspace - 2026-09-13 00:47 UTC
- Changed: added `pipeline/workbench.py`, its local HTML/CSS/JS assets, `Open ML Workspace.cmd`, `scripts/start-ml-workspace.ps1`, seven workspace tests and `docs/ML_WORKSPACE.md`. Updated README and the existing ML/temperature guides. No new Python/Node dependency was needed.
- New/changed interface: localhost-only research workspace at http://127.0.0.1:8765. Area entry invokes the existing offline weather CLI as an explicit background job. Reviewed-event CSV upload plus a source/coverage reference invokes the existing join and training commands. Read-only views show real cached temperature/load data, provenance, saved model reports, confidence and artifact downloads. One job runs at a time; immutable inputs/results live under ignored data/processed/workbench. Requests cannot select arbitrary files or launch shell commands.
- Architecture: this is a separate research authoring workspace, not the /api estimate endpoint or /web demo. Page loads only read cached files. Explicit preparation/training jobs run via subprocesses outside HTTP requests. The demo API remains read-only with its current mock contract. Label rules, calibration and simulation code were not changed.
- Validation: all 40 pipeline/workspace tests pass, including disposable synthetic training with saved-model downloads and provenance. A live HTTP weather job successfully reused real Amarillo caches and joined all 8,784 hourly rows to `data/processed/workbench/inputs/weather_20260913T004530_5518e405.parquet`. Verified service health, static assets, data endpoints, and idempotent launcher behavior. `git diff --check` passes. Browser loaded the app/fonts/data successfully; visual inspection was unavailable because CUA reported no connected browsers/apps.
- Runtime: launched the service in the background and opened its URL using the launcher. It remains running. Reopen with Open ML Workspace.cmd; no terminal commands are needed for normal use. Source changes remain local and uncommitted.
- Needs review: the new research UI wording preserves the existing system-exposure/site distinction and explicitly shows the missing real-label state. Human label/calibration and rendered visual review remain appropriate before demo promotion.
- Blocked on: real matching reviewed hourly event labels for actual training. No real-data model or predictions were fabricated. The workspace is usable now for area/weather preparation and data inspection.

## Requested exposure-hours prediction: current-data audit - 2026-09-13 00:54 UTC
- Checked: all prepared hourly inputs and processed model artifacts. The three datasets represent the same 8,784-hour period, with 8,781 known loads and up to 8,784 Amarillo temperatures; none contains event labels, reserve-shortfall evidence, constraint counts or prices. No model.joblib, model_card.json, test_predictions.parquet or exposure_by_location.parquet exists.
- Output: saved `data/processed/prediction_readiness.json` with dataset hashes, coverage, artifact inventory and null exposure/site-cutoff estimates. Null means unavailable, not zero event hours. No model logic or UI changed.
- Blocked on: independently sourced event/proxy evidence and a fitted/evaluated model. Current load and weather alone do not identify a cutoff frequency. No unsupported numerical prediction was generated.

## SPP historical evidence and publication to Kristian - 2026-09-13 UTC
- Changed: cached official SPP reports and CHILLS filings; added docs/SPP_EVENT_AND_CHILLS_ANALYSIS.md, source-hashed docs/spp-event-evidence.json, pipeline/events.py and seven evidence/cache tests. Workspace includes evidence provenance. Prepared the local 2024 Amarillo/event join: 150 confirmed EEA1 minutes across three hourly buckets; preserves all input hours and missing loads.
- Interface: python -m pipeline.events --hourly INPUT --out NEW_OUTPUT adds observational EEA minutes/IDs and evidence JSON/CSV companions. It creates no training labels, preserves unknown coverage, unions duplicate overlaps and excludes local/other-region/date-only events. Label and calibration logic were not changed by this research task.
- Findings: exact August 26, 2024 EEA1 joins existing load/weather; five 2024 Conservative Operations dates have no verified intraday intervals; April 26, 2025 Shreveport local load shed stays geographically separate. July 1, 2026 accepted CHILLS tariff and September 2 proposal targeting Q2 2027 are distinguished. Source pages, hashes, observations and limitations are in the report/catalog.
- Validation: all 47 pipeline/workspace/evidence tests pass; seven evidence tests repeated after timestamp cleanup. A non-fatal pandas/NumPy timedelta deprecation warning remains. Actual real-data join succeeded; downloaded summer-report page visually checked. Git whitespace and scoped credential/large-file audit passed.
- Needs review: historical evidence catalog, region mapping, CHILLS interpretation and original prototype label/calibration code before production use. Complete event/negative coverage and site operating terms remain absent; no real-data trained model or site cutoff prediction is claimed. OATI archive TLS validation failed; certificate checks remain enabled.
- Git: user explicitly requested all changes on Kristian and no push to main, overriding AGENTS.md default branch ownership for this publication. Created local Kristian from current work; origin/Kristian is an ancestor. Publishing all source/docs/tests/launcher changes together; ignored raw/processed caches, environments and runtime files remain local. No force push or main update is authorized.

## Backend reader wiring, file-backed economics, and direct CORS — 2026-09-12 17:53 PDT
- Changed: POST /api/estimate now defaults to api/pipeline_provider.py, importing pipeline.simulate and calling get_location_estimate(location_id) when the callable and expected parquet exist. It validates section 1's location/year/quantile/confidence/model-version shape, retains model provenance, and applies site_exposure once in api/estimate.py. Missing module/function/precomputed file yields the same response shape with assumption sources and obvious mock:// refs including 'placeholder, pipeline not wired yet'; no silent real-looking fallback. Broken imports/dependencies/readers, malformed output or insufficient horizons return 503; unknown locations 404; invalid requests/arithmetic overflow 422. Tariffs remain explicitly unextracted placeholders.
- New/changed interface: section 2 JSON unchanged: request {location_id,load_mw,term_years,flexibility_split,site_exposure}, response {inputs_echo,modeled_exposure,confidence,economics,tariff}. All numeric descendants inherit their block's source; echoed inputs remain user assumptions. Added response header X-Headroom-Exposure-Source=pipeline|placeholder (exposure only). CORS allows exactly http://127.0.0.1:5174, POST and Content-Type, without credentials; direct calls to port 8000 need no proxy. Existing frontend proxy is unchanged.
- Economics: api/economics.py reads/validates the shared marked JSON on every request; no hardcoded numeric fallback. It records all input/dependency refs in the response. Industrial electricity is read but informational; current annual costs are gross lost rental value and early benefit uses independent net margin, avoiding a second power-cost subtraction. The tolerance remains 5% around early benefit compared to full-term p50/p90 paths; zero interruptible cost has null break-even. Missing/invalid assumptions are explicit 503 errors.
- Final branch/data recheck: fetched again after scaffold publication. No simulate.py or any tracked data/processed files on all eight tips: Alex/main/origin Alex/origin main=6cfdde3, local Kristian/local Tharun/origin Kristian=a47a618, origin Tharun=e433d4c. Local simulate absent and processed parquet list empty, including ignored files. Therefore the running endpoint still returns placeholders; real reader behavior was verified with injected test records only. This cannot inspect teammates' unpushed/ignored files on their machines.
- Validation: full repository pytest discovery passes 152 tests; complete frontend suite passes 194 tests with --maxWorkers=2, plus 8 separate direct HTTP/client contract checks. Real OPTIONS preflight and POST to port 8000 both returned 200 with the exact allowed origin and explicit placeholder metadata. TypeScript/production build passes. Initial Python temp setup failed on Windows permissions/missing parent, resolved with a new verified .tools/test-runs path and cache disabled; initial concurrently built frontend run had one lazy-load timeout, full bounded-worker rerun passed without changing frontend code. Existing dependency deprecations and chart bundle-size warnings remain.
- Outside /web: api source/tests/README, root README, docs/frontend-api-contract.md and this outbox; shared assumptions scaffold already pushed to main and Alex as 6cfdde3. Backend implementation is pushed to Alex. No /web, pipeline, AGENTS.md, FROM_CLAUDE.md, or teammate branch edits.
- Needs review: Kristian must supply the reader/parquet and export LocationNotFoundError; restart API after changing imported pipeline code. Tharun must keep labels until actual per-source status is sourced, and align local economic controls if reviewed file defaults change. Electricity/gross-net economics policy and tariff extraction remain unfinished sourcing work. API is running at http://127.0.0.1:8000; UI remains at http://127.0.0.1:5174/.

## Teammate push status check — 2026-09-12 18:06 PDT
- Changed: fetched origin; Tharun advanced from e433d4c to 7669f42. Five Tharun commits remain outside main: confidence overlap fix (71f9884), favicon (05527b9), local reliability trigger copy (e433d4c), landing page (b59c2a6), and economics sourcing document (7669f42). Kristian remains at initial a47a618. Main remains 6cfdde3 with nothing new to merge into Alex.
- New/changed interface: no local implementation change or merge. Tharun's ASSUMPTIONS.md is prose/tables without the backend's marked JSON block; it also explicitly flags gross revenue versus net margin and inconsistent grid/IT power bases. Reconcile these before adopting it; do not overwrite the loader-compatible scaffold or put gross revenue into the net-margin input unchanged. The proposed real/derived and real/cited source types in that doc are not canonical API enums.
- Needs review: Tharun's work is available on his branch but not yet main; branch policy still routes integration through main. Neither teammate branch contains simulate.py or tracked processed parquet. No teammate branches or UI files were modified. Citation accuracy was not audited during this status check.

## Kristian push confirmed — 2026-09-12 18:29 PDT
- Changed: fetched origin/Kristian at 2e4d9d2, adding ML preparation/features/labels/training/confidence/simulation, weather/event evidence, a local workbench, and tests/docs. pipeline/simulate.py now exports get_location_estimate(location_id, *, path=None) and LocationNotFoundError; missing precomputed output raises FileNotFoundError. No merge performed.
- New/changed interface: the reader now exists remotely, but no data/processed parquet is tracked on that branch. The reader expects data/processed/exposure_by_location.parquet. Main has no new commits to merge into Alex yet; backend still uses placeholders locally.
- Needs review: inspect the new reader's full return shape, ML tests, human-sensitive labeling/calibration, and offline output availability before integration through main. This status check did not run the new pipeline or establish that a trained model/parquet exists on Kristian's machine.

## Integration merges and real pipeline attempt — 2026-09-12 18:48 PDT
- Changed: merged Kristian 2e4d9d2 into main (c3d3e30), then current Tharun 59cbdda (2375f7b), then Alex's existing backend (2c11c96). Resolved outbox collisions by retaining both complete histories. Kept Tharun's landing page, navigation, favicon, confidence layout, and evidence copy; preserved source-dependent quiet honesty labels during dashboard conflicts. No pipeline label or calibration code was changed.
- Real pipeline attempt: installed requirements-ml.txt in the ignored local venv. `python -m pipeline.workflow fetch-sample` succeeded against https://portal.spp.org/file-browser-api/download/hourly-load?path=/2024/2024.zip (retrieved 2026-09-13T01:42:28Z). Cached 17,562 real rows. `prepare-load --raw data/raw/spp/access_check/2024_hourly_load.parquet --out data/processed/ml_inputs/spp_2024_load_only.parquet` succeeded: 8,779 exact duplicates removed; one conflicting hour and two absent hours remain unknown; 8,784 hourly slots, 8,781 known loads. Raw parquet SHA256: 9142040c00166449449683682778a87bcbb1e2f450355d23e6b4aac57180ecd4. Location is SPP_SYSTEM, a system sum, not a pricing node or site.
- Exact blockers: calling `label_hours` on that input with docs/ml-policy.example.json raised `ValueError: observed_event needs ['event_active']; do not substitute a different label silently.` The workflow `train --hourly data/processed/ml_inputs/spp_2024_load_only.parquet --policy docs/ml-policy.example.json --run-dir data/processed/integration_attempt` exited with `Cannot continue: Policy requires a nonempty label_ref.` The downstream `simulate --run-dir data/processed/integration_attempt` found no model_card.json. No model was trained and no exposure_by_location.parquet was created. An initial PowerShell quoting failure was corrected before these actual stage checks.
- Availability: pipeline/simulate.py and get_location_estimate now exist on main and origin/Kristian. No branch has a tracked data/processed parquet; our newly downloaded/normalized load caches are local and gitignored. Teammates' ignored caches cannot be pulled through Git. Selected official event episodes do not establish full negative-hour coverage. The simulator also requires at least a full year of held-out reference data; the current one-year load archive cannot meet that gate under the chronological training split.
- New/changed interface: canonical POST request/response unchanged. Integrating a read-only GET /api/economics-assumptions for the frontend to consume the exact file-backed defaults/provenance. The API will accept SPP_SYSTEM as an explicitly marked placeholder until matching precomputed output exists, without aliasing it to a site node.
- Needs review: real matching hourly event/proxy evidence, an explicit reviewed label definition, sufficient historical coverage, label/calibration logic, and experimental annual-tail methodology remain Kristian/team work. Exposure/confidence and unextracted tariff data remain visibly assumptions; a successful real load download does not make exposure real. Economics reconciliation and integration tests follow.

## Backend artifact wiring and economics reconciliation — 2026-09-13 01:52 UTC
- Changed: FastAPI calls Kristian's actual get_location_estimate(location_id, path=canonical_path). Matching model_card.json and simulation_metadata.json must accompany the parquet; the adapter checks source policy/hash/version/settings and confidence consistency. Model refs retain experimental annual-tail, Low-cap, classifier-agreement-only, and p99-longest-episode caveats. Missing pieces return explicit assumption placeholders; broken/mismatched data returns 503. Added parquet reader dependencies to api/requirements.txt. HTTP never fetches grid data or trains/simulates.
- New/changed interface: canonical POST /api/estimate unchanged. New GET /api/economics-assumptions returns the exact marked JSON block: schema_version=1, status=placeholder|mixed|sourced, and gpu_rental_price_usd_per_hour, industrial_electricity_price_usd_per_mwh, gpus_per_mw, early_connection_years, early_margin_usd_per_mw_year, close_call_fraction. Each is {value,source_type,ref,unit,source_url,retrieved_on,low,high}. GET/POST allow direct CORS from http://127.0.0.1:5174. BUILD_PLAN sections 1/3 and consumption documentation updated; section 2 body unchanged.
- Economics: kept Tharun59cbdda defaults of 575 GPUs/grid-MW, $3/GPU-hour, four-year early-access gap, and $317,000/MW-year. $10,577,700/MW-year is scenario GROSS REVENUE. $317,000 is his UNVERIFIED 3% operating-margin assumption applied to that revenue, rounded; not sourced net margin/net profit. No new haircut invented. Removed claims that this was a genuine margin or validated by a decision flip. Margin and 5% tolerance remain mock:// placeholders; status mixed. Other selected planning inputs remain assumptions attributed to Tharun's immutable research commit, without claiming independent external verification. Kansas $82.1/MWh is informational, not a site tariff. Cost uses gross rental value while benefit uses assumed operating margin; docs and result refs state the different bases.
- Validation: full Python suite passed 249 tests plus 19 subtests, including all merged ML/workspace/evidence tests. Adapter tests include a real temporary-parquet reader HTTP round-trip with training/simulation/network methods prohibited; disposable synthetic fixtures never populate demo data. Non-fatal upstream Starlette/httpx and NumPy timedelta deprecation warnings remain. Frontend/live HTTP checks follow.
- Outside /web: backend/API, dependencies, shared contracts and this outbox changed as requested. AGENTS.md and FROM_CLAUDE.md contents were not edited. The pre-existing user-authored AGENTS.md update is included unchanged in the requested complete publication.
- Blocked on: reviewed matching event/proxy evidence and sufficient history. No real-run exposure parquet exists; exposure/confidence/tariff placeholders remain honest.

## Tharun frontend HTTP integration — 2026-09-13 01:55 UTC
- Changed: retained Tharun's landing/navigation/design, with technical wiring only. Default API origin is http://127.0.0.1:8000 (VITE_API_BASE_URL override, explicitly empty for same-origin proxy). Each request also fetches GET /api/economics-assumptions; validated metadata drives economic controls, policy, and provenance. Cross-response arithmetic checks reject a mismatched file revision. Local mode/failure fallback remains usable, and local economic overrides are never sent as unsupported POST fields.
- New/changed interface: SPP_SYSTEM is the initial location and is labeled system aggregate with no site-specific coverage. City examples remain illustrative. The bundled offline assumptions snapshot exactly matches docs/ASSUMPTIONS.md's marked JSON. Estimate JSON remains canonical. Local derived economics retains mock provenance while its exposure or margin is placeholder. No landing, styling, favicon or decorative work added.
- Validation: 212 frontend tests passed; production TypeScript/Vite build passed with existing large-chunk advisory. Eight live GET/POST contract checks passed against the restarted API, including current defaults, slider changes, zero exposure/flexibility and different terms/locations. The real cached SPP load also reads successfully with network calls forbidden. Installed Python packages pass dependency compatibility checking.
- Team update: origin/Tharun advanced to d3b2770 during integration, fixing mixed economics provenance and stale tests. Its intent matches this integration; merging that latest commit next so the team history is included. origin/Kristian remains 2e4d9d2; no new main commits were present at the final fetch.
- Needs review: remaining ML evidence/model gaps are documented above; no demo exposure numbers were relabeled as real. API is running at http://127.0.0.1:8000 and Vite remains at http://127.0.0.1:5174.

## Sourced economics wired in and reconciled with the placeholder scaffold — Tharun, 2026-09-12/13
- Changed: independently sourced real economics inputs (docs/ASSUMPTIONS.md) in parallel with Alex's schema/pipeline-wiring work below, not knowing about it in advance. Reconciled on merge: kept Alex's machine-readable JSON schema and api/economics.py/pipeline_provider.py wholesale (strictly more robust than my own earlier direct-constant approach in api/estimate.py, which I discarded), and ported my sourced citations into that schema's six fields. Also shipped a landing page at `/` (workspace moves to `/app`), a mobile confidence-badge overlap fix, a favicon, and a local-reliability-issue tariff disclosure - all on `/web`, no conflict with backend work.
- New/changed interface: `docs/ASSUMPTIONS.md` status is now `mixed` - `gpu_rental_price_usd_per_hour` (3.00, H100 composite), `industrial_electricity_price_usd_per_mwh` (82.10, EIA Kansas), `gpus_per_mw` (575, grid-interconnection basis), and `early_connection_years` (4, LBNL Queued Up gap) are `source_type: "data"` with real HTTPS citations and a 2026-09-12 retrieval date. `early_margin_usd_per_mw_year` (317,000) and `close_call_fraction` (0.05, unchanged) stay `source_type: "assumption"`/`mock://` - no public opex/capex margin figure exists for AI-GPU-neocloud infra, so an explicit 3% assumed-margin-on-sourced-revenue derivation is documented in the file rather than treating the raw $10.58M/MW/yr gross revenue figure as a margin (that number breaks the decision math entirely - see the file's derivation section). `economics.source` still correctly stays `mock://` end-to-end per `is_mock = status != "sourced" or placeholder_exposure` - real sourcing here does not and should not flip that until exposure is also real.
- Validation: updated every test in api/test_economics.py and api/test_estimate.py that hardcoded the old scaffold's placeholder values (2/50/1000/3yr/500,000) or the old direct `mock://illustrative/economics-placeholder` ref format, retargeting the placeholder-provenance-validation tests at `close_call_fraction` (still genuinely placeholder) instead of `gpu_rental_price_usd_per_hour` (now genuinely sourced). All 152 backend tests, 178+ frontend tests, and both production builds pass. Verified in a real browser that the site_exposure slider still demonstrates all three decision states (0.4/0.55/0.9 → worth_it/close_call/not_worth_it) with the new real numbers, and that the landing page's live demo (same deriveScenario()) picked them up automatically.
- Needs review: the 3% margin assumption is mine, not cited - if anyone sources a real opex/capex-amortization figure for GPU-neocloud operating margins, replace it directly; the sourced $10,577,700 gross-revenue figure underneath does not need to change. Also flagging for Alex/whoever owns `/api` next: I did not add a `restart_overhead_minutes`, `checkpoint_overhead_pct`, `utilization_pct`, or SLA-tier field even though I sourced real data for them (see docs/ASSUMPTIONS.md's "Not yet wired into the calculation" table) - the six-field schema doesn't have room for them yet.

## Final concurrent Tharun merge — 2026-09-13 02:00 UTC
- Changed: included origin/Tharun d3b2770 and its intermediate Alex/Kristian merges. Its mixed-economics mock-label and test fixes are already represented by the tested integration; conflict resolution retains the direct HTTP/metadata wiring and matching offline snapshot. Preserved Tharun's additional outbox entry without duplicating the existing Alex history. No teammate branch was pushed.
- Reconciliation: selected density/rental/timing defaults stay assumptions rather than data because they are scenario choices/derivations; the research citations remain in full. The Kansas reference is attributed as reported, without new verification. The 3% margin remains explicitly unverified with mock provenance. This supersedes the historical Tharun outbox entry's data classifications and any claim that a decision flip validates the economics. No model output or UI design changed in this final merge.
- Validation: the final merge changes only the outbox relative to the tested integration; 249 Python tests plus 19 subtests, 212 frontend tests, 8 live HTTP checks, and production build have passed. Prepared real load remains in ignored local caches; no exposure model artifact is available.
- Publication: publishing the full integrated history to main. Frontend http://127.0.0.1:5174/app and API http://127.0.0.1:8000 remain running. Exact ML blockers and artifact handoff requirements are documented above and in BUILD_PLAN.md.

## Product name: Fluxline — 2026-09-13 02:03 UTC
- Changed: replaced the working product title with Fluxline in landing/workspace navigation and accessible names, both browser-tab titles, root/frontend README titles, economics-doc heading, scenario download filename, API documentation title, and ML workspace header/tab/launch message. Private frontend package name is fluxline-web. Text and name metadata only; no styling, layout, icons or graphics changed.
- New/changed interface: downloaded scenarios are named fluxline-scenario.json. JSON schemas, provenance markers, transport headers, environment variables and health identifiers remain compatible. Ordinary electrical transmission-headroom wording and historical outbox entries remain unchanged.
- Validation: production TypeScript/Vite build passed (existing chunk-size advisory only). Source audit found no remaining old product labels; live frontend HTML and OpenAPI titles display Fluxline. Package and lockfile names agree.
- Outside /web: README.md, docs/ASSUMPTIONS.md heading, api/main.py title, pipeline/workbench.py launch text and its HTML labels, plus this outbox, per the requested complete text rename. AGENTS.md and FROM_CLAUDE.md untouched. Publishing to main.
- Needs review: no design change; brand-mark/favicon artwork remains Tharun's design.

## Branch refresh and Tharun accessibility/presentation merge — 2026-09-13 03:15 UTC
- Changed: fetched all origin branches. Tharun advanced from d3b2770 to 042ece5: 1ed82eb aligns provenance-button accessible names with formatted visible labels, b800be4 incorporates prior main, and 042ece5 refines landing color discipline/closing-line spacing. Merged into main without conflicts as 9a83c72. These are Tharun's design changes; Fluxline branding is preserved. Kristian remains 2e4d9d2 and Alex remains ec9ae7f, both already integrated; origin/main had no incoming commits beyond our existing 595dcbb.
- New/changed interface: provenance primitives now compute accessible names from formatted/plain visible text; exact underlying values remain in provenance JSON. No API, model, schema, or runtime wiring changes.
- Validation: 212 frontend tests passed; 8 opt-in live tests skipped in the ordinary suite. TypeScript/production build passed with the existing large-chunk advisory. No backend changes required a Python rerun. Publishing merge and this log to main; teammate branches untouched.
- Needs review: nonblocking primitive API detail for Tharun: displayText is present in exported prop types but not forwarded as a caller override by Sourced/SourceInfo; current callers do not use it. No integration blocker found. No files outside /web changed except this outbox.

## Break-even sensitivity tornado chart — 2026-09-12 PDT
- Changed: added a Recharts horizontal interval chart on the results view, sorted by absolute dollar swing, with visible low/high-input decisions, exact-value provenance, keyboard-accessible axis sources, an endpoint table, bounded eased recomputation, and reduced-motion support. Tharun's existing landing/design remains the base; styles are scoped to the new panel.
- Accounting decision: user explicitly answered "Keep current formulas; mark electricity and utilization not modeled." GPU rental, flexibility split and site exposure vary independently; early contribution remains fixed. Rental affects gross interruption cost only, not the independent early operating-margin input. Electricity/utilization have documented sourced ranges but `status: "not_modeled"`, null outcomes and no zero-effect bars. Full flexibility/site ranges are user-assumption domains, not empirical confidence intervals. Bars show the change in the p50 annual-cost-path contract comparison, not a probability or a contract-value quantile; endpoint decisions retain the canonical p50/p90 rule.
- New/changed interface: POST /api/estimate and GET /api/economics-assumptions are unchanged. Scenario export adds `sensitivity: {baseline, rows, source, notes}`. Modeled rows have `{key,label,unit,baseline_input,status,low,high,swing_usd,source}`; endpoints contain `{input,delta_value_usd,snapshot,source}`. Unmodeled rows add `reason`, set swing/delta/snapshot to null, and retain sourced input ranges. Snapshot includes sourced p50/p90/p99 annual costs and comparison values, fixed early value, decision and nullable break-even hours. Exact TypeScript/JSON shape is in docs/frontend-api-contract.md#break-even-sensitivity-handoff; range/accounting policy in docs/ASSUMPTIONS.md#sensitivity-policy.
- Wiring: positive site factors recover unscaled exposure by inverse canonical arithmetic, without an extra HTTP request. At zero, a second canonical estimate with site_exposure=1 is necessary; source/model-version, confidence, scenario, exposure and economics consistency are checked before accepting it. Existing timeout/abort/stale-response handling covers that read. API failures stay explicit local mocks; the chart never combines API estimates with mock exposure. Strict-threshold rounding regression was fixed by matching canonical multiplication grouping and retaining received exposure for rental/flexibility sweeps.
- Validation: 264 frontend tests passed cleanly (256 unit/UI plus eight live FastAPI cases; live cases also compare every flexibility/site endpoint), production TypeScript/Vite build passed, and the full Python suite passed 249 tests plus 19 subtests. Existing large-chunk advisory and upstream Python deprecation warnings remain. Source audit found no banned styling in the new panel. Computer-use inventory returned no connected apps/browsers, so actual browser visual inspection remains for Tharun/human review at http://127.0.0.1:5174/app. Local API/Vite servers remain running.
- Outside /web: added policy/export documentation in docs/ASSUMPTIONS.md, docs/frontend-api-contract.md and a CP4 pointer in docs/BUILD_PLAN.md; no canonical API fields, machine-readable economic defaults, deadlines, pipeline code, AGENTS.md or FROM_CLAUDE.md changed. No standalone roadmap file was present, so implementation follows the explicit task and the available CP4 contract. Main was re-fetched with no incoming commits before publication. Pushing to main; teammate branches untouched.
- Needs review: economic margin/tolerance and exposure placeholders retain their existing mock status; this chart does not validate those inputs or introduce independent electricity/utilization effects. Team review can expand the accounting model later through a separately agreed contract.

## Ink and Signal visual restyle - 2026-09-12 22:48 PDT (uncommitted)
- Changed: replaced the amber/steel-teal frontend identity with the requested near-black surfaces, #212325 hairlines, #F2F1ED text, #8A8D88 muted text, and #7FD8CF active-state signal. Landing, workspace, fan/tornado/reliability charts, evidence panel, source popovers and surface colors now share the system. Tighter internal spacing, clearer section gaps, square controls, no gradients or shadows. Mock/model series remain neutral; mock and permanent site/confidence caveats retain their exact text and behavior.
- Brand/type: used the supplied octahedron and lowercase fluxline wordmark in both headers, vertically centered with the artwork's aspect ratio preserved inside its padded SVG canvas. Wired the supplied ICO, 32px PNG and Apple touch icon. Supplied SVG/favicon/brand masters were not regenerated or edited and remain untracked. Self-hosted Archivo 600 and Roboto Mono variable fonts plus OFL licenses from google/fonts (ofl/archivo and ofl/robotomono); retained Plex Sans body faces. Removed obsolete Fraunces and Plex Mono binaries/licenses. Heading/body font faces exclude ASCII digits so inline numbers also fall through to Roboto Mono; numeric components/axes/tables use tabular figures.
- New/changed interface: none. Canonical requests/responses, source objects, economics and sensitivity math, scenario state, slider semantics, animation hooks, confidence framing, and provenance mechanics are unchanged. No tests needed assertion edits and no tests were removed.
- Validation before -> after: frontend 264 -> 264 passed (256 unit/UI plus 8 live API cases, 16 files); Python 249 -> 249 passed plus 19 -> 19 subtests. Production TypeScript/Vite build passed. Existing large-chunk advisory and 17 upstream Python deprecation warnings remain. Source audit found no old palette/font references, gradients, or shadows in active /web source; protected AGENTS.md and historical outbox descriptions intentionally remain untouched.
- Browser review: isolated headless Edge checked landing/workspace at 1440px and 390px, including evidence panel and pinned source JSON. Actual loaded fonts are Archivo SemiBold, Roboto Mono and Plex Sans; favicon/preload requests return 200. No document horizontal overflow; the detailed sensitivity table retains its intentional internal horizontal scroll. Review screenshots are local/ignored under .tools/ink-and-signal. Vite remains at http://127.0.0.1:5174/app and the API at http://127.0.0.1:8000.
- Outside /web: repaired only the internal ML workbench's font allowlist and CSS font URL/alias in pipeline/workbench.py and pipeline/workbench_assets/style.css so deleting Plex Mono does not break that tool. Its broader visual identity is unchanged; no ML or API behavior changed. This outbox is the only documentation change outside /web.
- Needs review / limits: no unresolved /web restyling blocker. Hardware-rendered interactive WebGL appearance was not visually verified in the headless GPU-disabled review; only renderer colors changed, and existing render-loop/geometry/fallback tests pass. Existing landing claims about a trained ensemble and its unsourced preview/static figures predate this pass and need a separate content/provenance review; preserved them under the explicit visual-only restriction. AGENTS.md still describes the old identity and was not edited; this task follows the user's newer brief. Nothing staged, committed, or pushed; waiting for explicit publication instructions.


## Multi-factor high-demand exposure model and hours results - 2026-09-13 UTC
- Changed: added pipeline/history.py, generation.py, stress.py and hours.py; expanded prior-observation features with temperature/load interaction, load ramps and net demand. Added scored-period probability sums, monthly CSV, joint contract totals and local workspace hours/annual controls with an explicit site-exposure slider. Added safe explicit server restart, reproduction guide and a small version-controlled result snapshot.
- User scope: clarified that stress need not be an emergency and requested temperature plus load and other factors. The starting target is an explicit high-demand proxy (reference 95th percentile), not an inferred physical overload. Threshold 42,064.0228 MW was fitted on 2019 through 2021 only. No change to label.py or train.py calibration.
- Real data/model: cached 2019-2024 SPP load and generation plus Amarillo ERA5; 52,608 slots, 67 unknown loads and 1,029 missing complete hourly wind/solar observations. Fifteen-member model spp_lgbm_20260913T041851_30b0029de7 uses load, temperature, actual wind/solar, calendar and their past interactions. Expected held-out proxy hours 819.3906 versus 835 observed, across 10,426 scored hours; Brier 0.003016 versus prevalence 0.074131 and prior-status persistence 0.018415. These evaluate the demand proxy only.
- Annual research output: 2,000 seven-year seasonal trials, first-year system P50/P90/P99 834/950.10/1033.01 hours; joint seven-year totals 5876.5/6161.1/6387.0. Confidence Low. No growth/climate projection, no actual site cutoff forecast. Site assumption applied only in the workspace view, never baked into pipeline outputs.
- Interface: train now saves hours_summary.json/monthly_expected_hours.csv; workflow hours summarizes older runs, simulate additionally writes contract_hours.json. Workspace GET /api/run adds hours and annual; explicit POST kind=hours accepts an existing run and years=0 for scored-period summary or 1-7 for simulation. Read-only page loads never fetch/train. Model downloads and controls verified at http://127.0.0.1:8765. Existing /api and /web provider contracts unchanged.
- Validation: all 53 tests pass (new probability-sum/gap/duplicate/timezone/ensemble checks, joint-trial quantiles, past-only multi-factor features, generation gaps/conflicts and threshold train/test separation). Live hours job succeeded, live model/annual/download endpoints verified and workspace JavaScript syntax compiled successfully. Existing non-fatal pandas/NumPy warning in events.py remains. No connected browser visual verification is claimed.
- Needs review: demand-proxy validity, threshold choice, regional relevance of Amarillo, inherited calibration, confidence density and unvalidated annual tails. Reserves, constraints, outages, imports and prices remain unavailable in this fitted model and are disclosed. Private model/data caches remain local/ignored; docs/multifactor-model-summary.json records provenance/results.
- Publication: continuing the user-authorized Kristian branch workflow; do not push or merge main. The local workspace is running with the real model.
## Arbitrary location search and automated exposure reports - 2026-09-13 UTC
- User scope: accepts user-chosen cities/state or parcel coordinates, without a curated city list. Added local workspace search/candidate selection and an explicit scan-and-report job. PNNL's historical HIFLD utility polygons screen SPP applicability; other-grid matches do not calculate hours, and unverified coverage needs a saved explicit user assertion. This does not verify current utility service or post-2024 SPP expansion.
- Changed: pipeline/site.py orchestrates cached location-specific ERA5 retrieval, removes the original Amarillo weather, pairs the chosen point with shared 2019-2024 SPP load/wind/solar, fits the existing high-demand proxy ensemble, and simulates seasonal annual/term exposure. Cache keys include coordinates, source data and pipeline hashes/settings; completed model/simulation artifacts are hash-checked on reuse. No label.py or train.py/calibration changes.
- Report: annual system and assumed-site P50/P90/P99 clock hours, joint term totals, conditional MWh, exact prior-hour historical conditions, monthly and temperature comparisons, held-out evaluation, input coverage, grid/weather source URLs and hashes, and limitations. Facility MW and conditional share affect energy only. Site exposure remains an explicit downstream slider. Research confidence is Low; these are high-demand proxy scenarios, not actual site cutoffs, all forms of network stress, tariff-trigger predictions or future dates.
- Interface: POST /api/jobs kinds site-scan and site-report invoke the offline workflow; GET /api/site-scan, /api/site-report and allowlisted /api/site-download read saved results. Only server-registered search candidates can start a report. Existing host/token/origin protection and one-job limit apply. Demo /api and /web contracts unchanged. Docs/LOCATION_REPORTS.md explains the click-by-click workflow; local workspace remains running at http://127.0.0.1:8765.
- Real validation: Wichita, KS resolved to 37.69224,-97.33754 with historical SPP territory matches. Real weather downloaded/cached for 2019-2025 (2025 supplies the final UTC boundary of the 2019-2024 SPP archive). Full location model/report and HTML/Markdown/JSON downloads succeeded. First-year system P50/P90/P99 833/949.10/1034.02 h, expected held-out proxy hours 815.4402 over 10,426 scored hours (75 unscored), Brier 0.003053. All annual confidence Low. docs/location-model-summary.json records the example and provenance.
- Reuse validation: entered the same point directly as coordinates and requested 200 MW, 50% conditional load, 25% site exposure and three years. The exact fitted model/simulation was reused; first-year assumed site P50 208.25 h and conditional energy 20,825 MWh verified, with no new fit or simulation.
- Tests: full 61-test suite passed before final report polish; all 16 affected site/workspace tests passed afterward (9 new site tests; 62 total tests now). Checked JS syntax, UI IDs, live job/status/report/download interfaces, download traversal rejection and artifact provenance. No connected browser was available for visual verification. Existing events.py NumPy timedelta warning remains unrelated.
- Publication: follows the user's existing authorization to publish to Kristian only. No main push or merge. Raw downloads, private data/model caches and runtime files remain ignored; only code, tests, docs and a small example result snapshot are included. Human review of proxy suitability, applicability, inherited calibration and annual tails remains necessary before production use.



## GitHub updates merged into local Kristian - 2026-09-13 05:06 UTC
- Changed: fetched origin; Kristian already matched origin/Kristian at b67e767. Merged 27 shared-main commits through b15fb9a into local Kristian, preserving the location-report and multi-factor ML work. The only conflict was this handoff log; both complete histories were retained. Received AGENTS.md updates unchanged from main.
- Validation: all 62 ML/workspace tests pass. The running local workspace responds successfully, displays Fluxline branding and retains its location controls and saved real-data reports. No remaining conflict markers or whitespace errors.
- Scope: local synchronization only; no GitHub push and no model retraining. Frontend/API suites were not run because Node/frontend dependencies and FastAPI are absent from this machine environment. Incoming implementation outside the conflict was retained without extra edits.

## Learned warning signs and regional transfer - 2026-09-13 UTC
- Changed: added pipeline/signals.py for exact calibrated tree-contribution explanations, signed input groups, training-defined parameter comparisons evaluated on later observations, and similar historical hours. Added pipeline/regional.py to train on separate SPS, OKGE, LES and OPPD load-area high-demand proxies with representative local ERA5 weather and shared SPP demand/wind/solar histories. New query locations have unknown outcomes, preserved as missing. Existing train.py, label.py and their calibration are unchanged.
- Interface: the local workspace offers Learn stress signs across areas and Explain this saved model's warning signs. POST job kinds site-transfer and site-signals preserve registered-input, Host/Origin/token and one-job protections. Reports show parameter thresholds, missing inputs, direction agreement, historical comparisons, and validation on completely excluded areas. Inference hours use monthly probability expectations; no transferred P50/P90/P99 outcome quantiles are manufactured. Site exposure remains a visible downstream assumption. Demo /api and /web are unchanged.
- Real validation: model spp_regional_cc7d60f6ff429cb1e0cb completed all three excluded-area folds and the pooled fit. Every evaluation starts after that fold's calibration, including unequal-history cases. All four areas beat their training-prevalence Brier baseline, but hour calibration varies substantially: OPPD estimated 302.5 proxy hours versus 1,181 observed. Confidence remains Low; this does not validate actual cutoff behavior or new-site annual totals.
- Wichita example: 10,430 query hours scored, 72 unscored; stationary annual expected regional high-demand exposure 631.0057 h. At 100 MW, full conditional load and site exposure 1, conditional energy is 63,100.57 MWh/year. The highest-scored historical hour used prior temperature 34.5 C and SPP demand 50,887.832 MW, with temperature/demand interaction the largest positive explanation. Sources, input hashes, separate area thresholds and validation results are recorded in docs/regional-warning-summary.json; full raw inputs and models remain ignored local caches.
- Reuse verification: a second explicit report reused the exact model without fitting, with 200 MW, 50% conditional share, 25% site exposure and three years. Regional expected hours stayed unchanged; assumed-site annual hours 157.7514 and conditional energy 15,775.14 MWh verified. Existing Wichita system-model explanation also completed without retraining.
- Tests: all 71 ML/workspace tests pass, including calibrated contribution reconstruction, negative calibration slopes, unlabeled inference, missing observations, separate area labels, time/area exclusion, uneven-history leakage regression, annual coverage and invalid probabilities. Live report/status and all three downloads passed, traversal was rejected, and workspace JavaScript syntax and referenced HTML IDs passed. No connected browser visual verification is claimed. Existing events.py NumPy warning remains unrelated.
- Publication and limits: user-authorized Kristian branch only; no push to main or teammate branches. AGENTS.md and FROM_CLAUDE.md were not edited. The workspace remains at http://127.0.0.1:8765. Actual local constraints, reserves, outages, site interruption logs and operating-rule triggers are absent; explanations concern a research high-demand proxy, not a future cutoff schedule. See docs/WARNING_SIGNS.md for workflow and interpretation.

## Simplified location inputs and hours output - 2026-09-13 UTC
- Changed: replaced the local ML workspace's dense research screen with a responsive location/facility form and a prominent annual site-hours result. Inputs are location, MW, flexible share, visible site-exposure assumption and years. Supporting outputs show full-term site hours, annual MWh and regional high-demand hours. Removed warning-sign displays, model selection, training controls and research charts from the main screen at the user's request; underlying model/evidence and research API/CLI remain available.
- Flow: Estimate hours explicitly starts a location search and automatically continues to site-transfer only for a single match and unchanged captured inputs. Ambiguous matches require selection. Historical SPP confirmation is never automatic and resets when location changes. Saved results restore their original assumptions; editing inputs flags the existing result until recalculated. Late responses cannot replace a newer selection. No automatic jobs on page load or refresh.
- Output: all numbers remain modeled exposure with Low confidence, clear units and source access. Earlier saved simulations retain their P50 meaning and joint-term totals. Added a readable text export from saved values plus the full original JSON download. No warning-sign explanations are displayed or included in the plain-text summary. Updated README and local workflow/model guides.
- Validation: nine JavaScript behavior checks passed in the Node runtime, plus all seven workspace and nine site Python tests. Verified real saved Wichita full/quarter-exposure and legacy-simulation values, export units and source references. Live HTML/JS/CSS/fonts and report endpoints pass; all referenced HTML IDs exist. Computer-use inventory had no connected browser, so visual browser inspection was unavailable.
- Scope: static ML workspace assets, UI tests, documentation and this outbox only. No changes to models, labels, calibration, cached inputs, demo /api or /web, AGENTS.md or FROM_CLAUDE.md. Local app remains at http://127.0.0.1:8765; publishing to Kristian only under the user's standing instruction.

## Main synchronized into Kristian - 2026-09-13 UTC
- Changed: after publishing the simplified estimator as db0255e, fetched origin and merged main through ad01055 into Kristian as 83d1053 without conflicts. Received five main-side commits covering frontend comparison/climate/history features, 21-zone artifacts, optional XGBoost ensemble members and relaxed simulation-history requirements. Preserved the simplified local estimator.
- Validation: all 71 Python pipeline/workspace tests pass. XGBoost and FastAPI are absent from this local environment, so the optional XGBoost branch and frontend/API suites were not exercised. No model retraining or artifact regeneration was performed during synchronization.
- Needs review: incoming simulation now substitutes other available blocks for missing months; upstream annual outputs retain experimental status. If XGBoost is enabled later, pipeline/signals.py currently uses LightGBM-specific contribution methods and needs a compatible XGBoost adapter. These upstream changes were retained as requested; this sync does not certify their scientific validity.
- Publication: push the merged history and this handoff to Kristian only. No push to main, no changes to AGENTS.md or FROM_CLAUDE.md.

## Team integration paused by user - frontend checkpoint 656e4fc
- Changed: fetched main ec8d203, Kristian b67e767 and Tharun 0adc04a. Saved the approved Ink and Signal frontend/assets in local main commit 656e4fc. Prepared merges only in isolated .tools/integration-ink/checkout on integration/ink-signal-ml (7bd8017). Its /web and brand Git trees exactly match the frontend checkpoint. Root app files and running servers remain on the preserved frontend; nothing was pushed.
- ML finding: main's published artifact is the separate 2024 observed-event run spp_lgbm_20260913T055001_b63ee2577e, with only October 20-December 31 held-out history. Its missing-season substitution cannot support the claimed seasonal annual output. Retained strict simulator guards in the isolated merge. Kristian's newer 2019-2024 multifactor code/summaries are included there, but its matching actual run bundle remains absent.
- New/changed interface: no published change. Isolated uncommitted API readiness guard/tests and API/BUILD_PLAN documentation require a full held-out year and 8760 scored hours per location before serving annual model provenance; insufficient evidence retains explicit placeholders. Focused API tests passed 127; focused merged ML checks passed 23 plus 16 subtests. Full combined suite, live round-trip, final build, bringing integration into root main and push are still pending.
- Paused: user explicitly asked to return only after Kristian pushes the previously ignored files. Resume from the saved integration checkout, re-read team instructions/updates, and re-fetch then. Required newer-run bundle: exposure_by_location.parquet, model_card.json and simulation_metadata.json together; for model iteration also model.joblib, test_predictions.parquet, simulation_trials.parquet and input/policy manifests. Do not reconstruct artifacts from rounded JSON summaries or overwrite the preserved frontend.

## Landing preview label shortened - 2026-09-12 (uncommitted)
- Changed: removed the requested "— SAME MATH AS THE FULL TOOL" suffix from web/src/Landing.tsx; label now reads "TRY IT". No styling, calculation or provenance changes.
- New/changed interface: none. Scoped diff/search checked; no new tests needed for this text-only deletion. No commit or push.
- Needs review: integration remains paused. Preserve this newer root frontend edit when resuming the isolated integration checkout; it intentionally postdates frontend checkpoint 656e4fc.

## Client-facing scenario copy - 2026-09-12 23:49 PDT
- Changed: removed TRY IT entirely. Replaced visible illustrative/mock/developer wording with scenario locations, assumed exposure/economics/sensitivity, supplied defaults, evaluation unavailable and tariff evidence not supplied. The landing preview explicitly remains an assumed scenario. Source-dependent labels still disappear only when their data earns it; unavailable diagnostics and unverified economics remain disclosed.
- New/changed interface: no request/response, data, calculation or source-object changes. Exact original references, confidence basis and tariff records remain available in provenance inspection/export, including historical mock://illustrative identifiers. Ink and Signal styling, fonts, assets and layout are unchanged.
- Validation: all 264 frontend tests passed, including eight live cases against the merged API on temporary port 8001. Updated only affected copy assertions; no tests removed. This frontend supersedes the earlier TRY IT-only edit and will be retained in the resumed integration.

## Latest Kristian integration with frontend preserved - 2026-09-12 23:51 PDT
- Changed: resumed at user request; fetched Kristian c53ac3c, main ad01055 and Tharun 0adc04a. Integrated Kristian's multifactor, regional transfer, warning-sign and simplified ML-workspace changes, including inherited main history. Tharun's history is included through main; incoming /web comparison/telemetry/router changes were excluded to preserve the user's chosen frontend. Final /web and brand trees exactly match frontend checkpoint 389d121, including all requested client-facing copy edits. No teammate branch was pushed.
- Artifact status: only the three canonical files were published, now model spp_lgbm_20260913T060709_b63ee2577e. They still use 2024 observed-event labels with 1742 held-out clock hours and 1717 scored hours per location, not Kristian's newer six-year multifactor/regional model. The newer actual run files, trained model and caches remain absent; summaries are not reconstructed into artifacts. The published run's held-out Brier 0.005033 is worse than its 0.002905 baseline; annual confidence remains Low.
- Backend protection: retained strict one-year/all-season simulator guards and the one-year rejection test removed upstream. Added an API readiness check using inclusive split endpoints and per-location scored hours; missing/insufficient annual evidence yields the existing explicit assumption placeholder with model/version/reason, while malformed/mismatched artifacts still fail. A live POST against the pulled canonical artifact returned 200 with X-Headroom-Exposure-Source=placeholder and the exact 1742/1717-hour reason. No silent substitution of missing seasons or node aliases enters the frontend.
- New/changed interface: canonical POST/GET shapes, economics math and source objects unchanged. BUILD_PLAN.md and api/README.md document annual readiness and the exact three-file handoff. New regional inference supplies expected hours only, so it is not substituted into p50/p90/p99 fields. All heavy operations remain explicit offline ML-workspace jobs; the estimate API only reads saved output.
- Validation: full Python suite 299 passed plus 30 subtests; all nine standalone estimator JavaScript checks passed; full frontend 264 passed including eight live cases against the merged API; production build passed. Existing bundle-size/deprecation warnings remain. Protected AGENTS.md/FROM_CLAUDE.md and label.py are unchanged. Restored strict simulation test rather than accepting a weakened test.
- Needs review: incoming optional XGBoost training path was retained from teammate code but XGBoost is absent in this environment and undeclared in requirements-ml.txt; passing tests exercise LightGBM. pipeline/signals.py still assumes LightGBM contribution methods and needs an adapter before mixed-ensemble explanations can run. Calibration/model-family settings and all target/annual research assumptions still need ML-owner review. No new model fit or external grid-data fetch was performed. Obtain the newer matching parquet/model-card/simulation-metadata bundle (and model.joblib, predictions/trials/input manifests for iteration) before activating it.
- Publication: completing the user's authorized main integration and push; existing Ink and Signal assets/design and the latest copy are preserved. Local API will be restarted with the readiness guard; Vite remains at http://127.0.0.1:5174/app.

## Scenario location catalog wired to parquet - 2026-09-13 20:56 UTC
- Changed: `/app` now fetches selectable IDs from new `GET /api/locations`, reading only `location_id` from the configured exposure parquet. Chose a dynamic endpoint so future artifact IDs appear without a frontend release. Current catalog contains 17 load zones, SPP_SYSTEM, and the three existing demos: 21 options total. Zones are labeled `<ID> · SPP load zone`; demos retain `· scenario`. No styling, model, economics, carbon, existing estimate contract or artifact changes.
- New interface: `{"locations":[{"id":"SPP_SYSTEM","label":"SPP system aggregate","kind":"system"},{"id":"CSWS","label":"CSWS · SPP load zone","kind":"zone"},{"id":"spp-wichita-demo","label":"Wichita, KS · scenario","kind":"scenario"}]}` (example entries, not the complete catalog). `kind` is `system|zone|scenario`; IDs are unique. System comes first, zones sort by ID, demos come last. Missing parquet retains the original four choices; a present malformed/unreadable catalog returns 503. The endpoint reads local precomputed data only and does not claim annual estimate readiness.
- Frontend: selecting a zone posts its exact ID through the existing estimate transport. Bounded/cancellable catalog loading retains usable choices on failure and does not reset a selected ID. New-zone pending/offline rendering reuses the unchanged system mock formula with explicit assumption/mock provenance identifying the requested zone and generic fixture; this also prevents the results/chart-axis path crashing on a previously unknown ID. The existing fallback status remains visible. Original four-location arithmetic and all HTTP response source objects remain unchanged.
- Real-data verification / blocker: live GET returns all 21 choices. Live POSTs for **CSWS, OKGE and LES each return 404**, not real modeled-exposure values. Direct `get_location_estimate()` can read these IDs, but the existing provider rejects their 1742-hour held-out span / 1717 local scored hours against its 8760-hour annual requirement. Its existing placeholder set has no real-zone entries. Model `spp_lgbm_20260913T060709_b63ee2577e`; parquet SHA256 `ffe0e5e3f633fd19065611cae868f6066bb77dfc1803a271d955f4213671ce35`. No guard was bypassed and no model provenance was invented. Actual three-zone real-output acceptance remains blocked until the ML owner supplies a valid matching annual artifact bundle; fixing that is outside this dropdown-only task.
- Tests distinguish catalog membership from model readiness: authored full-year software fixtures for CSWS/OKGE/LES pass through the actual parquet reader/provider/POST with model-sourced output, while a short-history fixture still fails. Frontend tests cover all 21 IDs, exact three-zone requests, future IDs, catalog errors, pending/404 fallback, and the complete results view. These fixtures are explicitly test data, not claims about the production artifact. Live contract coverage also checks the current catalog.
- Validation before changes: full Python 1,624 passed + 30 subtests; frontend 267 passed; production build passed. The initial concurrent baseline frontend run had 266 passes / one existing 5-second App-test timeout while Python ran; the unchanged serial rerun passed all 267. Final suites ran serially: Python **1,643 passed + 30 subtests**, frontend **287 passed** including nine live API tests, TypeScript/production build passed. Same 17 Python deprecation warnings and existing Vite chunk-size warning; no tests removed. Independent review found and corrected a malformed array-valued catalog-kind validation gap.
- Scope / publication: API catalog plumbing and this outbox are the authorized changes outside `/web`; protected AGENTS.md/FROM_CLAUDE.md and all three canonical data artifacts retain their hashes. Restarted the existing local API at 127.0.0.1:8000; Vite remains at 127.0.0.1:5174/app. Committing/pushing this task to **Alex only**, with no merge or push to main or teammate branches. Overnight wind-classifier/carbon/standalone-grid-impact work and its integration limits are recorded separately in `docs/FROM_ALEX.md`.

## Serve precompiled grid-impact snapshots over HTTP - 2026-09-13 21:16 UTC
- Changed: added `GET /api/grid-impact/{location_id}` in `api/main.py`, calling `read_live_grid_impact` -> `read_grid_impact_snapshot` with a retained, bounded validation cache. Requests only read precompiled files; no raw composition, fetching or training. Existing API handler ASTs, including POST /api/estimate, are unchanged; no frontend, economics or pipeline files changed.
- Interface: unchanged `grid-impact-v1` result with `location_id`, `schema_version`, `boundary`, `units`, `basis`, `coverage` (wind/shift), `evidence_context`, `evidence`, and six sourced quantities: `wind_absorption_mwh_in_observed_hours`, `wind_absorption_mwh_per_year`, `carbon_absorbed_tonnes_in_observed_hours`, `carbon_absorbed_tonnes_per_year`, `carbon_shifted_tonnes_in_observed_hours`, `carbon_shifted_tonnes_per_year`. Every quantity is `{value: number|null, source_type, ref}`; local evidence pointers resolve within the returned dictionary. This GET serves a fixed offline capacity scenario, independent of /api/estimate inputs. Valid partial/unavailable snapshots return 200; missing files/IDs 404; invalid/unreadable files 503; unsafe IDs 422. Success/404/503 carry `Cache-Control: no-store`; existing CORS permits 127.0.0.1:5174.
- Offline bundle: explicitly included `data/processed/grid_impact/live_v1/` in this commit despite the general processed-data ignore. Compiled 21 unavailable catalog snapshots with `compile_grid_impact_snapshot(id, path)` and copied eight verified 2025 point snapshots byte for byte; 29 snapshots + manifest, 3,418,702 bytes. No original artifacts were overwritten. Default serving uses that directory; `FLUXLINE_GRID_IMPACT_DIR` can select another compiled bundle. Manifest records exact IDs and source/file hashes; staged Git blobs match all 29 snapshot hashes.
- Verified observations: AEPM_CSWS, LES_LES, OKGE_OKGE, OPPD_OPPD, SPS_SPS, WFEC_WFEC, SPPNORTH_HUB and SPPSOUTH_HUB return existing real-observation-based wind capacity scenarios. Examples: LES_LES 38,200 MWh, OKGE_OKGE 15,250 MWh, AEPM_CSWS 1,400 MWh in evaluable 2025 hours, under explicit 100 MW / 0.5 upward-availability assumptions. Output provenance remains assumption; these are not measured recoverable energy. All eight have 8,687 evaluable / 73 unknown hours; annual quantities and carbon shifted stay null. Associated wind operational CO2 is zero, not a carbon offset or atmospheric removal.
- Unavailable / needs review: all 18 catalog aggregate/zone IDs (CSWS, EDE, GRDA, INDN, KACY, KCPL, LES, MPS, NPPD, OKGE, OPPD, SECI, SPP_SYSTEM, SPRM, SPS, WAUE, WFEC, WR), plus the three demo scenarios, return six sourced nulls and unavailable coverage. Exact point identifiers are not load-zone aliases; no reviewed mapping or complete exact-zone input/supported shift schedule exists in the supplied bundle. No real location has complete annual/shifted-carbon output here. The complete-data HTTP branch is verified with explicitly authored software fixtures, not substituted production numbers.
- Validation: Python before **1,643** -> after **1,673 passing**, both +30 subtests and the same 17 deprecation warnings; frontend **287 -> 287 passing**, including nine existing live API cases; production build passed with the existing chunk-size warning. Added 30 tests covering real published snapshots, complete/partial/unknown authored cases, source preservation, no composition on requests, corruption, missing files, cache deletion, CORS and path handling. All 29 actual live HTTP responses equal their validated snapshots; `http://127.0.0.1:8000/api/grid-impact/LES_LES` is running. Verification `.tools/grid_impact_live_http_verification.json` SHA256 `207d500476520756d1fd0349807605e41c27bfce930486c46ca8f26e9bf59fce`.
- Publication: source edits confined to api/grid_impact.py and api/main.py, plus the expressly requested tests, compiled snapshots and this log. Protected team files, canonical artifacts and model files are unchanged. Commit/push to **Alex only**; no main or teammate-branch push. Frontend consumption can now use the additive endpoint without a proxy; UI integration remains with Tharun.

## Six documented zone references enabled - 2026-09-13 21:47 UTC
- Changed: `GET /api/grid-impact/{location_id}` now exposes existing settlement-point scenarios for **CSWS -> AEPM_CSWS, LES -> LES_LES, OKGE -> OKGE_OKGE, OPPD -> OPPD_OPPD, SPS -> SPS_SPS, WFEC -> WFEC_WFEC**. This is a within-area network-load price reference, not an assertion that a point measures the whole zone. All original snapshot bytes and quantities remain unchanged; no model, economics, frontend or POST /api/estimate change.
- Justification: `pipeline/prepare.py` defines the component load-area IDs; `pipeline/regional.py` has city/area references but no settlement crosswalk, and `site.py` supplies none. [SPP Protocols v38, section 6.7.3(1), printed p679](https://www.spp.org/documents/39052/integrated%20marketplace%20protocols%2038.pdf#page=679) documents the repeated-area network-load name. [SPP Markets UI Guide v21, Query Settlement Locations, printed p36](https://www.spp.org/documents/66792/integrated%20marketplace%20market%20user%20interface%20guide%20v21.0%2020230612.pdf) identifies aggregate CSWS_LA with AEPM_CSWS. All twelve cached official 2025 monthly price files independently confirm the six exact Location/Pnode pairs; no suffix-only guessing, city proximity, or hub assignment was used. The code whitelist is limited to the reviewed 2025 observation period.
- Interface: mapped live responses keep `location_id` equal to the requested zone and add `location_mapping: {requested_location_id, source_location_id, pnode, relationship, scope, source: {source_type: "data", ref}}`. Its relationship is `network_load_settlement_location_within_settlement_area`; scope is `point_reference_scenario_not_zone_total_or_site_deliverability`. Each scalar retains its original point datum in the evidence graph plus mapping evidence and an explicit reference-scenario assumption; numerical values and existing assumption source types are unchanged. This optional metadata extends only the live response; exact-point responses and the strict stored snapshot schema are unchanged. Frontend consumers should retain the named source point and scope when displaying a mapped result.
- Availability: **6 of 21 dropdown choices now return sourced observed-period wind scenarios; 15 remain unavailable**. Remaining IDs: EDE, GRDA, INDN, KACY, KCPL, MPS, NPPD, SECI, SPRM, WAUE, WR, SPP_SYSTEM, and the three demo scenarios. There is no reviewed association from those entries to the eight supplied snapshots; the two trading hubs cannot stand for a whole system/zone. All annual and shifted-carbon totals still remain null. The six values still use the published 100 MW / 0.5 upward-availability assumptions, with associated wind operational CO2 zero; they are not measured recoverable wind or carbon offsets.
- Verification: all 21 live endpoint requests passed, with exact point quantities and final evidence references preserved; six mapped and fifteen sourced-null results. Added 14 regressions, including direct-zone-data priority, missing/corrupt/deleted references, reviewed-year bounds, no arbitrary name/hub mapping, immutable originals and complete source retention. Full Python **1,687 passed + 30 subtests** (14 more than the previous checkpoint), frontend **287 passed** including live API cases, production build passed; existing 17 Python warnings and Vite size warning remain. Final 44 HTTP tests also passed after evidence encoding was made independent of Windows line-ending conversion.
- Evidence / publication: committed `data/processed/grid_impact/live_v1/zone_mapping_evidence.json`, SHA256 `daf3fc27f105421a0c992f1a45e02a67f2e4566be4d4514415b5a0042eea5bed`, records the primary sources, exact pairs and all twelve source/cache hashes. The serving manifest documents the new resolver behavior without rewriting old snapshots. Local API is running at 127.0.0.1:8000 with the final mapping. Push this code, tests, evidence and log to **Alex only**.

## Grid Impact on /app: wind, carbon framing, cheap-power scenario and surface — 2026-09-13
- Changed: the scenario results now request `GET /api/grid-impact/{location_id}` independently of estimate readiness whenever the selection changes. Added an Ink & Signal panel with wind-consumption scenario, cheap-power value, carbon shifted, associated wind operating emissions, sourced monthly chart/table, evidence download and visible unavailable/error/retry states. Load changes recompute locally; old-zone replies are aborted/ignored. No estimate contract, economics, exposure math, ML, ranking, landing page, AGENTS.md or FROM_CLAUDE.md changes.
- Formula: `USD = selected_load_mw * upward_available_fraction * SUM(-LMP_usd_mwh * 1 hour)` across evaluable hours where system wind/load >= 0.5 AND exact-point day-ahead LMP <= 0. The reference is explicitly **$0/MWh**, avoiding an invented normal/retail tariff. Zero-price hours add MWh but no dollar premium. `MWh = selected_load_mw * upward_available_fraction * qualifying_hour_count`. Upward availability starts at the published 0.5 and has its own visible adjustable control; it is not site_exposure or interruption flexibility. All resulting numbers are `source_type: assumption`, with exact load, fraction, formula and original evidence in their refs. This is conditional wholesale energy value, not measured savings, guaranteed retail pass-through, local deliverability or verified curtailed-wind capture.
- Offline evidence: new `api/grid_power.py` compiles the pinned `data/processed/wind_audits/proxy_ver_2025_v1/hourly.parquet` (SHA256 54f0692fdbf0fca2fd724e0fed699bf350dad873cb009afea387e3faafe4b2f2). It independently reproduces every published count and creates 12 x 24 UTC month/hour bins per point. Committed `data/processed/grid_impact/live_v1/cheap_power_v1.json` is 1,979,415 bytes, SHA256 28809ff68bbc7605f47f676b7442d6e0d2bbfb76204b8472a3e5dea01a6e3249, identified by the serving manifest. Serving reads only compiled JSON, validates integrity/identity/counts, and binds price evidence to the exact wind snapshot; missing/stale prices stay unavailable, malformed evidence follows existing 503 handling. No raw hourly reads, external fetching or classification in HTTP. Old point snapshots remain byte-for-byte unchanged.
- Additive live interface: `cheap_power` is either `{status: "unavailable", reason: string, bins: []}` or `{status: "observed_hours_only", source_location_id: string, snapshot_sha256: string, basis: string, input_source: {source_type, ref}, reference_price_usd_mwh: S, proxy_hours: S, unknown_hours: S, usd_per_available_mw: S, bins: [{month: S, hour_utc: S, proxy_hours: S, unknown_hours: S, usd_per_available_mw: S}]}`. `S = {value: number, source_type: "assumption", ref: string}`; bins are ordered by month 1–12 then UTC hour 0–23. Month/hour coordinates are calendar values; per-MW dollar quantities integrate one-hour LMP observations. `grid-power://...` bin refs resolve through returned `cheap_power.input_source` and `basis`; upstream wind/carbon DAG references are retained in the complete downloadable response. Existing six grid-impact quantities, source graph, location_mapping and `/api/estimate` shape/math remain unchanged. Frontend validates transport before any multiplication and treats older servers without cheap_power as price-unavailable.
- Visualization: Monthly / Surface toggle; the surface base is **month x UTC hour**, height **observed wholesale scenario USD**. This makes the timing and dollar concentration visible without inventing contract-year forecasts or percentiles. The inspector shows the supplied bin's qualifying hours, MWh, dollars and excluded unknown hours. Reused the existing Three.js demand-driven renderer, shared rotation/zoom/reset controls, nearest-supplied-point picking and keyboard interaction. Monthly chart is default, lazy 3D is opt-in, and WebGL/lazy-load failure returns to the chart/table. Faces are display interpolation, not new observations.
- Real checks: LES (LES_LES): 764 qualifying hours, $7,940.537 per available MW; at 100 MW and 0.5 availability = 38,200 MWh and **$397,026.85**. OKGE (OKGE_OKGE): 305 hours, $2,061.7952/MW = 15,250 MWh and **$103,089.76**. Live CSWS also verified: 28 hours, 1,400 MWh, **$6,307.885** at the same controls. Live EDE remains unavailable. All six mapped zones (CSWS/LES/OKGE/OPPD/SPS/WFEC) have price evidence; the other fifteen catalog entries still have no defensible mapping and show no dollar figure. First/warm local endpoint timings were approximately 172/95 ms for available responses, 7 ms for EDE.
- Carbon/coverage limits: each supplied point has 8,687 evaluable and 73 unknown hours over the 2025 UTC observation period. No missing-hour fill or annual/contract extrapolation. Carbon shifted remains **null/unavailable** without supported shift schedules and intensities. The existing associated-wind operating-emissions value is zero, explicitly **not** carbon absorbed, removed, avoided, or an offset. The frontend does not turn that zero into a sustainability-benefit claim.
- Verification: BEFORE full Python **1,687 passed + 30 subtests**, frontend **287 passed**. AFTER full Python **1,717 passed + 30 subtests**, frontend **310 passed**, including 12 live HTTP checks; TypeScript and production build pass. Added real-artifact/HTTP tests for six mapped plus fifteen unavailable choices, exact offline reproduction, corrupt/stale evidence, source validation, dollar/MWh arithmetic, selection races, load/availability updates, camera/point inspection and idle-loop regression. Existing 17 Python deprecation warnings and Vite >500 kB warning remain; no tests deleted. An initial targeted pytest run lacked a writable basetemp and hit Windows temp-directory setup errors; all required runs used the normal explicit writable basetemp and passed. Browser automation reported no connected browsers, so screenshot/GPU visual verification remains for a human; jsdom controls/geometry/fallback were verified. An auto-review rejected an attempted whole-file HEAD-based encoding repair; a small preserving patch fixed only the affected glyphs instead, with no lost edits.
- Publication: current branch **Alex** only. Local API restarted at 127.0.0.1:8000 with the new attachment; existing Vite remains running at 127.0.0.1:5174/app. Out-of-/web changes are the explicitly requested additive API/price compilation, serving evidence, tests and this log. No main or teammate-branch push.

## Remove standalone source info icons — 2026-09-13
- Changed: removed all shared SourceInfo "i" buttons and their popup triggers. Their source objects remain as nonvisual companion metadata; displayed Sourced values and chart ticks still support click/keyboard inspection. Tariff provenance now opens from the existing clause text, with its wrapping/type preserved. Updated the footer hint. No data, math, API or honesty copy changed.
- New/changed interface: none. Updated existing icon-based tests to assert icon absence, retained input metadata and direct-value inspection; no tests deleted.
- Verification: full frontend run after assertion updates passed 308/310, with two existing App timing failures (async retry lookup / 5-second test budget). Isolated App rerun passed all 14 with a 15-second budget; all 310 cases passed across these runs. Production build checked; existing Vite chunk-size warning remains. Python was not rerun for this presentation-only change. Push scoped to Alex.
- Publication update: commit/push was rejected by automatic approval review, which considers the earlier no-commit/no-push instruction still active. No commit or push occurred for this cleanup; changes remain local pending explicit publication approval.

## Diagnose all zone estimates and correct misleading errors - 2026-09-13
- Directly posted all 21 parquet IDs to the live endpoint before and after. Exact per-ID status/error bodies are in docs/ZONE_ESTIMATE_DIAGNOSTIC.md. These are 17 load zones, SPP_SYSTEM and three demo aliases. All 21 underlying reader lookups work; no missing columns or IDs were found.
- Root cause: the existing annual-readiness guard rejects the current bundle's 1742 held-out clock hours / 1717 local scored hours against its 8760-hour requirement. The fallback has only four authored locations, so the other 17 incorrectly became 404. Those 17 now return explicit 503 coverage errors identifying the existing location and missing evidence. SPP_SYSTEM and three demos still return 200 with assumption/placeholder exposure. Zero current responses contain real modeled exposure; the requested all-real-200 acceptance remains blocked, not completed.
- Fetched and inspected upstream Kristian through 6c19dc5 and main through 9e8a6a0 without merging. Confidence regeneration 54c7571 changes confidence_score, not location IDs, schema or exposure values. Follow-up evidence validation retains annual readiness. The longer system-only high-demand-proxy experiment is a different target and is not a replacement 21-location artifact. No ready replacement bundle was found locally or on the inspected branches. Asked whether to retain the guard or explicitly serve experimental results with visible limitations; no answer received, so the guard remains intact. No model, training, confidence, economics or artifact files changed.
- Corrected the real-zone caption to 'SPP load zone · zone-specific model data; site exposure is your assumption'. System/demos retain their separate site-data caveat. No API response schema changes. API plumbing/tests and this diagnostic/outbox are the authorized changes outside /web.
- Validation: full Python baseline 1717 passed + 30 subtests; after 1752 passed + 30 subtests (17 existing warnings). Expanded authored annual-ready software fixtures to all 21 IDs and added all 17 real-artifact readiness regressions; fixture success is not evidence that production artifacts pass. Full frontend 310 passed including 12 live checks. Production build passed with the existing chunk-size warning; initial sandbox build failed on directory access, then the authorized rerun passed. No tests deleted.
- Publication scope: this zone diagnostic/error/caption change to Alex only under the latest explicit push instruction. Earlier source-icon cleanup remains local and is excluded from this commit; its existing outbox history is preserved. Main and teammate branches are unchanged.


## Publish Alex checkpoint to main - 2026-09-13
- Changed: merged committed Alex through 6d8fd44 with origin/main 9e8a6a0 in an isolated worktree. Preserved main's national-stack/leaderboard, VPP, comparison and telemetry work alongside Alex's location catalog, grid-impact serving/panel, wind/carbon evidence and zone diagnostics. Retained Ink and Signal typography, colors, brand, quiet source presentation and baseline provenance. The original Alex checkout and its seven uncommitted source-icon cleanup files were not changed or included.
- Merge integration: resolved four frontend conflicts, combined companion endpoint test routing, aligned saved-scenario result typing, and retained main's existing vpp_solar_homes request field in all-zone assertions. No new API contract beyond the interfaces already committed on the two branches. Leaderboard success tests now use explicit authored fixtures instead of requiring an ignored teammate artifact; added an actual missing-manifest 503 regression. No generated rankings or model evidence fabricated.
- Needs review / blocked on: annual-ready zone estimates remain unavailable as documented in ZONE_ESTIMATE_DIAGNOSTIC.md. The national_stack/zone_rankings.json serving artifact is absent in this checkout and the original workspace; its existing endpoint honestly returns 503. Publishing the combined code does not claim those data blockers are solved. Kristian's newly fetched 77015a7 was not merged as part of this Alex-to-main publication.
- Validation: full merged Python suite 1754 passed + 30 subtests, one skipped, 17 existing warnings. Production build passed with the existing large-chunk warning. Initial integration checks caught and resolved old request-field assertions and the ignored leaderboard test dependency. Final frontend result recorded below. Publishing to main is explicitly authorized by this task; no push to teammate branches.
- Final frontend validation: all 316 tests passed across 22 files, including 12 live HTTP checks against the isolated merged API on port 8011. Build passed. Original demo servers were left untouched; the temporary test API is stopped after verification.


## Sync main into local Alex - 2026-09-13
- Changed: fetched origin and fast-forwarded local Alex from 6d8fd44 to main 3e83e48. Restored all seven uncommitted frontend source-icon cleanup files; retained backup stash c90cd4e1e2aefe9f5848348a8253d92fac65fd9a. Resolved the footer conflict by keeping main's panels and the local direct-value hint. Adapted incoming telemetry coordinates to Sourced so the icon-removal change does not hide the displayed coordinate values or their provenance. Local changes remain unstaged and uncommitted; no push performed.
- Interface: no new contract change. Restarted the stale local API on 127.0.0.1:8000 to load main's existing VPP/assumptions schema; live six-field SPP_SYSTEM POST verified. Annual-history/data blockers remain unchanged.
- Validation: 42 affected frontend tests passed; production build passed with existing large-chunk warning. The main merge's full Python/frontend suites were already validated in the preceding publication task. Fetched origin/Alex has newer commits (6707e78); this request synced main only and did not integrate those additional commits.


## Fix VPP sensitivity crash - 2026-09-13
- Changed: sensitivity now uses the existing canonical VPP net-load and arbitrage formulas for its baseline, independent endpoints and break-even denominator, including nonpositive net cost. VPP controls/assumptions remain fixed per bar and appear in provenance. Updated the API economics consistency check to the same existing VPP formula. Retained invariant checks; no stale/wrong sensitivity fallback. Added a ScenarioProvider error boundary with a visible exception message, logging and reset action.
- Verification: real headless Edge against live /app reproduced the original blank-tree RangeError for homes=1 and 500. After the fix, actual field entry for 1, 50, 500 and 10000 retained economics and three updated sensitivity bars with zero browser exceptions. Saved local QA output/screenshots under ignored .tools/browser-qa. Full Python 1755 passed + 30 subtests (17 existing warnings); full frontend 325 passed including 12 live HTTP checks; production build passed with existing chunk-size warning. Initial targeted tests exposed a test-only live leaderboard request continuing after teardown; companion fetches are now isolated in the App fixture. No tests removed.
- Interface: no API response or economics formula changes. Scope is the requested crash fix and regression tests. Older uncommitted source-icon cleanup is preserved separately. Publishing fix to Alex while preserving newer origin/Alex commits; main will receive only these requested fixes after both are verified.


## Ship honest leaderboard serving manifest - 2026-09-13
- Changed: replaced site_rank's seeded invented wind numbers and unsupported 0.45 carbon-offset multiplier with deterministic availability compilation over shipped exposure/grid-impact evidence. Ran python -m pipeline.site_rank; shipping data/processed/national_stack/zone_rankings.json explicitly, despite the processed-directory ignore rule. The generator performs no training, fetching, missing-hour filling or seasonal extrapolation.
- Interface: GET /api/zone-rankings retains operator/composite_weight_formula/description/rankings and adds status (available|unavailable), excluded_locations [{location_id,reasons,source}], and available_wind_evidence [{location_id,reference_location_id,period_start_utc,period_end_exclusive_utc,proxy_hours,evaluable_hours,unknown_hours}]. Each wind scalar is {value,source_type,ref}, preserving its assumption classification from real observation-based screening/timing and pointing to the exact shipped snapshot hash and evidence context. Legacy valid nonempty ranking manifests remain readable. An unavailable manifest must contain reasons and cannot smuggle in ranked scores.
- Actual availability: zero legitimate composite ranks, 21 explicit exclusions (17 load zones, system aggregate and three scenario aliases). Annual exposure remains below its readiness requirement; annual avoided-carbon evidence is absent, and associated wind operating emissions are not avoided emissions. Six real within-zone wind references are supplied separately: CSWS=28, LES=764, OKGE=305, OPPD=492, SPS=468, WFEC=179 screened hours in the supplied 2025 observation period, each with 8687 evaluable and 73 unknown hours. These are screening assumptions over real inputs, not fabricated observations, site deliverability or composite rankings. No annual/zero/carbon substitute is presented.
- Live verification: restarted only this repo's existing API to load the additive response schema. Real Edge against /app confirmed HTTP 200, six rendered evidence rows, all 21 visible exclusion explanations and zero browser exceptions. Screenshot/report retained in ignored .tools/browser-qa. The original missing-manifest 503 is resolved; an actual composite ranking remains blocked on legitimate annual exposure and avoided-carbon inputs.
- Validation: full Python after this fix 1758 passed + 30 subtests (17 existing warnings). Regression tests require the actual shipped file, assert live endpoint 200, compare each count to original validated snapshots, reproduce the file offline, and reject fabricated ranks in an unavailable manifest. Frontend tests exercise the actual shipped JSON and source inspection. Final full frontend/build result recorded below. Scope outside /web is solely the requested generator/manifest, API schema, tests and this outbox. Protected model training/calibration/label files unchanged.
- Final validation: all 326 frontend tests passed, including 12 live HTTP checks; TypeScript and production build passed. The second bug is fixed as an out-of-box HTTP 200 evidence/availability view, not a claim that unsupported composite ranks became valid.



## Scoped Tharun integration with Alex safeguards — 2026-09-13
- Changed: reconciled location search from fbafd4d (API client/fixture, hook/tests, LocationEstimate/tests, LocationField/tests and necessary App/ScenarioContext/legacy-ledger wiring). Added the search/sort/horizontal-chart interaction from Tharun's leaderboard to Alex's honest availability UI. Ranked risk sorts by actual p90 hours ascending, not an ambiguously oriented score; unsupported annual risk/carbon/composite sorting never invents numbers. Chart mode displays sourced observed wind-screening hours when composite ranks are unavailable; the full exclusions list remains visible and unfiltered. Ranking API/manifest/generator contract is unchanged from Alex b3dea75.
- Preserved: sensitivity.ts and ScenarioErrorBoundary.tsx are byte-identical to b3dea75, with the same error-boundary wrapper in App. Portfolio API, alerts and their tests are unchanged. The sole PortfolioPanel reconciliation prevents a city query from silently saving the previous zone ID; existing zone save/compare/threshold flows remain available, and city results use Tharun's existing comparison ledger.
- Provisional decision: ported 70996f2's explicit FLUXLINE_ALLOW_PROVISIONAL=1 provider option, requiring existing valid provenance/schema, retaining the annual-readiness reason, assumption sources and Low confidence. Avoided the incoming direct timestamp reparse so incomplete coverage metadata cannot produce an uncaught exception. Launcher remains default OFF/unset, with a regression forbidding auto-setting the variable. Without explicit opt-in all 17 under-evidenced load zones retain 503/unavailable. Opted-in values cannot become real evidence or clear portfolio thresholds. /api/estimate response shape and economics formulas are unchanged.
- Deliberately excluded: no new BQML/Vertex/cloud/research/capacity/outage code or research documents were imported into the scoped b3dea75 baseline. Existing Alex capacity/outage files were retained. Wind_signal.py and carbon.py are identical between the inspected tips and were left unchanged; no new grid-impact data/mappings were adopted. Six supported references remain CSWS, LES, OKGE, OPPD, SPS, WFEC; composite ranking remains unavailable.
- Remote-state finding: fetch found every remote branch repointed to 0745810 (parents fbafd4d and 46e84be), including forced Alex/Kristian updates. That tree had removed the portfolio and replaced the honest ranking implementation, while enabling provisional mode in the launcher. The scoped feature was prepared from the requested b3dea75 baseline. Publication will apply only these reviewed paths plus restoration of the protected portfolio/ranking files to the new remote tip, preserving unrelated work already present there. No force push and no push to Kristian/Tharun; main only after final validation.
- Validation: scoped full Python 2024 passed + 42 subtests, one existing skip, two deprecation warnings; frontend 341 passed including live contracts; production build passed with existing chunk-size warning. Added all-zone default-off/explicit-opt-in/portfolio exclusion regressions, city-save isolation and leaderboard interaction tests. Real Edge on the local integration verified VPP=500 with three sensitivity bars and no crash; wind sort starts LES=764, OKGE search yields one row, chart retains all 21 exclusion reasons; Lincoln search returns 20 real Open-Meteo/GeoNames matches with Lincoln, Nebraska first; LES/EDE portfolio and 200-hour threshold survive reload; default LES POST stays 503 with its annual-readiness explanation. No browser exceptions. The city lookup was cached offline using the existing location reader; no model training, seasonal extrapolation or cloud job was started. Full city exposure reports remain dependent on the existing local job runner/cached reports. QA reports/screenshots are in ignored .tools/browser-qa/merge-*.

## Final publication reconciliation ? 2026-09-13
- Applied the reviewed scope to remote tip 0745810 without removing or importing additional research work. Existing cloud/research/capacity/outage files on that tip remain byte-identical; protected confidence/train/label/simulate/wind/carbon files also have no publication diff. Restored portfolio API/UI/tests, the honest site_rank generator and zone_ranking validation from Alex; the shipped zero-rank/six-reference manifest remains intact. Restored Alex's catalog fixture after the incoming hardcoded confidence values caused a provenance mismatch; no validation weakened.
- Final exact publication tree: 2008 Python tests + 42 subtests passed, one existing skip, two deprecation warnings; all 341 frontend tests passed; production build passed with the existing chunk-size warning. This tree retains the current remote's ML/test history (hence its different count from the isolated b3dea75 baseline); this task did not remove ML tests.
- Repeated every live acceptance check in real Edge against this exact tree: VPP=500 keeps three sensitivity bars and no crash; LES is first under wind sorting with 764 sourced hours; OKGE search filters to one row; chart view keeps all 21 exclusion explanations; real Lincoln city search returns 20 matches; LES/EDE portfolio and its saved threshold persist across reload; default LES estimate is 503/unavailable; zero browser exceptions. No city exposure report was fabricated or model job run. City estimation beyond lookup requires the existing cached reports/local job runner.
- Publish Alex first, then merge the verified tree to main as explicitly authorized in this task. Record the manually reconciled scoped branch as a parent without changing tested content, so the prior Alex history remains reachable. No forced updates and no pushes to teammate branches. Original seven uncommitted local source-icon cleanup files remain excluded and will be preserved when syncing the local Alex checkout.

## Top workspace tabs and city-search startup diagnosis - 2026-09-13
- Changed: added four flat Ink and Signal top tabs below the unchanged header: Overview (default), Scenario Stress Test, Zone Analytics, Portfolio & Alerts. No router/dependency or API/data/economics contract change. Shared ScenarioProvider stays mounted; only active tab content mounts. Existing lazy Surface behavior is retained. Portfolio moved out of ScenarioComparison into its own tab; only its spatial instructions changed to name Scenario Stress Test. Scenario inputs, VPP and saved ledger survive switching; server portfolio/thresholds restore on remount. Tab-local disclosure/chart/draft controls reset on remount by design.
- Error boundary: retained the existing global ScenarioErrorBoundary around ScenarioProvider. Unexpected errors still show the existing message/reset instead of a blank page; the tradeoff is one global recovery screen rather than independent per-tab recovery. Added tab visibility/default/CTA/keyboard/URL/state-retention regressions and updated existing App tests to explicitly open Scenario. No component behavior tests removed.
- City-search diagnosis: reproduced POST /api/location-estimator/search for Wichita, KS returning 503 while port 8765 was absent. Running powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/start-fluxline.ps1 -NoBrowser started the missing workbench; the same actual search then completed successfully. Open Fluxline.cmd already invokes this script, which starts scripts/start-ml-workspace.ps1 (python -u -m pipeline.workbench on 127.0.0.1:8765), uvicorn api.main:app on 127.0.0.1:8000 and Vite on 127.0.0.1:5174. No launcher/search algorithm change was needed. Running only uvicorn and Vite misses a required process.
- Error messages now distinguish an unreachable/unresponsive workbench, a missing workspace token (wrong service/response on port 8765), and malformed job JSON. Existing busy/conflict status remains unchanged. Workbench errors now explicitly carry Cache-Control: no-store. Seven added API regression cases cover connection refusal/timeouts/token/malformed response/busy behavior; the existing real loopback workbench/token contract test remains.
- Explicit live result: city SEARCH/geocoding works end-to-end against the correctly started stack: YES. Job 20260914T051909_7b0e2f0a succeeded and produced Wichita, Kansas at 37.69224, -97.33754 from actual Open-Meteo/GeoNames results, cached in ignored data/processed/workbench/sites. This is not a completed exposure estimate. The subsequent real UI estimate started offline job 20260914T052351_aea0d43d, which prepared cached weather/generation inputs and entered regional cross-area model validation. It is still processing at this checkpoint; no claim of a completed report, no invented hours, no bypass of model checks. Uncached reports require offline preparation before a network-free demo. No ML source files were modified.
- Live UI verification: real Edge, /app, all four tabs, VPP=500 with three sensitivity bars/no crash; scenario input and ledger retention; leaderboard wind sort starts LES, OKGE search yields one row, chart keeps all 21 exclusion explanations; LES/EDE portfolio save/compare and 200-hour LES threshold persist across tab unmount/remount (honestly unavailable against unsupported annual evidence). Zero browser exceptions. Full-page screenshots saved under ignored .tools/browser-qa/tabs-{overview,scenario,zones,portfolio}.png, with raw result in tabs-live.json. Final suite/publication results follow below.
- Scope/preservation: API error diagnostics and tests are the only backend edits; role overlap logged here. Existing source-icon cleanup was safely stashed separately before fast-forwarding Alex to latest main 86af78a; it is excluded from this publication and will be restored locally afterward. Protected model, economics, provenance, ranking and confidence code/contracts unchanged.
- Final city result: YES, both actual city search and the subsequent exposure/economics display now work end-to-end locally. Offline job 20260914T052351_aea0d43d succeeded after preparing inputs and fitting/validating the existing regional model (no pipeline code changes). Repeated live UI search returned cached HTTP 200/succeeded for search and estimate, model spp_regional_3a0270f637be071133b8, historical_spp_match, Low confidence. Regional stationary expected exposure is 631.0056989752204 h/year; at the tested user exposure=0.4, displayed expected site exposure is 252.40227959008817 h/year. These remain expected high-demand-proxy comparisons, not annual tail quantiles or actual site cutoffs. The completed report/model/raw caches are local ignored artifacts, not shipped by this UI/diagnostic commit; other machines need their own completed preparation. Canonical /api/estimate annual-readiness protections are unchanged.
- Repeated all live tab/VPP/leaderboard/portfolio checks against the restarted API and full packaged stack with zero browser exceptions. Screenshots refreshed in .tools/browser-qa/tabs-*.png and final raw report in tabs-live.json. Packaged launcher confirmed all services ready after restart. First-run errors/tests were corrected without removing assertions: restored the original footer hint, isolated browser storage between tests, supplied explicit no-store failure headers, and gave full-tab asynchronous DOM assertions five seconds on a CPU-busy workstation (no production timeout changes).
- Final validation: full Python 2016 passed plus 42 subtests (two existing deprecation warnings); full frontend 343 passed across 26 files, including 12 live HTTP contract checks; TypeScript/production build passed with the existing large-chunk warning. Logs: .tools/tabs-final-python.log, tabs-verified-frontend.log, tabs-build.log, tabs-final-browser.log. Publishing the tested scoped change to Alex first, then fast-forwarding main. Local source-icon cleanup remains separate.

## Overview interactive preview and requested copy cleanup - 2026-09-13
- Changed: replaced the thin Overview with a responsive two-column introduction/slider, an honesty section, concise model/surface/grid-impact explanations, and links into Scenario Stress Test and Zone Analytics. Reused Ink and Signal tokens, existing range styling and Sourced number easing; no dependencies. Only Overview-specific styles were added. Shared scenario/portfolio/leaderboard behavior and all backend/model/economics contracts remain unchanged.
- Slider choice: option (a), local React state with no ScenarioContext access or fetching. The standalone preview displays 100 authored reference hours multiplied by its local factor, each value carrying an assumption source at mock://overview/interaction. Its header says Interactive preview; the source explicitly identifies authored non-pipeline values. The original scenario factor remains unchanged when dragging or navigating. Removed the fictional crossover and extra captions per the user's subsequent request for a simpler preview.
- Evidence: wind-evidence count is computed from distinct location_id values in the actual imported zone_rankings manifest (currently six), and its availability copy follows manifest.status. National architecture copy is deliberately qualified as research using synthetic proxies: reading pipeline/national_stacking.py confirms six configured regions but does not support claiming validated national coverage. No ML files modified or jobs started.
- User-directed copy removals: removed Overview's NO LIVE GRID FETCHES footer label, the general source-info footer hint, the closing portfolio paragraphs/CTA, and the preview disclaimer paragraph. Removed visible illustrative/sample labels; retained the concise Interactive preview heading and exact assumption metadata. Also removed the requested visible wind-evidence table caption, preserving an accessible Wind evidence table name, and changed the leaderboard's demo display suffix to Scenario. These two label-only Zone Analytics changes were explicitly requested after the initial Overview-only brief; no ranking logic changed. Existing internal provenance references/contracts are preserved.
- Live verification: real Edge at 1440px and 390px; pointer drag and Home/End update preview values, zero API calls during interaction, zero browser exceptions, unchanged real scenario factor=0.4, preview resets to local 0.4 on remount. Both navigation destinations, all four tabs, and VPP=500 still work. No mobile horizontal page overflow. Final screenshots .tools/browser-qa/overview-desktop.png and overview-mobile.png; raw checks overview-live.json. Initial screenshot harness needed the actual uppercase header selector; production behavior was unaffected.
- Preservation: started Alex at latest main ef01c03; older seven-file source-icon cleanup remains in a backup stash while publishing only this task. The newly requested footer removal supersedes that stash's older footer-hint wording. Push Alex only; main/Kristian/Tharun must stay untouched.
- Final validation: full Python 2016 passed plus 42 subtests (two existing deprecation warnings); full frontend 345 passed across 27 files, including live HTTP contracts; production build passed with existing large-chunk warning. Two new Overview tests and expanded App/leaderboard assertions verify isolated preview arithmetic/provenance, no fetches, navigation, actual manifest count and requested copy removals. Updated the old leaderboard-label expectation to Scenario; no tests deleted. Logs: .tools/overview-python.log, overview-verified-frontend.log, overview-approved-build.log and overview-browser.log.
