# Alex lane outbox

## Overnight sync and wind opportunity screen - 2026-09-13 07:55 UTC
- Synced the four overnight prompt/plan files to main at `5a35024`; aligned existing Alex by fast-forward, preserving all previous work. All subsequent lane pushes target Alex only.
- Changed: `pipeline/wind_signal.py` and its tests. The rule is same-footprint system wind/load >= 0.50 AND exact local LMP <= 0 USD/MWh. Both thresholds are explicit assumptions; the result is a high-wind/low-price opportunity proxy, never measured recoverable wind or local headroom.
- Interface: `wind_oversupply_hours(hourly, sources=..., wind_scope=..., load_scope=..., policy=...)` consumes UTC-hour rows with `location_id`, `system_wind_mw`, `system_load_mw`, `lmp_usd_mwh`. Sources map each numeric input column to `{source_type, ref}`. It returns a nullable `wind_oversupply_proxy` column and source/method attributes.
- Interface: `summarize_wind(..., flexible_load_mw={value,source_type,ref}, available_fraction={value,source_type,ref})` returns per-location records with sourced observed/evaluable/unknown/missing-interval/proxy hours and wind-absorption MWh scenarios. Annual MWh is null without one complete evaluable calendar year; all-unknown opportunity and energy are null. No site_exposure factor or zone-level wind allocation is invented.
- Verified: initial full baseline 299 passed + 30 subtests; initial wind pass 327 passed + 30 subtests, with only the 17 pre-existing dependency/event warnings. Production build passed (existing chunk-size warning). The diagnostic suite's 157 errors came from inaccessible stale Windows pytest temp directories; a fresh explicit `--basetemp` resolves them without code changes.
- Needs integration: there are currently no cached fuel-mix, generation, LMP or constraint observations locally. Next checkpoint will add a strict adapter around existing cache readers. Current gridstatus fuel-mix aggregation may combine SPP and SWPW; never silently combine it with historical SPP-only load or divide system wind by zone load.
- Research: SPP's price explanation https://spp.org/markets-operations/ and 2024 market report https://www.spp.org/documents/73953/2024_annual_state_of_the_market_report.pdf support the distinction between a location's price/congestion and actual wind-curtailment availability. Reviewed 2026-09-13. A generic constraint count alone cannot establish relief from additional load.

## Existing SPP cache adapters and actual archive check - 2026-09-13 08:20 UTC
- Changed: wind preparation now calls existing `ingest.load_dataset` readers and `generation.normalize_generation`; no ingestion code or new EIA fetch was added. Exact price market and output-ID-to-price-node mapping are mandatory. Complete five-minute/hourly interval coverage is required; overlapping conflicting revisions fail instead of being averaged.
- Interface: `prepare_wind_inputs(generation, system_load, prices, price_locations=..., market=..., generation_format="gridstatus"|"historical")`; `read_cached_wind_inputs(system_load_path, price_locations=..., market=...)`; `read_cached_historical_wind_inputs(system_load_path, year=2024, price_locations=..., market=...)`. Readers do not fetch; missing caches raise. `read_cached_generation_archive(year)` retains every raw fuel column and checks URL/vintage/content fingerprint.
- Actual data: reused existing `fetch_public_evidence` to cache SPP `GenMix_2024.csv` (105,408 five-minute rows), SHA256 `39e52ba5d3b974f99790af2505769207b64e31fe713291d26f2c470945c0a4b0`. URL: https://portal.spp.org/file-browser-api/download/generation-mix-historical?path=/GenMix_2024.csv . This is an ignored runtime cache, not a synthetic artifact.
- Real reader check: the existing normalizer plus 8,784-row 2024 load table yields 8,713 known wind/load hours and 71 missing hours. Of the known hours, 2,966 meet the ASSUMED 0.50 wind-share threshold. These are not classified absorption opportunities because local LMP is unavailable. Existing normalization treats GMT MKT Interval as observation time in left-closed hourly bins; that timing convention remains explicit.
- Exact pull failures: existing `get_fuel_mix` rejected 2024-01-01 because it serves only the last 365 days; `get_lmp_day_ahead_hourly` requested `.../da-lmp-by-settlement-location?path=/2024/01/By_Day/DA-LMP-SL-202401010100.csv` and received HTTP 404. No false zero-price substitute was created.
- Verified: full Python suite reached 474 passed + 30 subtests with the separate carbon/API work in progress; 17 existing warnings. Earlier production build passed. The original frontend, API endpoint contract and Kristian's model files remain untouched by lane implementation.

## Signed operational-carbon accounting and verified EPA factors - 2026-09-13 08:20 UTC
- Changed: `pipeline/carbon.py` and 93 focused tests. `fuel_mix_intensity(frame, factors, expected_fuels=..., application_source=...)` requires a declared complete fuel universe and long-form UTC-hour `{timestamp_utc,fuel,generation_mwh,source_type,ref}` rows. Missing fuel rows or positive-generation factors produce null intensity, never renormalization or an invented zero.
- Interface: factors are `{value,source_type,ref,unit:"kgCO2/MWh",boundary:"direct_operational_co2"}`. `shift_carbon(intensities,moves,selection_source=...,hourly_limits=...)` uses explicit later makeup-hour pairs with equal removed/made-up MWh and shared hourly capacity limits. Signed carbon is `MWh * (risk-hour intensity - makeup-hour intensity)`. Dirtier makeup stays negative; absent observations/schedules are not fabricated zero-energy pairs.
- Interface: `wind_carbon(absorbed_mwh,selection_source=...)` reports electricity absorbed plus associated direct operational CO2. EPA's wind operational factor is zero; this is neither atmospheric removal nor a displaced-generation benefit. No lifecycle or marginal-dispatch claim is made.
- Real source: https://www.epa.gov/system/files/documents/2025-06/egrid2023_data_metric_rev2.xlsx , retrieved 2026-09-13T08:06:02Z, SHA256 `3dfbbcf2f949d58d5b2dbee3aab8150bd04a0c8ebb730ba1cd37a013bd4450ab`. `load_egrid_swpp_factors()` verifies the offline cached bytes, revision URL, SWPP/2023 row and kg/MWh labels before reading factors; it never downloads or runs inside the API.
- Verified SWPP factors: Coal `1024.2995616`, Natural Gas `488.030508`, Oil `1300.920264` kgCO2/MWh (BA23: BACCO2RT, BAGCO2RT, BAOCO2RT). Wind/Solar/Hydro/Nuclear operational zeros cite the eGRID2023 Technical Guide, printed page 10. Applying 2023 BA averages to 2024 hourly generation still requires an ASSUMPTION application source.
- Unresolved: raw SPP fuel labels include Diesel Fuel Oil, Waste Disposal Services, Waste Heat and Other. The loader does not silently map those to Oil or zero. Supplied-factor coverage remains explicit; complete hourly carbon intensity cannot be claimed until those choices are sourced or explicitly assumed.
- Verified: carbon 93/93; full suite 474 passed + 30 subtests, 17 pre-existing warnings. Reordered rows/pairs serialize identically; placeholder refs cannot be upgraded; shared capacity handles binary floating-point roundoff without allowing material overscheduling. Standalone API composition is under a separate provenance-size audit before its commit.

## Standalone grid impact and integration audit - 2026-09-13 08:30 UTC
- Changed: `api/grid_impact.py` composes existing wind summaries and conserved carbon-shift pairs without registering a route or changing `/api/estimate`. The wind adapter now rejects invalid subhourly net wind before averaging and preserves exact assumption precision in refs.
- Output contract: `schema_version="grid-impact-v1"`, `location_id`, `boundary`, `units`, `basis`, `coverage`, `evidence_context`, `evidence`, and six sourced scalars: `wind_absorption_mwh_in_observed_hours`, `wind_absorption_mwh_per_year`, `carbon_absorbed_tonnes_in_observed_hours`, `carbon_absorbed_tonnes_per_year`, `carbon_shifted_tonnes_in_observed_hours`, `carbon_shifted_tonnes_per_year`.
- Provenance: scalar refs are `grid-impact://evidence/<sha256>` pointers into the response's `evidence` dictionary. Its content-addressed nodes preserve original input values, source kinds, refs, factors, units, accounting boundaries and explicit time pairs. Resolve these locally; refs never trigger a network/file request. Missing inputs stay null assumptions. Annual results require a complete evaluable UTC calendar year and complete-period schedule.
- Reader: `get_location_grid_impact(location_id, path=...)` accepts an offline `grid-impact-inputs-v1` JSON envelope with `locations:[{location_id,wind_summary,carbon_shift,shift_coverage}]`. It checks the entire envelope/unique IDs but composes only the selected payload. Malformed selected records raise; absent files/locations return sourced unavailable values. Carbon formulas belong to `pipeline/carbon.py`; the adapter additionally checks pair conservation, impossible zero-energy carbon, and total consistency.
- Performance: 365-pair JSON fell from 6.02 MB to 0.97 MB; annual refs are 87 characters. A 21-location cold read fell from 1.90 s to 0.41 s. A full-year raw-input composition remains too slow for demo-time use; precompiled per-location snapshots are the next step. Do not wire heavy raw composition into a request handler.
- Verified: full Python suite 500 passed + 30 subtests, 17 existing warnings; frontend 256 unit tests plus all 8 explicitly enabled live API tests passed; 9 estimator behavior checks passed; production build passed with its existing chunk-size warning. No frontend or canonical API files changed in this checkpoint.
- Needs urgent review outside this lane: new `origin/main` commit `dcbbf5a` adds `pipeline/national_stacking.py`, which aliases SPP rows to synthetic national regions, adds unseeded noise, fills missing targets with zero and fits its parent on test targets; the emitted model card claims production national coverage and 10,000 trials without those trials being implemented. These files were inspected read-only, not merged or used by Alex's wind/carbon work.
- Data progress: official SPP annual VER curtailment archive is now cached using the existing downloader, SHA256 `291fb1c1d7ac13d99c2ba7e55296106217cac6f84ba2db5d1b6fb4e75e432b9d`, URL `https://portal.spp.org/file-browser-api/download/ver-curtailments?path=/2024/2024.zip`. The earlier DA daily 404 was an archive rollup: the official listing exposes `/2024/2024.zip`; its first cached download is in progress. These archives need unit, interval and missing-data checks before model use.

## Full fuel-mix completeness audit - 2026-09-13 08:35 UTC
- Changed: `normalize_spp_generation_archive(raw, generation_source=..., timing_source=...)` in `pipeline/carbon.py` preserves all ten historical fuel families and additional named Market/Self pairs. Only the archive's `Gas Self` spelling aliases `Natural Gas Self`; no fuel emission factor is inferred from its name.
- Interface: output columns are `timestamp_utc`, `fuel`, `generation_mwh`, `unit="MWh"`, `generation_status`, `source_type`, `ref`; `attrs.expected_fuels` supplies the full fuel universe to `fuel_mix_intensity`. Explicit missing/negative-net statuses carry null generation, including after parquet round trips. Unmarked NaN, boolean/infinite observations, conflicting duplicates and malformed cadence are rejected.
- Honesty: twelve distinct complete five-minute observations are required per fuel-hour. Signed Market/Self components may offset within a sample; a negative combined sample makes that fuel-hour unknown rather than clamping it to zero. Observation-time hourly binning remains an explicit assumption, so these derived hourly records remain assumptions.
- Actual cached-year check: 87,850 fuel-hour rows across 8,785 UTC bins normalized in 4.2 seconds; 86,542 complete, 707 incomplete-observation and 601 negative-net Solar rows. The provider's annual file extends from 2024-01-01 06:05 UTC into 2025-01-01 06:00 UTC; it is not a complete UTC calendar year by itself.
- Actual carbon result: no numeric intensity can yet be justified. Missing generation affects 669 hours; the remaining 8,116 hours lack applicable positive-generation factors, involving Diesel Fuel Oil, Other and Waste Disposal Services. Waste Heat is retained too. No zero-factor substitution, shifted-carbon scenario or site claim was fabricated.
- Verified: 115 carbon tests passed; full Python suite 522 passed + 30 subtests, 17 existing warnings. Full-year intensity preparation takes about 43 seconds and must stay offline. Tharun should serve only prepared outputs and preserve null status until the remaining fuel/accounting assumptions are explicitly reviewed.

## Immutable grid-impact snapshots and accounting convention - 2026-09-13 08:52 UTC
- Changed: `compile_grid_impact_snapshot(location_id,path,wind_summary=...,carbon_shift=...,shift_coverage=...)` performs heavy composition offline and atomically publishes a new file without replacing an existing/racing destination. Envelope: `{snapshot_version:"grid-impact-snapshot-v1",location_id,result_sha256,result}`; `result` retains the six sourced scalars and complete evidence graph.
- Serving interface: `read_grid_impact_snapshot(location_id,path,cache=GridImpactSnapshotCache(...))` validates the snapshot without composition/model calls. The caller-owned cache is bounded by entries and serialized bytes, hashes fresh file content on every read, and returns isolated copies. Missing files remain unavailable; corruption, wrong locations, source upgrades, unreachable/dangling evidence and inconsistent coverage raise. Digests establish integrity, not source authenticity.
- Synthetic benchmark: 365 pairs produced a 754,540-byte snapshot, 0.349-second cold read and 0.103-second warm read. 8,760 pairs produced 17,963,912 bytes, 3.423-second cold and 0.559-second warm reads. Prewarm large snapshots at application startup; do not run offline composition in a request. No route or canonical API contract was changed.
- Scientific correction: EPA eGRID rates exclude biogenic CO2, allocate CHP emissions to electricity, and group plants by primary fuel. Loader refs and the standalone carbon basis now say so explicitly; these are attributed electricity-output rates, not all physical stack CO2. Coarse boundary tag and numeric factors are unchanged. See eGRID2023 Technical Guide sections 3.1.2.1, 3.1.2.2 and 3.1.3.3.
- Crosswalk warning: EPA `WDS` means wood/wood-waste solids, not SPP Waste Disposal Services. EPA groups diesel/DFO under Oil, but applying the broad SWPP Oil rate to SPP diesel still needs an explicit representativeness assumption. No unsupported waste/Other factors were added.
- Verified: 112 grid-impact and 116 carbon tests pass; full repository-root Python suite passed 682 tests + 30 subtests with 17 existing warnings, including the new wind-archive regressions in progress. Price/VER archive readers and their tests are the next wind checkpoint; the requested narrow frontend popup/text cleanup is being handled separately without data or economics changes.

## Requested source-popup and wording cleanup - 2026-09-13 09:03 UTC
- Scope exception: the user's new request explicitly authorized a narrow `/web` edit during Alex's lane; the main wind/backend task continues. Changed only App, Sourced, ConfidenceBadge and five affected test files.
- Removed the oversized raw-JSON source panel, its pin-hint footer and automatic hover/focus opening. Intentional click or keyboard activation shows compact source details. Exact `{value,source_type,ref}` records and full confidence descriptions remain in control metadata and the scenario export.
- Removed the crossover's redundant Assumed tag and `under these assumptions` text; the decision eyebrow now reads `DECISION`. Permanent site-exposure/user-assumption and coverage framing remain. No calculations, API shapes, source objects or styling files changed.
- Verified: frontend test cases increased from 264 to 267 by splitting two exhaustive interaction cases into independent cases, retaining every assertion and the existing timeout bounds. All 267 pass, including eight live FastAPI checks at `127.0.0.1:8000`; production build and typecheck pass. The existing bundle-size warning remains.
- Visual verification limitation: browser tooling returned `No browser is available`. This checkpoint was verified through source review and interaction tests, not a claimed browser screenshot inspection.
