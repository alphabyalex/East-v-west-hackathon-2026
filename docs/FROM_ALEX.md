# Alex lane outbox

## Overnight sync and wind opportunity screen - 2026-09-13 07:55 UTC
- Synced the four overnight prompt/plan files to main at `5a35024`; aligned existing Alex by fast-forward, preserving all previous work. All subsequent lane pushes target Alex only.
- Changed: `pipeline/wind_signal.py` and its tests. The rule is same-footprint system wind/load >= 0.50 AND exact local LMP <= 0 USD/MWh. Both thresholds are explicit assumptions; the result is a high-wind/low-price opportunity proxy, never measured recoverable wind or local headroom.
- Interface: `wind_oversupply_hours(hourly, sources=..., wind_scope=..., load_scope=..., policy=...)` consumes UTC-hour rows with `location_id`, `system_wind_mw`, `system_load_mw`, `lmp_usd_mwh`. Sources map each numeric input column to `{source_type, ref}`. It returns a nullable `wind_oversupply_proxy` column and source/method attributes.
- Interface: `summarize_wind(..., flexible_load_mw={value,source_type,ref}, available_fraction={value,source_type,ref})` returns per-location records with sourced observed/evaluable/unknown/missing-interval/proxy hours and wind-absorption MWh scenarios. Annual MWh is null without one complete evaluable calendar year; all-unknown opportunity and energy are null. No site_exposure factor or zone-level wind allocation is invented.
- Verified: initial full baseline 299 passed + 30 subtests; initial wind pass 327 passed + 30 subtests, with only the 17 pre-existing dependency/event warnings. Production build passed (existing chunk-size warning). The diagnostic suite's 157 errors came from inaccessible stale Windows pytest temp directories; a fresh explicit `--basetemp` resolves them without code changes.
- Needs integration: there are currently no cached fuel-mix, generation, LMP or constraint observations locally. Next checkpoint will add a strict adapter around existing cache readers. Current gridstatus fuel-mix aggregation may combine SPP and SWPW; never silently combine it with historical SPP-only load or divide system wind by zone load.
- Research: SPP's price explanation https://spp.org/markets-operations/ and 2024 market report https://www.spp.org/documents/73953/2024_annual_state_of_the_market_report.pdf support the distinction between a location's price/congestion and actual wind-curtailment availability. Reviewed 2026-09-13. A generic constraint count alone cannot establish relief from additional load.

## Existing SPP cache adapters and actual archive check - 2026-09-13 08:20 UTC
- Changed: wind preparation now calls existing `ingest.load_dataset` readers and `generation.normalize_generation`; no ingestion code or new EIA fetch was added. Exact price market and output-ID-to-price-node mapping are mandatory. Complete five-minute/hourly interval coverage is required; overlapping conflicting revisions fail instead of being averaged.
- Interface: `prepare_wind_inputs(generation, system_load, prices, price_locations=..., market=..., generation_format="gridstatus"|"historical")`; `read_cached_wind_inputs(system_load_path, price_locations=..., market=...)`; `read_cached_historical_wind_inputs(system_load_path, year=2024, price_locations=..., market=...)`. Readers do not fetch; missing caches raise. `read_cached_generation_archive(year)` retains every raw fuel column and checks URL/vintage/content fingerprint.
- Actual data: reused existing `fetch_public_evidence` to cache SPP `GenMix_2024.csv` (105,408 five-minute rows), SHA256 `39e52ba5d3b974f99790af2505769207b64e31fe713291d26f2c470945c0a4b0`. URL: https://portal.spp.org/file-browser-api/download/generation-mix-historical?path=/GenMix_2024.csv . This is an ignored runtime cache, not a synthetic artifact.
- Real reader check: the existing normalizer plus 8,784-row 2024 load table yields 8,713 known wind/load hours and 71 missing hours. Of the known hours, 2,966 meet the ASSUMED 0.50 wind-share threshold. These are not classified absorption opportunities because local LMP is unavailable. Existing normalization treats GMT MKT Interval as observation time in left-closed hourly bins; that timing convention remains explicit.
- Exact pull failures: existing `get_fuel_mix` rejected 2024-01-01 because it serves only the last 365 days; `get_lmp_day_ahead_hourly` requested `.../da-lmp-by-settlement-location?path=/2024/01/By_Day/DA-LMP-SL-202401010100.csv` and received HTTP 404. No false zero-price substitute was created.
- Verified: full Python suite reached 474 passed + 30 subtests with the separate carbon/API work in progress; 17 existing warnings. Earlier production build passed. The original frontend, API endpoint contract and Kristian's model files remain untouched by lane implementation.

## Signed operational-carbon accounting and verified EPA factors - 2026-09-13 08:20 UTC
- Changed: `pipeline/carbon.py` and 93 focused tests. `fuel_mix_intensity(frame, factors, expected_fuels=..., application_source=...)` requires a declared complete fuel universe and long-form UTC-hour `{timestamp_utc,fuel,generation_mwh,source_type,ref}` rows. Missing fuel rows or positive-generation factors produce null intensity, never renormalization or an invented zero.
- Interface: factors are `{value,source_type,ref,unit:"kgCO2/MWh",boundary:"direct_operational_co2"}`. `shift_carbon(intensities,moves,selection_source=...,hourly_limits=...)` uses explicit later makeup-hour pairs with equal removed/made-up MWh and shared hourly capacity limits. Signed carbon is `MWh * (risk-hour intensity - makeup-hour intensity)`. Dirtier makeup stays negative; absent observations/schedules are not fabricated zero-energy pairs.
- Interface: `wind_carbon(absorbed_mwh,selection_source=...)` reports electricity absorbed plus associated direct operational CO2. EPA's wind operational factor is zero; this is neither atmospheric removal nor a displaced-generation benefit. No lifecycle or marginal-dispatch claim is made.
- Real source: https://www.epa.gov/system/files/documents/2025-06/egrid2023_data_metric_rev2.xlsx , retrieved 2026-09-13T08:06:02Z, SHA256 `3dfbbcf2f949d58d5b2dbee3aab8150bd04a0c8ebb730ba1cd37a013bd4450ab`. `load_egrid_swpp_factors()` verifies the offline cached bytes, revision URL, SWPP/2023 row and kg/MWh labels before reading factors; it never downloads or runs inside the API.
- Verified SWPP factors: Coal `1024.2995616`, Natural Gas `488.030508`, Oil `1300.920264` kgCO2/MWh (BA23: BACCO2RT, BAGCO2RT, BAOCO2RT). Wind/Solar/Hydro/Nuclear operational zeros cite the eGRID2023 Technical Guide, printed page 10. Applying 2023 BA averages to 2024 hourly generation still requires an ASSUMPTION application source.
- Unresolved: raw SPP fuel labels include Diesel Fuel Oil, Waste Disposal Services, Waste Heat and Other. The loader does not silently map those to Oil or zero. Supplied-factor coverage remains explicit; complete hourly carbon intensity cannot be claimed until those choices are sourced or explicitly assumed.
- Verified: carbon 93/93; full suite 474 passed + 30 subtests, 17 pre-existing warnings. Reordered rows/pairs serialize identically; placeholder refs cannot be upgraded; shared capacity handles binary floating-point roundoff without allowing material overscheduling. Standalone API composition is under a separate provenance-size audit before its commit.

## Standalone grid impact and integration audit - 2026-09-13 08:30 UTC
- Changed: `api/grid_impact.py` composes existing wind summaries and conserved carbon-shift pairs without registering a route or changing `/api/estimate`. The wind adapter now rejects invalid subhourly net wind before averaging and preserves exact assumption precision in refs.
- Output contract: `schema_version="grid-impact-v1"`, `location_id`, `boundary`, `units`, `basis`, `coverage`, `evidence_context`, `evidence`, and six sourced scalars: `wind_absorption_mwh_in_observed_hours`, `wind_absorption_mwh_per_year`, `carbon_absorbed_tonnes_in_observed_hours`, `carbon_absorbed_tonnes_per_year`, `carbon_shifted_tonnes_in_observed_hours`, `carbon_shifted_tonnes_per_year`.
- Provenance: scalar refs are `grid-impact://evidence/<sha256>` pointers into the response's `evidence` dictionary. Its content-addressed nodes preserve original input values, source kinds, refs, factors, units, accounting boundaries and explicit time pairs. Resolve these locally; refs never trigger a network/file request. Missing inputs stay null assumptions. Annual results require a complete evaluable UTC calendar year and complete-period schedule.
- Reader: `get_location_grid_impact(location_id, path=...)` accepts an offline `grid-impact-inputs-v1` JSON envelope with `locations:[{location_id,wind_summary,carbon_shift,shift_coverage}]`. It checks the entire envelope/unique IDs but composes only the selected payload. Malformed selected records raise; absent files/locations return sourced unavailable values. Carbon formulas belong to `pipeline/carbon.py`; the adapter additionally checks pair conservation, impossible zero-energy carbon, and total consistency.
- Performance: 365-pair JSON fell from 6.02 MB to 0.97 MB; annual refs are 87 characters. A 21-location cold read fell from 1.90 s to 0.41 s. A full-year raw-input composition remains too slow for demo-time use; precompiled per-location snapshots are the next step. Do not wire heavy raw composition into a request handler.
- Verified: full Python suite 500 passed + 30 subtests, 17 existing warnings; frontend 256 unit tests plus all 8 explicitly enabled live API tests passed; 9 estimator behavior checks passed; production build passed with its existing chunk-size warning. No frontend or canonical API files changed in this checkpoint.
- Needs urgent review outside this lane: new `origin/main` commit `dcbbf5a` adds `pipeline/national_stacking.py`, which aliases SPP rows to synthetic national regions, adds unseeded noise, fills missing targets with zero and fits its parent on test targets; the emitted model card claims production national coverage and 10,000 trials without those trials being implemented. These files were inspected read-only, not merged or used by Alex's wind/carbon work.
- Data progress: official SPP annual VER curtailment archive is now cached using the existing downloader, SHA256 `291fb1c1d7ac13d99c2ba7e55296106217cac6f84ba2db5d1b6fb4e75e432b9d`, URL `https://portal.spp.org/file-browser-api/download/ver-curtailments?path=/2024/2024.zip`. The earlier DA daily 404 was an archive rollup: the official listing exposes `/2024/2024.zip`; its first cached download is in progress. These archives need unit, interval and missing-data checks before model use.

## Full fuel-mix completeness audit - 2026-09-13 08:35 UTC
- Changed: `normalize_spp_generation_archive(raw, generation_source=..., timing_source=...)` in `pipeline/carbon.py` preserves all ten historical fuel families and additional named Market/Self pairs. Only the archive's `Gas Self` spelling aliases `Natural Gas Self`; no fuel emission factor is inferred from its name.
- Interface: output columns are `timestamp_utc`, `fuel`, `generation_mwh`, `unit="MWh"`, `generation_status`, `source_type`, `ref`; `attrs.expected_fuels` supplies the full fuel universe to `fuel_mix_intensity`. Explicit missing/negative-net statuses carry null generation, including after parquet round trips. Unmarked NaN, boolean/infinite observations, conflicting duplicates and malformed cadence are rejected.
- Honesty: twelve distinct complete five-minute observations are required per fuel-hour. Signed Market/Self components may offset within a sample; a negative combined sample makes that fuel-hour unknown rather than clamping it to zero. Observation-time hourly binning remains an explicit assumption, so these derived hourly records remain assumptions.
- Actual cached-year check: 87,850 fuel-hour rows across 8,785 UTC bins normalized in 4.2 seconds; 86,542 complete, 707 incomplete-observation and 601 negative-net Solar rows. The provider's annual file extends from 2024-01-01 06:05 UTC into 2025-01-01 06:00 UTC; it is not a complete UTC calendar year by itself.
- Actual carbon result: no numeric intensity can yet be justified. Missing generation affects 669 hours; the remaining 8,116 hours lack applicable positive-generation factors, involving Diesel Fuel Oil, Other and Waste Disposal Services. Waste Heat is retained too. No zero-factor substitution, shifted-carbon scenario or site claim was fabricated.
- Verified: 115 carbon tests passed; full Python suite 522 passed + 30 subtests, 17 existing warnings. Full-year intensity preparation takes about 43 seconds and must stay offline. Tharun should serve only prepared outputs and preserve null status until the remaining fuel/accounting assumptions are explicitly reviewed.

## Immutable grid-impact snapshots and accounting convention - 2026-09-13 08:52 UTC
- Changed: `compile_grid_impact_snapshot(location_id,path,wind_summary=...,carbon_shift=...,shift_coverage=...)` performs heavy composition offline and atomically publishes a new file without replacing an existing/racing destination. Envelope: `{snapshot_version:"grid-impact-snapshot-v1",location_id,result_sha256,result}`; `result` retains the six sourced scalars and complete evidence graph.
- Serving interface: `read_grid_impact_snapshot(location_id,path,cache=GridImpactSnapshotCache(...))` validates the snapshot without composition/model calls. The caller-owned cache is bounded by entries and serialized bytes, hashes fresh file content on every read, and returns isolated copies. Missing files remain unavailable; corruption, wrong locations, source upgrades, unreachable/dangling evidence and inconsistent coverage raise. Digests establish integrity, not source authenticity.
- Synthetic benchmark: 365 pairs produced a 754,540-byte snapshot, 0.349-second cold read and 0.103-second warm read. 8,760 pairs produced 17,963,912 bytes, 3.423-second cold and 0.559-second warm reads. Prewarm large snapshots at application startup; do not run offline composition in a request. No route or canonical API contract was changed.
- Scientific correction: EPA eGRID rates exclude biogenic CO2, allocate CHP emissions to electricity, and group plants by primary fuel. Loader refs and the standalone carbon basis now say so explicitly; these are attributed electricity-output rates, not all physical stack CO2. Coarse boundary tag and numeric factors are unchanged. See eGRID2023 Technical Guide sections 3.1.2.1, 3.1.2.2 and 3.1.3.3.
- Crosswalk warning: EPA `WDS` means wood/wood-waste solids, not SPP Waste Disposal Services. EPA groups diesel/DFO under Oil, but applying the broad SWPP Oil rate to SPP diesel still needs an explicit representativeness assumption. No unsupported waste/Other factors were added.
- Verified: 112 grid-impact and 116 carbon tests pass; full repository-root Python suite passed 682 tests + 30 subtests with 17 existing warnings, including the new wind-archive regressions in progress. Price/VER archive readers and their tests are the next wind checkpoint; the requested narrow frontend popup/text cleanup is being handled separately without data or economics changes.

## Requested source-popup and wording cleanup - 2026-09-13 09:03 UTC
- Scope exception: the user's new request explicitly authorized a narrow `/web` edit during Alex's lane; the main wind/backend task continues. Changed only App, Sourced, ConfidenceBadge and five affected test files.
- Removed the oversized raw-JSON source panel, its pin-hint footer and automatic hover/focus opening. Intentional click or keyboard activation shows compact source details. Exact `{value,source_type,ref}` records and full confidence descriptions remain in control metadata and the scenario export.
- Removed the crossover's redundant Assumed tag and `under these assumptions` text; the decision eyebrow now reads `DECISION`. Permanent site-exposure/user-assumption and coverage framing remain. No calculations, API shapes, source objects or styling files changed.
- Verified: frontend test cases increased from 264 to 267 by splitting two exhaustive interaction cases into independent cases, retaining every assertion and the existing timeout bounds. All 267 pass, including eight live FastAPI checks at `127.0.0.1:8000`; production build and typecheck pass. The existing bundle-size warning remains.
- Visual verification limitation: browser tooling returned `No browser is available`. This checkpoint was verified through source review and interaction tests, not a claimed browser screenshot inspection.

## Real wind archives and responsive sourced scenarios - 2026-09-13 09:22 UTC
- Changed: strict offline readers for SPP annual VER curtailments and day-ahead settlement prices. `read_cached_wind_curtailment_archive(year)` and `read_cached_day_ahead_prices(year,settlement_locations=[exact IDs])` validate cached URL/content hashes, ZIP members, headers, operating dates and conflicting revisions. Daily members are read once; monthly duplicates are excluded. No network occurs in these readers.
- Real evidence: VER 2024 archive SHA256 `291fb1c1d7ac13d99c2ba7e55296106217cac6f84ba2db5d1b6fb4e75e432b9d`; DA 2024 archive `f4c84709e762bcac452ef9b2098b5154e3e2386e59295cdd22b8008bd612d5c6`. URLs are `https://portal.spp.org/file-browser-api/download/ver-curtailments?path=/2024/2024.zip` and `https://portal.spp.org/file-browser-api/download/da-lmp-by-settlement-location?path=/2024/2024.zip`. Both use existing ingest caching, not new fetch code.
- Interface: `prepare_wind_curtailment_labels(raw,origin=...,system_scope="SPP_SYSTEM",scope_source=...)` labels any positive reported wind category within a fully observed/evaluable twelve-interval hour; incomplete hours stay unknown. Energy curtailment is not added again to redispatch. These are system event labels, not recoverable MWh or full hours of curtailment.
- Observed proxy counts at the declared 0.50 wind-share/0 USD-MWh policy: AEPM_CSWS 35, LES_LES 1160, OKGE_OKGE 229, OPPD_OPPD 855, SPPNORTH_HUB 1029, SPPSOUTH_HUB 522, SPS_SPS 459, WFEC_WFEC 238. Each has 8,713 evaluable joined hours; no annual total is justified. Identifiers are exact settlement points, with no invented city/site mapping.
- Scenario interface: summaries now include `energy_model:"declared_available_capacity_times_proxy_hours_v1"` and `scenario_inputs:{flexible_load_mw:{value,source_type,ref},available_fraction:{value,source_type,ref}}`. `read_wind_scenario(location_id,path,flexible_load_mw=...,available_fraction=...,cache=...)` recomputes only wind-only bound snapshots, retaining original and new provenance separately. It rejects existing carbon-shift schedules and unbound legacy snapshots; it never infers site_exposure semantics.
- Verified: eight new immutable `_2024_bound_review.snapshot.json` files in ignored `data/processed/grid_impact/` support zero/different controls; warm recomputes took 1.36-3.34 ms. Wind MWh is a capacity scenario, associated direct wind CO2 is zero, annual energy and shifted carbon remain null. Safe multiplication order avoids intermediate overflow while rejecting unrepresentable totals.

## Frozen historical wind-event classifier - 2026-09-13 09:22 UTC
- Changed: `prepare_wind_classifier_features`, `fit_wind_event_classifier`, `predict_wind_event_classifier` in `pipeline/wind_signal.py`. This separate offline research experiment predicts reported SPP-system wind-event occurrence, not the existing shutdown-risk model, confidence badge, local availability or energy absorption.
- Exact policy: four wind/load actual lags at 1h and 24h; no current values, interpolation, prices or target-derived features. Train/scaler January-August 2024; regularized sigmoid September only if at least 20 examples per class; test October-December. All splits UTC. Seed 2026, C=1, unweighted logistic, lbfgs, max_iter=2000, tol=1e-8. No test-based model choice.
- Interface: fit returns `(bundle,report,prediction_rows)`. JSON bundle `spp-wind-event-logistic-v1` contains model_id, ordered features, standardizer, base_model, calibrator, and source/cohort/policy manifest. Report carries sourced cohort counts, raw/calibrated/baseline Brier, log loss, AUC and ten fixed reliability bins. Rows retain sourced probabilities, observed target, evaluation flag, input hash/sources, period role and timing limitations. Safe JSON loading never executes pickle or provenance refs.
- Frozen model `6cfb0e75875fb7116fb753d217e9514e10124c9183dd8c924fad87269814b87d`: 5,674 eligible training hours, 720 calibration, 2,182 test (1,989 positive/193 negative; 26 excluded). Raw Brier 0.0549638149396274; calibrated 0.05372127591616836; training-prevalence baseline 0.08315264238763241. Raw/calibrated AUC 0.9405330353212097. Log loss respectively 0.17484929517423325, 0.16669861048441414, baseline 0.310836520219898.
- Honesty: generation hourly-bin timing is still an assumption, so derived metrics/probabilities remain assumption-sourced research. Actual VER event labels are data. Publication/revision availability is unverified; this is historical replay, not demonstrated live forecast skill or site confidence. No production route/model was replaced.
- Reproducibility: two fits with reversed input/source order were byte-identical; JSON-reloaded predictions matched every probability. Immutable bundle/report/rows and publication hashes are stored under ignored `data/processed/wind_model/<model_id>/`; bundle SHA256 `e5e1ccb56eb40146d15a7c638cc314a181b6b6e4122a678e4c6f80aac318fcde`. Full repository-root Python suite: 777 passed + 30 subtests, 17 existing warnings.

## Predeclared independent 2025 replay - 2026-09-13 09:22 UTC
- Before inspecting 2025 outcomes, the above 2024 model and publication manifest were frozen. Preserve its four lags, fitted scaler/base/calibrator and event policy; evaluate all eligible 2025 UTC hours without retraining, recalibration, threshold tuning or model selection. Compare raw, calibrated and constant 2024 training-prevalence probabilities on identical rows; report missing/excluded hours and both probability/calibration diagnostics.
- Existing downloader cached 2025 GenMix, annual VER rollup and twelve monthly load files. Only archive structure/headers/hashes were examined so far. VER uses the exact new member `2025-VER-Curtailments-ANNUAL-ROLLUP.csv`; load uses `HOURLY_LOAD-2025MM.csv`. Owned readers need explicit format support before observations can be evaluated; no 2025 price data is required by this classifier.
- This evaluation remains offline SPP-system historical replay with unverified publication timing and assumption-sourced hourly binning. A second year must not silently promote the experiment to site availability, confidence or production status. No files owned by Kristian/Tharun or shared API contracts are being changed.

## Requested removal of repeated demo badges - 2026-09-13 09:32 UTC
- Scope exception: the user's repeated urgent frontend request authorized removal of the small source-letter superscripts, repeated Assumed pills and slider USER ASSUMPTION badge. Changed twelve frontend component/test files; no CSS, source data, calculations, model or API shapes changed.
- Values remain clickable for deliberate source inspection; standalone source controls use a neutral info icon. The source popup has a neutral heading, opens only on click/keyboard, and native raw-JSON hover titles were replaced with short ordinary titles plus exact `data-provenance` metadata. Scenario exports preserve every source object.
- Actual confidence levels, the slider's explanatory meaning and one accurate unavailable/unverified status per affected section remain. No fake inputs were relabeled as measurements. Removed the unused MockLabel rendering function while retaining its source-detection helper.
- Verified: 46 focused frontend tests and all 267 frontend tests pass, including eight live API cases; production build passes with the existing large-chunk warning. No tests or timeout protections were removed. Vite at `127.0.0.1:5174` serves the updated App/Sourced modules with no badge JSX or source-letter rendering.
- Visual limitation remains: no browser surface is available to the agent. HTTP module checks and interaction tests confirm the changes are served; no screenshot inspection is claimed. Continuing the authorized backend lane and audit loop after this checkpoint.

## Later-year readers and provenance audit - 2026-09-13 09:50 UTC
- Changed: explicit 2025 GenMix, VER annual-rollup and monthly-load cache readers in `pipeline/wind_signal.py`. `read_cached_monthly_load(2025)` returns raw MarketHour plus the seventeen components and all month sources for the existing `normalize_legacy_load`; it never fetches, imputes, chooses revisions or writes. VER accepts only the exact annual member and removes ten exact complete repeated header rows, rejecting malformed lookalikes. All cached URL/hash/header/date checks remain.
- Data limit: the file named `2025-VER-Curtailments-ANNUAL-ROLLUP.csv` supplies 8,016 hourly bins from January 1 06:00 UTC through December 1 05:00 UTC, not a complete UTC year. Its filename must not be used as evidence of full-year coverage.
- Provenance corrections: carbon complete-generation totals now retain the declared fuel universe/application source that determines missingness. Both wind and carbon source validators reject upgrades hidden in recognized nested `{method,inputs}` provenance; unrelated JSON citations remain opaque. Probability refs now identify the target timestamp/input hash; metric refs include evaluation window, input/label hashes and eligible-cohort hash.
- Numerical check: recombining the raw load frames produced up to three floating-point steps of summation difference (maximum 1.4551915228366852e-11 MW), with identical missingness. The replay instead preserves all 8,784 frozen input rows exactly and appends only later observations. No frozen values were rounded, replaced or silently renormalized.
- Verified: 201 focused wind/evaluation/reader tests and 127 carbon tests passed; full repository-root Python suite passed 880 tests + 30 subtests, with the same 17 existing warnings. Frontend remains at 267/267 and a passing production build from the immediately preceding checkpoint.

## Primary-source correction to generation interpretation - 2026-09-13 09:50 UTC
- Needs team review: SPP's GenMix metadata identifies Self columns as end-of-dispatch MW targets, not verified meter readings. It calls GMT MKT Interval hour-ending, while gridstatus 0.36 treats the five-minute field as interval-start. Retain the existing explicit binning assumption; do not upgrade this mixed generation/dispatch series to verified actual generation or metered MWh.
- Evidence: https://portal.spp.org/api/pageConfig/by-slug/generation-mix-historical , cached SHA256 `0160d0a41a70029190b79c3856cd427eeb59381a0d1af1f083f8e40011d435db`. SPP Public Data Guide v33, printed page 19, confirms MW and market/self split; GenMix Load is STLF, not actual demand: https://www.spp.org/documents/69875/spp%20markets%20public%20data%20guide%20and%20samples%20v33.zip , SHA256 `c171f2294188a9249170e90a52b4c108a5179a5229692367876cbba4309bdce1`.
- Additional primary context: SPP Market UI Guide v18.0, printed page 14, uses interval-ending notation generally, but does not fully resolve the annual GenMix file semantics: https://www.spp.org/documents/66676/integrated%20marketplace%20market%20user%20interface%20guide%20v18.0.pdf . All evidence was cached through the existing downloader; no EIA fetch or shared ingestion/generation edit was added.
- Changed: `GENMIX_SOURCE_QUALIFICATION` accompanies new predictions/reports and raw reader refs. Current wording says reported inputs and identifies dispatch/timing limits. The validator accepts only the exact old or revised limitation literal so the frozen model remains replayable; new outputs always attach the revised qualification. Legacy acceptance is not endorsement of the old actuals wording.
- Original 2024 bundle/report/prediction files and fitted parameters remain byte-for-byte unchanged. A later timing correction must be a separately versioned experiment, not a silent change to this held-out replay. Carbon normalization documentation now identifies its endpoint-MW-to-MWh calculation as a scenario approximation; no formula changed.

## Frozen 2025 validation, including missing calendar hours - 2026-09-13 09:50 UTC
- Interface: `evaluate_wind_event_classifier(bundle,hourly,labels,sources=...,baseline_probability={value,source_type,ref},start_utc=...,end_exclusive_utc=...) -> (report,rows)`. It requires a later window after the fixed 2024 experiment, evaluates the full requested UTC grid, never fits/selects a model, and returns `spp-wind-event-independent-replay-v1` with sourced coverage, raw/calibrated/supplied-constant metrics, input hashes and source qualification. Baseline origin is explicitly caller-supplied and separately verified against the frozen report.
- Real 2025 request: 8,760 calendar hours; 8,016 supplied label hours; 744 absent labels, 61 unknown supplied targets, 146 missing exact-lag inputs. Exclusion causes overlap: union 904 excluded, leaving 7,856 identical evaluation rows for every available comparator (6,800 positive / 1,056 negative).
- Results: raw Brier `0.0871824661469341`, log loss `0.2770713584515885`; calibrated Brier `0.08763549573865433`, log loss `0.28362254790764796`; both AUC `0.8741492591354724`. Frozen training-prevalence baseline Brier `0.11636928267871754`, log loss `0.39478002120308503`, AUC `0.5`. Calibration was slightly worse on this later period; no automatic model selection or production promotion occurred.
- Reproducibility: loaded frozen model `6cfb0e75875fb7116fb753d217e9514e10124c9183dd8c924fad87269814b87d`; fitting was patched to raise during evaluation, and all original artifact hashes were rechecked afterward. Independent sklearn metric calculations agree within 1e-12. All derived model metrics remain assumption-sourced historical research because of generation interpretation/timing limitations.
- Immutable ignored artifacts: `data/processed/wind_model/<model_id>/replay_2025_review_20260913.json` (189,013 bytes; SHA256 `8fabce992cb884afa5a67f0f64446d9cebe6cd1293336470f65fc0e63a995f5e`) and companion predictions JSON (77,964,710 bytes; SHA256 `bc0516fa9eb95c229895c9fa4351c49286c1d312cd7b474d7bd370afec8bda0f`). The detailed row artifact is offline audit material, not a demo HTTP payload. No route or shutdown-risk/confidence model was changed.

## Main leaderboard integration mismatch - 2026-09-13 09:52 UTC
- Read-only fetch found main `9e8a6a0`, Tharun `e1257ef`, Kristian `2d22db1`. No merge was performed during the isolated Alex lane; the current frontend remains intact.
- Needs Tharun review: `docs/FROM_THARUN.md:14-33` on main requests bare annual floats/global model provenance in `national_stack/wind_absorption.json`; `api/zone_ranking.py:20-21` rejects nulls. Our verified `grid-impact-v1` supplies per-value provenance/evidence, observed-period scenarios and null annuals without complete coverage. A filename swap cannot reconcile these contracts.
- Actual loader gap: main `pipeline/site_rank.py:78` still unconditionally calls its mock generator and has no wind_absorption.json reader. Lines 33-34 apply an invented 0.45 tCO2/MWh offset; `ZoneLeaderboard.tsx:186-189` calls this CO2 Prevented/displaced. Our wind-associated direct operational CO2 is zero, not displacement or atmospheric removal; signed shifted carbon requires a separately supported conserved schedule. Do not adapt our outputs into the offset formula.
- Provenance/geography gaps: ranking records discard source_type and render bare figures. Expected zone IDs such as OKGE/WR/spp-wichita-demo have no reviewed mapping to our exact settlement-point IDs. Integration must preserve sourced nullable values, comparable coverage, explicit capacity/availability controls and correct point semantics. `read_wind_scenario` is the existing fast wind-only adapter; it never invents a site_exposure mapping.
- Earlier national-model warning remains unresolved: `6947fba` fixes SHAP/history handling but does not change `pipeline/national_stacking.py`; synthetic national aliases, unseeded noise, zero-filled missing targets, training on test targets and unsupported production/trial claims remain. Kristian `2d22db1` is documentation only. These files were inspected, not edited or used by the independent wind work.

## December supplement and complete-calendar replay v2 - 2026-09-13 10:06 UTC
- Found the actual December file outside SPP's partial annual rollup. `read_cached_wind_curtailment_month(2025,12)` validates its exact URL/hash/schema/size and Central operating month, retaining missing values/revisions for the existing label preparer. Official source: https://portal.spp.org/file-browser-api/download/ver-curtailments?path=/2025/12/VER-Curtailments-MONTHLY-202512.csv ; 824,602 bytes, SHA256 `877a5e0f866ce8c06937a0caa23d21a8d26ee3a92f1e1bccaf38f952ad7f16a4`, 8,928 five-minute rows. Only reviewed December 2025 is accepted by this reader.
- New immutable dataset combines six January UTC hours from the 2024 archive, the existing 2025 rollup and the December supplement. All 8,760 UTC calendar hours now have supplied label records; 72 targets remain unknown and 146 hours lack lag inputs, with 27 overlapping exclusions. Evaluation uses 8,569 identical eligible rows (7,462 positive / 1,107 negative). Complete calendar coverage is not complete evaluable coverage.
- Same frozen model, zero fits: raw Brier `0.08483715110003835`, log loss `0.2718539027309567`; calibrated Brier `0.08521522493120687`, log loss `0.2783341954665869`; both AUC `0.873760628073537`. Frozen baseline Brier `0.11258798356654705`, log loss `0.3852241033571313`, AUC `0.5`. Calibration still does not improve the annual comparison. All derived metrics remain assumption-sourced research, with no model selection/promotion.
- Artifacts in the same ignored model directory: `replay_2025_complete_calendar_v2.json`, SHA256 `3771b18cfe923884a2262db71b80c3f7ab0c7ba368d5ce8664bd4da9bd52d09d`; `predictions_2025_complete_calendar_v2.parquet`, SHA256 `dcc8e978f37506b6fb1395e346afebb7463f7d9d91bb2101697e203ef2f8567e` (281,813 bytes). Parquet round-trip preserves every nested source and value exactly. The publication manifest records columns/paths/hashes; old partial reports and frozen artifacts remain unchanged.
- All twelve monthly diagnostics are retained under `monthly_diagnostics_2025_complete_calendar_v2.json`, SHA256 `fa629878cb897bf4a1c22eccc2b0652c3472de3d7089d42620ba5f32d11886be`. All 117 independent metric comparisons and aggregate reconciliations pass. July-August degradation remains visible; calibration worsens raw metrics in June, July, August, November and December. This is post-hoc diagnosis, not automatic parameter selection.

## Exact scenario multiplication and prepared-source boundary - 2026-09-13 10:06 UTC
- Changed: shared `wind_scenario_mwh(proxy_hours,flexible_load_mw,available_fraction)` validates controls, multiplies their exact binary-float ratios and rounds once. Wind summaries and the standalone API now share it. This fixes premature overflow/underflow without changing the count-times-capacity-times-availability formula or unknown-data handling.
- Verified numerical cases: `4 * 5e-324 * 0.5` correctly returns `1e-323` instead of false zero; true final overflow still raises, legitimate final underflow still rounds to zero, and zero availability remains zero. Existing one-float-step compatibility stays; erroneous old subnormal snapshots are rejected rather than accepted through a wider tolerance. All eight real structured snapshots retain exactly the same normal-scale energy values.
- Provenance correction: `read_cached_historical_wind_inputs` now marks its derived `generation_source` as assumption, preserving the original document source separately in `raw_generation_source` and attaching `source_qualification`. An observed file's data tag cannot accidentally pass through an unverified hourly-binning transformation. No frozen dataset or canonical estimate contract changed.
- Verified: 75 focused wind tests, 152 standalone API tests, and the full repository-root suite at 916 passed + 30 subtests; the same 17 pre-existing warnings remain. Frontend/build remain verified at 267 passing cases and the preceding production build, with no further frontend edits. Continuing the self-audit loop on Alex.

## Offline snapshot handoff - 2026-09-13 10:39 UTC
- Changed: `python -m api.grid_impact compile --input prepared.json --location-id EXACT_ID --output new.snapshot.json` compiles the existing `grid-impact-inputs-v1` envelope; `check --snapshot received.snapshot.json --location-id EXACT_ID` validates it without writes/recomputation. Both fail nonzero for missing/corrupt input; explicit unavailable records and partial-year nulls remain valid. Existing destinations are preserved.
- Interface: `read_grid_impact_snapshot(..., required=True)` raises on missing files for offline verification; its default API fallback is unchanged. CLI stdout is a compact status/location/schema/result-hash object. No HTTP wiring, fetching, training, implicit capacity, or site-ID mapping was added.
- Reproduced all eight existing structured review snapshots byte for byte from one complete prepared envelope. Local transfer bundle: `data/processed/grid_impact/handoff_2024_review_v1.zip`, 32,344 bytes, SHA256 `21e9d63931965db3a5bd200d472d36d5c5172b3d9e3966fb2fa836edd1157df9`. Includes eight snapshots, `prepared_inputs.json` (SHA256 `ec173eade9c9579857c11c511974a07a21078cda2048d737160bf3458568b2f1`) and a file/ID/hash manifest with current source qualifications and limitations.
- Integration prerequisite: runtime artifacts remain gitignored and are not uploaded by pushing Alex. Transfer this reviewed bundle separately, compare sender hashes, then run check from the repo root. Verification proves structural integrity, not authenticity, annual completeness, or local deliverability. The bundle does not include/retrain the frozen classifier; replay needs its separate exact inputs/labels/baseline/bundle.
- Current scenarios retain assumed 100 MW x 0.5 available capacity, original high-wind/low-price counts, null annual values, zero associated operational wind CO2 and unavailable shifted carbon. No observed value or source was changed by packaging.

## Carbon evidence and input-validation audit - 2026-09-13 10:39 UTC
- Fixed: cold snapshot reads now compare carbon scalars with their cited kilogram derivations and annual/unknown provenance. Newly compiled shift conversions preserve a named sourced `carbon_shifted_kg_co2` quantity with `unit="kg CO2"`; six output scalars and accounting math are unchanged. Known legacy derivations remain checked; opaque untyped legacy refs remain readable with their validation limit documented. Cache hits do not rerun these checks.
- Fixed: the monthly load reader preserves signed regional values under the shared normalizer's existing semantics, while still rejecting booleans, invalid text and infinities. Negative complete system totals still fail downstream. Actual 2025 cache: 8,760 rows, 14 missing regional readings, no negative components/totals; existing prepared data was not rewritten.
- Hardened the private metric helper against multidimensional/misaligned arrays and nonfinite/nonbinary targets. Current public callers already prevented these invalid cases; published metrics and frozen models were unaffected. Synthetic endpoint/tie/single-class values still match sklearn, and unavailable calibration retains nulls.
- Verified: full root Python suite 997 passed + 30 subtests, the same 17 pre-existing warnings; final test-only sklearn keyword compatibility check 96/96 passed. Focused snapshot tests 171, CLI tests 16; all 16 prior local snapshots remain readable. Frontend/build remain at the prior 267 passing tests and production build; no further frontend changes.
- Remote audit: fetched origin again; main remains `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`. No new merges or resolution of the earlier integration/model warnings. Continuing on Alex only.

## Portable frozen-model replay - 2026-09-13 10:56 UTC
- Changed: `publish_wind_replay(*,bundle_path,hourly_path,labels_path,baseline_path,start_utc,end_exclusive_utc,output_dir)` and `python -m pipeline.wind_signal replay --bundle ... --hourly ... --labels ... --baseline ... --start-utc ... --end-exclusive-utc ... --output-dir NEW_DIR` now run the existing evaluator offline. No fitting, fetching, parameter selection or API wiring; source-bearing parquet attributes and the explicit caller baseline are mandatory.
- Publication contract: `manifest.json` uses `schema_version="spp-wind-replay-publication-v1"`, research status, model_id, normalized UTC window, source-file hash, runtime versions, parquet settings/columns, `inputs`/`outputs` role-to-relative-path maps, and `files:{relative_path:{sha256,bytes}}`. Four exact consumed byte buffers are copied to inputs/, alongside report.json and predictions.parquet. The final immutable manifest is published last; existing/racing directories are preserved, and failed publication must not be treated as complete.
- Actual replay: copied the exact previously sourced baseline `0.861297144871343` from the verified v2 report (new baseline file SHA256 `e19bd8d6f76d7f5de41245634044894e603282434de8411aa19f30abc4ca0ae3`). Same frozen model, four explicit inputs totaling 563,915 bytes, zero fits/fetches. Parsed report and all 8,760 prediction rows match v2 exactly; 8,569 remain eligible. The prediction parquet retains SHA256 `dcc8e978f37506b6fb1395e346afebb7463f7d9d91bb2101697e203ef2f8567e`. Original artifacts were not changed.
- Local publication: `data/processed/wind_model/6cfb0e75875fb7116fb753d217e9514e10124c9183dd8c924fad87269814b87d/replay_2025_portable_v3/`, manifest SHA256 `1e8d9968c0660a7a4742ecc70d753060f038e6007a134e13e1d46791fa60decc`. Sibling `replay_2025_portable_v3.zip` is 677,271 bytes, SHA256 `2294bf46a65f0f9434cb32bf7b546edbda81cfa9fd3e7870952ca21da0b33f57`. Artifacts remain ignored/local and require separate transfer.
- Independent verification: actual CLI replay in an isolated source copy with only this publication reproduced all seven files byte for byte, including the manifest; no raw cache was present. Separately, the preceding eight-location grid-impact bundle passed all checks and recompiled identical snapshots from an isolated copy without raw caches. These establish reproducibility under the recorded runtime, not source authenticity, verified forecast availability or site-specific deliverability.
- Tests: full root Python suite 1,024 passed + 30 subtests (same 17 existing warnings); frontend all 267 tests passed, including 8 live local API contract checks; production build passed with its existing large-chunk warning. Publisher-specific coverage is 53 tests, including exact input copying, nested null/source preservation, concurrent input changes, invalid metadata, interrupted publication and immutable destinations. No frontend edits.
- All model timing qualifications and research framing remain unchanged; the new packaging does not promote the classifier or replace the canonical exposure API. Continuing the audit loop on Alex.

## Raw archive boundary recovery and frozen replay v4 - 2026-09-13 11:23 UTC
- Found one complete hour split between raw annual GenMix archives: January 1, 2025 06:00 UTC has one sample in 2024 and eleven in 2025. Concatenating all 210,528 raw samples before the existing normalizer recovers 10,444.65 MW under the unchanged observation-time assumption. Separately normalized partial-hour means cannot be combined. All 8,784 frozen prefix rows and all load values remain exact; no shared normalizer, labels, fitting or calibration changed.
- Added six regression cases for unequal raw partitions, exact prefix preservation, missing components/intervals, conflicting revisions and duplicates that cannot stand in for missing intervals. Full repository-root Python suite: 1,030 passed + 30 subtests, same 17 existing warnings. Frontend remains at 267 passing tests and a passing production build from 10:54 UTC; no further frontend edits.
- Versioned input `wind_inputs/spp_2024_2025_classifier_raw_stitched_v4.parquet` has SHA256 `0a5350c38872b49871d42b6d73845d81637e55929518aaba20ab0dbf93cac856`. This single recovered input adds two evaluable predictions through the exact one-hour and 24-hour lags: 8,571 eligible (7,464 positive / 1,107 negative), 72 unknown labels, 144 missing lags, 27 overlapping exclusions, 189 excluded of 8,760 requested hours. All 17,228 previously known raw/calibrated probabilities remain bit-for-bit unchanged.
- V4 annual raw Brier `0.08481804789469169`, log loss `0.271801079664323`; calibrated Brier `0.08519556998496339`, log loss `0.27827526476534636`; both AUC `0.8737784787637086`. Frozen baseline Brier `0.11256620092705442`, log loss `0.38516905531483553`. Independent annual/monthly metric and coverage reconciliation passed. Calibration still worsens this annual comparison; every derived result remains assumption-sourced research, with no production promotion.
- Local-only transfer bundle in the existing frozen-model directory: `replay_2025_raw_stitched_v4.zip`, 706,779 bytes, SHA256 `2df3adbf5c5802bac1bb0f94ae0a2ee860b5512fe59e63e327a2079329fc4215`. Manifest SHA256 `2f15c94dabe4a9bf9a8e92af13416789fe8029b78b67eaa26a8c311956196e6b`; report SHA256 `4619e34c69d4cf95f8a52a199c5e2e9474133c9111c1aa1d63d00e87167682f5`; predictions SHA256 `4493141c249977f7dbac3f2a9aeb50d1da1e30c794cbdc73cbcf1bca5f13e733`. All seven archive members match their originals. Older publications remain intact; pushing code does not transfer ignored runtime artifacts.
- Remote check at 11:22 UTC: main `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`, unchanged. No merge or frontend replacement. Existing source/assumption badge removal remains served at `http://127.0.0.1:5174/app`; source values and click-only inspection remain intact.

## EPA fuel-category review: keep unsupported intensities unknown - 2026-09-13 11:23 UTC
- Cached the official eGRID2023 Technical Guide at `data/raw/epa/evidence/egrid2023_technical_guide.parquet`: https://www.epa.gov/system/files/documents/2025-01/egrid2023_technical_guide.pdf , PDF SHA256 `12164c665217f1b4d00ac6a8115a3806710dab79993d02959cdf7f7f66abd841` (2,768,135 bytes). No shared ingestion code or carbon factors changed.
- Printed pages 35/41/46 include diesel in DFO, and Table 3-3 groups DFO under oil. That supports a fuel-family classification, not treating a broad annual BA oil rate as a measured hourly diesel rate. Applying the existing oil factor to SPP Diesel still needs an explicit sourced application assumption and compatible generation basis.
- WDS means wood/wood-waste solids, not SPP's Waste Disposal Services. MSW combines biogenic and fossil contributions; eGRID removes only biogenic CO2. WH is waste heat; its Appendix C zero/No-EF convention is not a measured factor for SPP's category. OTH uses a natural-gas heat-input factor, which cannot be substituted for electricity-output kg/MWh without the corresponding heat rate and denominator.
- Workbook rows corroborate EPA classifications (DFO/OIL, WDS/BIOMASS, WH/OTHF, MSW/BIOMASS) but do not supply the missing SPP resource-to-fuel crosswalk. The cached SPP guide and reviewed public pages did not establish that mapping. Human review is still needed; positive generation with unsupported factors stays null. Wind-associated operational CO2 remains zero; shifted carbon remains unknown without supported intensity and a conserved makeup schedule.

## Reject ambiguous structured provenance - 2026-09-13 11:29 UTC
- Found and fixed a concrete source-upgrade path: default JSON parsing accepted a nested `source_type` twice, allowing a later `data` value to hide the earlier `assumption`. Wind/carbon validators and the API evidence compactor now strictly reparse recognized `{method,inputs}` references, rejecting duplicate keys, NaN/Infinity and float-literal overflow. Unrelated opaque citation text is preserved; valid refs, math and response shapes are unchanged.
- Added 48 focused regression cases across wind, carbon and API boundaries. These cover ambiguous nested/root keys, references nested inside reference strings, nonfinite numeric values, rejected snapshot publication, and unchanged opaque citations. Full repository-root suite: 1,078 passed + 30 subtests; same 17 existing warnings. Frontend has no new edits and retains its preceding 267 passing tests/build.
- Actual-artifact compatibility: all 16 review snapshots cold-read successfully; eight handoff snapshots regenerated byte-for-byte, all nine manifest file hashes verified, and 26 source artifacts stayed unchanged. The v4 replay reproduced its full report and all 8,760 rows exactly with fitting/fetching disabled; all six input/output file hashes remain unchanged and 8,571 rows remain eligible.
- Independent carbon numerical audit found no production-range defect: 100 deterministic ten-fuel mixtures agreed with exact rational arithmetic within 2.274e-13 kg/MWh. DST-offset conservation/reversal checks passed, and even a tiny positive unsupported-fuel contribution remained unknown rather than disappearing from coverage. No carbon factor or source-data changes.
- No HTTP wiring, canonical model changes or UI changes. This is validation of structured evidence, not authentication of an arbitrary citation. Continuing the owned-lane audit on Alex.

## Timestamp, CSV and cache-metadata import guards - 2026-09-13 11:46 UTC
- Fixed reproduced import hazards in the owned wind readers: pandas could silently discard CST/CDT/PDT timezone suffixes, and CSV `usecols` could hide unheaded fields. Documented bare GMT, Z and explicit-offset times retain their meaning; timezone-dropping warnings now raise. CSV records are streamed and checked for exact declared field counts before projection, with quoted fields, missing values and empty lines preserved. No shared normalizer or label policy changed.
- Fixed the same duplicate-JSON-key issue at all five raw `source_json` reader boundaries: earlier source types, URLs and hashes can no longer disappear behind a later key. Reused the strict artifact parser for cache/replay JSON, rejecting duplicate keys, nonfinite constants and overflowing numeric literals while preserving valid metadata and refs. This does not authenticate source claims.
- Actual archive audit: all 10,481,150 rows across 366 day-ahead members have eight fields; no malformed records. Selected AEC prices (8,784 rows), VER2024 (105,408), VER2025 rollup (96,192), December VER (8,928), GenMix2024/2025 (105,408/105,120) and monthly load (8,760) retain exact frames and provenance under the timestamp/CSV guards. Full-year DA preparation takes 63.083 seconds versus 26.688 before; the extra streaming pass stays offline and does not affect precompiled demo reads.
- Added a concurrent-request regression: eight requests sharing one initially cold snapshot cache retain independent capacity/source records, correct MWh, null annual/shifted values and operational zeros; caller mutation cannot poison the cache or later requests. Snapshot bytes remain unchanged.
- Verification: 499 focused wind tests; full root Python suite increased from 1,078 to 1,150 passing tests + 30 subtests, with the same 17 existing warnings. Frontend remains unchanged at its preceding 267 passing tests/build. No cached data, fitted parameter, HTTP contract or frontend edit.

## Frozen input-range and reliability reconciliation - 2026-09-13 11:46 UTC
- Reconstructed the exact 5,674-row training cohort (4,887 positive), SHA256 `61a674906d84ef306c42782b02a28c47bd00481aa298a5847fed97ea9f4a481e`, matching the frozen manifest. All four reconstructed means and population standard deviations equal the stored scaler exactly. The 8,571-row v4 evaluation cohort also matches its stored SHA256 `3b1ccbe94afa1f1518f334ee839ca84f8ff00ca2fd7fb76209af434d4b11c3ad`.
- Descriptive range audit: 82/8,571 eligible later rows (fraction `0.009567145023917863`) have at least one feature above its training maximum; none are below a minimum. Individual wind-lag counts are 39 each, load-lag counts 3 each. Training maxima are 23,402.358333333334 MW wind and 54,011.34300000001 MW load; later maxima are 26,096.325 and 54,529.3 MW. Marginal min/max coverage is not joint historical support, a confidence score or validation of forecast probabilities.
- Existing ten-bin reliability counts reconcile to all 8,571 rows for each comparator. Count-weighted absolute mean-prediction versus observed-frequency gaps: raw `0.022683142629650658`, calibrated `0.03287112548508881`, constant baseline `0.009546397305765897`. Overall mean raw/calibrated probabilities are `0.8590811588862898` / `0.9034602852589163`, against observed frequency `0.8708435421771089`. These are descriptive fixed-bin summaries, not future error bounds; the constant's smaller bin gap does not give it discrimination (its AUC remains 0.5 and Brier is worse).
- All diagnostics remain assumption-sourced because GenMix dispatch/timing qualifications persist. No refit, model selection, production promotion, API field or saved-artifact change was made. The original report/inputs/rows remain the reproducible evidence; no new confidence estimate is inferred from this audit.

## Raw-timestamp sensitivity and independent reproduction - 2026-09-13 12:12 UTC
- Compared the existing observation-time bins with a hypothetical five-minute interval-ending interpretation by shifting raw GenMix timestamps before the same normalizer. This is a sensitivity scenario, not a correction: SPP's archive documentation still does not resolve the five-minute timestamp semantics. Identical cached bytes, load, day-ahead prices, eight exact settlement points, and fixed wind/load >= 0.5 plus LMP <= 0 policy; no fitting, threshold tuning or annualization.
- Requested window remains 2024-01-01 06:00 through 2025-01-01 06:00 UTC: 8,784 hours per point. Each interpretation has 8,713 evaluable hours; 8,709 are common, four are known only under each interpretation, and 67 are unknown under both. This operating-year window is not a complete UTC calendar year.
- All-evaluable positive counts, existing -> hypothetical ending: AEPM_CSWS 35 -> 35; LES_LES 1,160 -> 1,159; OKGE_OKGE 229 -> 229; OPPD_OPPD 855 -> 854; SPPNORTH_HUB 1,029 -> 1,027; SPPSOUTH_HUB 522 -> 521; SPS_SPS 459 -> 457; WFEC_WFEC 238 -> 238. On common-known hours LES has one addition and one removal; OPPD, SPPNORTH, SPPSOUTH and SPS each lose one; the other three are unchanged. Differences outside that cohort are coverage changes, not flips.
- Independently reconstructed both wind means from raw samples; 8,710 complete generation hours satisfy ending_mean(H)-existing_mean(H) = [W(H+1h)-W(H)]/12 with maximum arithmetic discrepancy 4.547473508864641e-12 MW. A second implementation verified all eight contingency tables, exact price/time joins, source kinds and input hashes. No original cache, published snapshot or frozen-model artifact was changed.
- Local-only audit directory: `data/processed/wind_audits/gmt_binning_2024_v1/`. Report SHA256 `90395a57026c31b1b915d75d6e95d52c34052b4a835304fb6bc89bd2f15acccf`; comparison parquet `27d2cb7e6f19f5766298d8e4bb697cc5db5a039bf75367648469dbd8bd64fdd7`. Includes the exact script, manifest and separately captured `reproducibility.json` (SHA256 `a5b3155c71b85890ae85a3f3ceb11eb19af10acb58cf6ce5c0beada652f35026`) with runtime, code/input hashes and invocation. It fingerprints but does not transfer the ignored input caches.
- Separate-directory replay reproduced the script, report, parquet and original manifest byte for byte under the recorded runtime. All diagnostic numbers remain assumption-sourced research; no source-timing resolution, site deliverability, forecast validation or production promotion is inferred. Code/tests unchanged since the 1,150-pass full suite; frontend unchanged since its 267-pass suite/build.
- Demo confirmation: Vite at `http://127.0.0.1:5174/app` serves the component without assumption/source-letter badges and with click-only source inspection, verified at source level; no new frontend edits or visual-verification claim. Continuing with an independent system VER-event co-occurrence audit of the fixed proxy.

## Fixed proxy versus independent system VER events - 2026-09-13 12:30 UTC
- Independently compared the unchanged 2024 high-wind/low-price screen with reported SPP_SYSTEM VER wind events, separately at the same eight exact price points. Every point has 8,713 common-known hours of 8,784 requested: 60 unknown under both inputs, 11 with a known VER label but unknown proxy, and none with a known proxy but unknown VER label. Missing evidence was never treated as a negative.
- The common cohort contains 7,624 system-event hours (frequency 0.8750143463789739). All selected proxy hours coincide with an event: AEPM_CSWS 35, LES_LES 1,160, OKGE_OKGE 229, OPPD_OPPD 855, SPPNORTH_HUB 1,029, SPPSOUTH_HUB 522, SPS_SPS 459, WFEC_WFEC 238; every point has 1,089 proxy-negative/event-negative hours and zero proxy-positive/event-negative hours. Event capture ranges from 35/7,624 to 1,160/7,624. These are subsets of an already common system event, not perfect detection, local availability, recoverable energy or forecast validation. One shared system-label dataset is reused, not eight independent validation samples.
- Saved local-only `data/processed/wind_audits/proxy_ver_2024_v1/`: report SHA256 `b891341a6493602d90eaab64a573148f79c7cd1183f38c9a491b631cb45cca9d`, comparison parquet `eb19beff28f140c9e4918dc4814996986328018dc3a92eb7c972a2c92c861f20`, manifest `b5000ba53db9e7fec4e6617b7abf317b0cbff1f37b5ecabee07ba7b582f87a28`. Includes the exact script, runtime, code/input hashes and reproduction invocation; dependent caches remain ignored and are not included. All derived scalars are assumption-sourced, retaining original data provenance for VER/load/prices and the GenMix timing/dispatch qualification.
- Independent implementation verified all 176 per-location numbers, 70,272 joined rows and provenance. Separate-directory reproduction matched all four files byte for byte under the original source version; after the numeric-input guard below, all three payloads remain byte-identical and only the wind source-code fingerprint changes in the manifest. Original artifacts were preserved.
- Separate raw-CSV/Decimal reconstruction matches all 8,784 saved VER labels and both completeness columns: 7,634 positive, 1,090 negative, 60 unknown. All 105,408 five-minute intervals are unique; 28,203 energy-positive intervals are already redispatch-positive, so category quantities are not double-added. All 71 complete solar-only-positive hours remain wind-negative; 52 incompletely observed hours with a positive wind sample remain unknown. Removing an interior day in memory produces 24 unknown hours, never negatives.

## Reject lossy scientific quantity coercion - 2026-09-13 12:30 UTC
- Fixed a reproduced data-type bug: pandas could discard the imaginary part of complex quantities or convert datetime/timedelta values to nanoseconds before treating them as MW. Twelve pure-imaginary VER observations could consequently become a complete false event label. Wind numeric dataframe boundaries now reject those types before conversion; carbon's historical generation adapter applies the same guard. Numeric strings and None/NaN/pd.NA remain supported, including nullable pandas numeric columns.
- Added 65 regressions across public screening, adapter, classifier-feature, VER and carbon paths, covering native dtypes, mixed objects, no mutation, correct missingness and unchanged source records. Full repository-root Python suite: 1,150 -> 1,215 passing tests + 30 subtests, same 17 existing warnings. Focused wind: 499 -> 551; carbon: 152 -> 165. No test removed and no valid formula, threshold, source object, model parameter or API shape changed.
- Actual carbon compatibility: old/new normalizers agree exactly on values, refs, statuses and attributes across all 210,528 raw samples from the 2024/2025 archives, compared in 26 monthly partitions without publishing or replacing artifacts. Frozen v4 replay also reproduces the complete report and all 8,760 rows byte for byte with fitting/fetching/network connection creation disabled: 8,571 eligible, 189 excluded. Original inputs, reports, prediction parquet and manifests remain unchanged.
- Frontend remains unchanged at its preceding 267 passing tests and production build. Both local Vite ports 5173 and 5174 serve the source component without assumption headings or source-letter badges; click-only source inspection remains. No new UI edits, no HTTP wiring, no canonical/shared pipeline edits. Continuing on Alex with a cached-data availability check for an independent 2025 proxy comparison.

## Cached 2025 monthly prices and fixed-proxy comparison - 2026-09-13 12:52 UTC
- Added `read_cached_day_ahead_price_month(year=2025, month=1, *, settlement_locations)` to the owned wind module. It reads one already-cached official monthly CSV and returns the existing six price columns plus a source object. The annual reader remains unchanged. Exact URL/hash/JSON/schema/identities, LMP-only selection, negative prices, duplicates and missing values are checked; unexpected HE25 columns or populated padding fail. No fetching occurs in the reader or API.
- SPP's 2025 catalog lists twelve monthly folders, not an annual ZIP. Cached all twelve listed CSVs through the existing downloader, once each, with their catalogs; no shared ingestion changes. Files are `data/raw/spp/evidence/da_lmp_settlement_2025_MM.parquet`. Monthly Date + (HE-1) as UTC interval start matches all 70,272 selected 2024 daily prices/Pnodes exactly, including 23/25-hour DST days, and 192 additional 2025 January 1 daily prices. The guide does not explicitly define the wide timestamp columns: returned monthly provenance therefore remains assumption, retaining the original data URL/hash and that limitation.
- Prepared full UTC calendar 2025 using the twelve operating-month files and the six actual January 1 UTC hours from the prior-year archive. All 70,080 prices are known and unique, with no Pnode changes. Independent raw CSV/Decimal reconstruction matches every timestamp/Pnode/price exactly (canonical digest `f8c2be871c1c779592cf8fa2eb109bbf9ce6d7a9c75e9d20facb5953aa10f90d`). Only null boundary padding is discarded; in-period nulls remain unknown. The first six hours overlap the earlier 2024 operating-year screen, so those two reported windows are not disjoint or matched calendar-year samples.
- Unchanged wind/load inputs give 8,687 common-known hours per point, 72 jointly unknown VER/input hours and one further wind-unknown hour with a known VER label. Fixed proxy counts: AEPM_CSWS 28; LES_LES 764; OKGE_OKGE 305; OPPD_OPPD 492; SPPNORTH_HUB 595; SPPSOUTH_HUB 717; SPS_SPS 468; WFEC_WFEC 179. The common cohort contains 7,570 system-event hours (frequency `0.8714170599746748`); every selected hour coincides with one. This is retrospective system co-occurrence, not local deliverability, recoverable MWh, forecasting accuracy or independent site validation; thresholds were not tuned and no canonical model was promoted.
- Local-only `data/processed/wind_audits/proxy_ver_2025_v1/` contains prices, hourly inputs, comparison rows, report, exact preparation script and manifest. Report SHA256 `d9f9af89b4c346af8aff7eb570d3b4799445ffbdb34bf4c05762140e2d24ef70`; hourly `54f0692fdbf0fca2fd724e0fed699bf350dad873cb009afea387e3faafe4b2f2`; prices `38125781f410e64ea0d8027862010ac759a957fc1f2da5ecea2cd1efd650eb63`; manifest `851b168eac5ce518f52f5ddda0361ac42d39bbd5b72cdf5960755a0b927aa41b`. Separate-directory execution reproduced all six files byte for byte under the recorded runtime. Input caches are fingerprinted, not included or transferred by the code push.
- Raw missingness audit reproduced all 73 wind-unknown hours without a normalization discrepancy: 268 five-minute rows lack both wind components, across 66 partially known and seven wholly unknown hours. All timestamp rows exist; there are no conflicting duplicates or negative-net exclusions. June 2 18:40/18:45 UTC are wholly missing GenMix rows. January 23 01:00 UTC also has an incomplete system-load sum: only NPPD/OPPD/WAUE are present, fourteen required areas missing. These gaps remain unknown; missing components are never supplied by VER event labels.
- Tests: 95 new monthly-reader cases; focused wind 551 -> 646 passing, full root Python suite 1,215 -> 1,310 passing + 30 subtests (same 17 existing warnings). Frontend remains unchanged at 267 passing tests and its passing build. Remote check at 12:30 UTC: main `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`, unchanged; no merge, main push or UI replacement. Continuing on Alex with standalone 2025 snapshot packaging for explicit-capacity scenarios.

## Remove remaining demo badge outlines - 2026-09-13 13:04 UTC
- Changed outside the overnight lane at the user's explicit request: removed the remaining square border on standalone Info controls beside frontend inputs and selectors. Source-letter superscripts and repeated Assumed pills were already absent. An independent source audit found no other badge chrome; confidence levels remain plain text.
- New/changed interface: none. Source records, click-only inspection, export, slider semantics, confidence framing, calculations, typography and layout are unchanged. No shared model or API code was touched.
- Validation: full frontend suite 267 -> 267 passing, including eight live API cases; production build passes with its existing large-chunk warning. Both Vite ports 5173 and 5174 serve the cleaned source component; port 5174 also serves the borderless controls. Browser surfaces are unavailable, so this is source/HTTP/test verification, not screenshot QA. Hard-refresh `/app` to clear a stale tab.
- Publication remains Alex only. Teammate fetch at 13:01 UTC is unchanged: main `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`. Continuing the standalone 2025 grid-impact handoff and independent carbon/provenance audit; no frontend replacement or canonical endpoint wiring.

## Standalone 2025 snapshot handoff and serving audit - 2026-09-13 13:10 UTC
- Prepared `data/processed/grid_impact/handoff_2025_review_v1/`: eight immutable exact-point snapshots, `grid-impact-inputs-v1` prepared envelope and transfer manifest, 10 files / 4,531,466 bytes. Existing `grid-impact-v1` interfaces are unchanged. Declared scenario controls are sourced assumptions, 100 MW and 0.5 upward availability; neither is inferred from site_exposure or an alias.
- Observed scenario MWh: AEPM_CSWS 1,400; LES_LES 38,200; OKGE_OKGE 15,250; OPPD_OPPD 24,600; SPPNORTH_HUB 29,750; SPPSOUTH_HUB 35,850; SPS_SPS 23,400; WFEC_WFEC 8,950. Each point has 8,760 requested/observed rows, 8,687 evaluable hours and 73 unknown hours. All annual values and shifted-carbon values remain null; associated wind direct operational CO2 is zero. These remain assumption-sourced historical capacity scenarios, not measured recoverable wind, local headroom or a carbon offset.
- Manifest SHA256 `06d24b2fec992f574ada8d51c8b275208e8de93dff3b70a7f79ba64813fb13fe`; prepared envelope `789fbc4e7ad3903e6ee5aad2114851d77813a4d87ff585833ea954579ebfcb44`. Independent checks verified every file/hash, all 70,080 hourly selections, all eight six-scalar results, each 12-node provenance graph, exact capacity arithmetic and all coverage/null rules. Original 27 grid-impact artifacts were preserved.
- Verified 24 real CLI calls in an isolated copied-source checkout with no data directory: all eight prepared-input recompilations are byte-identical. Sixteen cold reads and sixteen capacity-scaling checks passed. Serving and recompilation require only the snapshots/prepared envelope plus matching code/dependencies. Optional full preparation is `.tools/prepare_grid_handoff_2025_v1.py` with the fingerprinted hourly parquet; that ignored script/cache are separately required and are not included in the transfer ZIP.
- Transfer archive `data/processed/grid_impact/handoff_2025_review_v1.zip`: 111,208 bytes, SHA256 `4f0b530540aba3dbbf953952ade0efc7eefbbf6a5d56701f48830198b4d9ac90`. All ten ZIP members pass CRC and byte-for-byte comparison with the immutable directory. These artifacts remain ignored and are not transferred by the code push; give Tharun the ZIP separately and compare its hash. `python -m api.grid_impact check --snapshot <file> --location-id <exact identifier>` verifies each received snapshot; the manifest records recompilation commands and required code hashes.
- Local Windows timing with recomposition disabled: per-point cold-read medians 5.37-8.72 ms, warm-read medians 0.84-1.05 ms, warm capacity-scenario medians 2.33-3.46 ms. These are local helper measurements, not HTTP/browser latency; warm reads still hash fresh bytes and return defensive copies. Measurements and exact script hashes are in `.tools/grid_handoff_2025_transfer_audit.json` (SHA256 `c222d7f82de8e27c6cd4823e7b30cf40fc4caa6179caeaaa2c8ec50642f510ed`). No performance refactor was needed.
- Separate read-only carbon audit passed 375 shift, 180 wind and 100 seeded fuel-mix/conservation scenarios, including signed kg/tonne conversion, mixed source kinds, unknown intensities and shared limits; no new discrepancy. No production code changed in this handoff. Latest full suites remain 1,310 Python tests + 30 subtests and 267 frontend tests/build passing. Continuing the scoped self-review; no canonical endpoint/UI wiring or main push.

## Consumer example verified - 2026-09-13 13:20 UTC
- Added a short standalone consumer example to `api/grid_impact.py`: exact point lookup, retained cache, mandatory sourced controls, units/evidence and explicit null handling. No route or behavior changed; executable AST matches the preceding commit exactly.
- Executed the documented example against LES_LES: declared 50 MW and 0.25 availability produce 9,550 observed MWh and an unavailable annual value, with sources and inputs intact.
- Full Python suite: 1,310 -> 1,310 passing + 30 subtests, same 17 existing warnings. Frontend remains at 267 tests and passing build; no further frontend edits.
- Snapshot manifest pins the pre-documentation code bytes from `a60d596`; artifacts remain immutable and compatible. Continuing a descriptive calendar-month/deletion audit of frozen 2025 scores, without fitting, model selection or confidence claims.

## Frozen wind-score calendar sensitivity - 2026-09-13 13:30 UTC
- Audited the unchanged v4 wind-event replay: all 8,760 UTC rows, 8,571 eligible, 189 excluded and 7,464 events. Saved all twelve calendar-month partitions and all twelve pooled single-month omissions; the constant baseline and evaluated mask stay fixed. No fitting, bootstrap, uncertainty interval or model selection.
- Both variants have lower pooled Brier/log loss than the baseline after every individual omission. Their relative ordering is less stable: removing July, August or November reverses the full-year Brier ordering; only July reverses log-loss ordering. October alone has raw Brier 0.057903530266381266 versus baseline 0.04854650216425085. These are descriptive results from overlapping cohorts, not independent validations or future confidence.
- Independent verification: all 150 loss values match scikit-learn within 1.1102230246251565e-16; 75 Decimal Brier checks, all calendar counts, masks and signed differences pass. Every statistic retains assumption provenance and all seven frozen input hashes. No parameter, source file, API or UI changed.
- Local artifact: `data/processed/wind_audits/frozen_2025_calendar_sensitivity_v1/`. Report SHA256 `6ca9f5c14141ef1bec960b840284281b459573e44398ee40e5115a63129b5c06`; manifest `f707804854af2d507ad5ead4306803a164071593300b17d7f6f27dc1f23d713f`. Includes complete tables and exact script; `audit.py --check` reproduced output bytes without writes. Inputs remain in the existing frozen replay bundle.
- Latest tests remain 1,310 Python + 30 subtests and 267 frontend/build passing. Continuing a read-only source-coverage review for any later independent period; no implicit extension of historical SPP_SYSTEM into the newer SPP/SWPW archive partitions.

## Reviewed winter archive extension and fixed replay protocol - 2026-09-13 13:55 UTC
- Extended the offline `read_cached_wind_curtailment_month(year, month)` reader to exactly December 2025, January 2026 and February 2026. Defaults, December dataframe/source bytes and label semantics are unchanged. Exact cache URL/hash, eight-column schema, CSV widths and Central operating-month bounds remain enforced; later months are rejected. No fetch occurs inside the reader.
- Cached the two officially listed monthly VER files once through the existing downloader. January raw SHA256 `2f4d0cc7b0eedac656fd77262d8c670c2a92fd8c90587f14c6f9ef022477d909`; February `90b74cd0eb2473b3708f8c9264ce835c52a25be368531e5391bb90159f3bb992`. They have 8,928 and 8,064 distinct consecutive five-minute intervals, respectively, and the same eight headers as December; each wind category has seven January blanks and sixty February blanks. BAA is absent, so a sourced historical-footprint assumption remains mandatory downstream.
- Fixed the next research window to UTC January 1 through March 1, 2026, exclusive, before inspecting any new event labels or predictions. Local protocol `data/processed/wind_audits/frozen_2026_janfeb_protocol_v1.json`, SHA256 `14b9f2a66d56aabddf2d451712ef599a00b39f51fca03e60830a9fce97a6a125`, retains model `6cfb0e75875fb7116fb753d217e9514e10124c9183dd8c924fad87269814b87d`, both raw/calibrated variants and the previously verified fixed training-prevalence baseline. All calendar hours and missing evidence must remain explicit; no fitting, variant selection, annualization or production promotion.
- Period selection is based on source compatibility, not outcomes. January/February load retain the historical seventeen-area wide schema. March instead mixes SPP/PSCO/WACM/WAUW records and unreviewed CF/NC rows; do not sum them. GenMix YTD explicitly identifies SPP and adds BAA/storage fields; only reviewed Wind/Solar fields enter preparation. SPP's [expansion page](https://spp.org/western-services/rto-expansion/) and [May 6 filing, page 2](https://spp.org/documents/76650/20260506_amended%20filing%20-%20revisions%20regarding%20the%20peak%20demand%20assessment%20and%20demand%20response_er26-2249-001.pdf) describe separate East/West balancing areas after April 1. That does not resolve the March CF/NC meaning or establish local headroom.
- Added 39 regression tests covering the new months, source preservation, no fetch, exact boundaries, malformed CSV/schema and unchanged unknown/category behavior. Full Python suite: 1,310 -> 1,349 passing + 30 subtests, same 17 existing warnings. Actual cached December dataframe and source match the preceding implementation exactly; both new raw hashes and missingness counts verified with fetching disabled.
- Frontend remains at its prior 267 passing tests/build and cleaned badge controls. No API route, canonical ML file, economics or UI change in this checkpoint. Preparing separate immutable winter inputs/labels for review; all raw and derived artifacts remain ignored and require separate transfer. Publication is Alex only.

## Frozen winter replay independently verified - 2026-09-13 14:15 UTC
- Prepared the fixed UTC January-February 2026 window with December lag history, without fitting or changing any model parameter. Inputs: `data/processed/wind_audits/frozen_2026_janfeb_inputs_v3/hourly.parquet`, SHA256 `7d755b049ef745c6b93b26cf021926ac533f73bdf5fc4811c320496b75fede5e`; labels: `frozen_2026_janfeb_labels_v1/labels.parquet`, SHA256 `060746ff133cca5e0a8ec86a94361409b8f12ba0255a03055c54dbc9e3732caa`. Both retain assumption provenance and the frozen timing/footprint qualifications. Their complete preparation publications reproduced byte for byte, including scripts, metadata and coverage reports.
- Raw verification covered 17,213 of 17,280 expected generation samples across the input window and all 24,480 load components. Nineteen wind hours are unknown including one December lag-history hour; no load hours are unknown. January 1 05:00 and 06:00 UTC retain missing wind components even after correct raw-boundary stitching. The independent VER oracle checked all 16,992 selected intervals and 50,976 category sign/missingness comparisons: sixteen requested hourly targets remain unknown. Nothing was filled or treated as a negative event merely because it was missing.
- Preparation review preserved two failed attempts. An extra solar-negative guard in v1 was inapplicable: thirty-two signed auxiliary solar hours are retained, and solar is never a classifier predictor. v2 found nine December aggregate load values differing despite all 408 raw components being bit-identical. Instrumenting the unchanged pandas sum demonstrated an F-to-C memory copy when the older full table fills missing cells elsewhere; its grouping differs from the complete new window. Maximum overlap difference is 1.0913936421275139e-11 MW. v3 retains both original sums without substitutions, injected nulls or new tolerance, verifies each exact C/F-order result, and records Decimal/fsum comparisons in `overlap_roundoff.json` (SHA256 `213281c16d77a5fb9e602ab1450a929877476e9a32cbe395573ed169f5fde783`). Shared normalizers and prior artifacts are unchanged.
- Published `data/processed/wind_model/6cfb0e75875fb7116fb753d217e9514e10124c9183dd8c924fad87269814b87d/replay_2026_janfeb_v1/` with fitting and network entry points patched to fail. Of 1,416 requested hours, 1,367 are eligible (1,305 event-positive, 62 negative); 37 missing-lag and 16 unknown-target hours overlap in four hours, excluding 49 in total. Every requested hour remains in the prediction table. Cohort SHA256 `529be8c48e4184f9c91303ac655bca8f7179c87da74dcd65a9cee063323be0a8`.
- Actual winter metrics: raw Brier `0.060485194640717166`, log loss `0.2071661936504929`; calibrated Brier `0.049250281796204935`, log loss `0.18409936542876784`; fixed training-prevalence baseline Brier `0.052011595381519`, log loss `0.2321383590853796`. Both variants have AUC `0.788728216536893`. Raw Brier is worse than baseline; calibrated Brier is slightly better. This does not establish annual or site skill, resolve input publication timing, or select a production variant. All metrics remain assumption-sourced research.
- Independent pyarrow/stdlib/80-digit Decimal verifier reconstructed every feature, mask and canonical hash without importing pipeline code. All 1,379 available probabilities per variant agree within a disclosed maximum 3.3306690738754696e-16 (six ULP); Brier/log-loss values match the separate fsum oracle exactly, rank AUC differs one ULP and reliability statistics at most three ULP. Verified 8,682 source objects. `frozen_2026_janfeb_independent_v1/` audit manifest SHA256 `43a9f1107af8fbff78c1ddb428031a977f0f922cfadcefa7f3fbdb6c653549b8`; exact oracle script SHA256 `f5ad276e60b96d2915f40f91614acc6f54ad52516f5b0855d1d9bc3b8469eb12` also passes the unchanged 2025 replay.
- The real replay CLI reproduced all seven publication files byte for byte in an isolated copied-source tree with no `data/` directory. Replay manifest SHA256 `07a31c20cf7c35bf96a80c92a7677db86ee6ca18f8d5f623f26e000bf8336436`; report `9037b680b43ecbd95f4e0230d0344fa0c77a4a7f07713f3f01b8d2bcad07450f`; predictions `e0ede04504a511e5536355bd45cb8bbc5ddf4c14475bdff5f2cb4d43efe94cea`. All artifacts remain ignored and need separate transfer. No canonical ML/API/UI change; latest full suites remain 1,349 Python + 30 subtests and 267 frontend/build passing. Fetch at 14:01 UTC is unchanged: main `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`.

## Reviewed winter prices and coverage review - 2026-09-13 14:40 UTC
- Extended the offline monthly DA price reader only to January/February 2026 after inspecting both officially listed raw files. They retain the exact 28-column schema and all eight reviewed settlement-point/Pnode identities; no BAA field, malformed rows, interior missing prices or populated boundary padding. Raw SHA256: January `334dface9000c5f29115769d6976a6e0a75adf64c8e88bbd357a16faaf869d24`; February `24d5ec98a72d6ede9f75ffd7f75cf4b9134beddb2b5ebb483036a94dcf42cba6`. Files were cached once through the existing downloader; the reader never fetches.
- The only production changes are the reviewed-year/month guard and its documentation. All twelve actual 2025 monthly outputs, 70,080 selected rows, match the prior function exactly in values, dtypes, attributes, serialized parquet bytes and source JSON. January/February 2026 yield 5,952/5,376 rows. Unreviewed later months reject before cache I/O. Monthly UTC mapping and source status remain explicit assumptions.
- Added 35 hermetic regressions; full root Python suite 1,349 -> 1,384 passing + 30 subtests, same 17 existing warnings. No test removed. Frontend unchanged: both local servers serve the badge-free source, with 33 focused UI tests passing again; preceding full 267-test/build result still applies. No visual-browser inspection is claimed.
- Recorded the next fixed-proxy protocol before computing its 2026 flags: `data/processed/wind_audits/fixed_proxy_2026_janfeb_protocol_v1.json`, SHA256 `6a701138c722471ce76ba5dad31a9b30854ba0933ec19ad7bc6ebdd4f5c67bc9`. Same UTC January-February window, eight exact points and thresholds (wind/load >= 0.5, LMP <= 0); actual December price cells supply the first six UTC hours. Separate classifier labels/scores were already inspected, explicitly recorded as prior knowledge: this is a descriptive check, not a newly blinded validation. Preparation and an independent raw-cell oracle are underway without tuning or annualization.
- Classwise saved-score audit completed for six fixed winter cohorts (pooled/January/February, each in 2025 and 2026): `data/processed/wind_audits/frozen_winter_classwise_v1/`, report SHA256 `9a73ec8b80a5f5dcf3f42f2407b239173dbf05f659bd5fd78542ac942279faa0`, manifest `0ed97fe80701c7a0514678a3d2bfdc834bcc9548cc859423aaa975816c1690cf`. All 510 statistics retain assumption provenance and seven input hashes; byte reproduction and 234 independent 80-digit Decimal/count checks pass, maximum rounding difference 1.1102230246251565e-16. No new predictions, fitting or selection.
- In 2026 pooled eligible rows, raw-model event-hour Brier contribution exceeds baseline by 0.014938465730528358; its non-event contribution improves by 0.00646486647133019, net loss increase 0.008473599259198168. This is squared-loss accounting, not a causal explanation. Known targets omitted by missing predictors are 31 event hours and two non-event hours; sixteen further labels are unknown. Missingness is not assumed random. A conservative algebraic bounds check is next.
- Frozen winter replay ZIP is ready for separate transfer: `data/processed/wind_model/6cfb0e75875fb7116fb753d217e9514e10124c9183dd8c924fad87269814b87d/replay_2026_janfeb_v1.zip`, 116,797 bytes, SHA256 `c60b0fcd63bf054ce6c3af66f144716df0cc4a1dd831428642beefbdd13b8972`; seven members verified by CRC and exact bytes. It includes the four canonical replay inputs, not raw caches/preparation sources. All artifacts remain ignored, not pushed. Remote fetch at 14:32 UTC remains main `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`; publication remains Alex only, with no API/UI or canonical-model wiring.

## Carbon precision and fixed winter evidence - 2026-09-13 15:08 UTC
- Fixed two rare binary64 underflow paths in `pipeline/carbon.py`: the complete weighted intensity and signed shift pair use exact rational arithmetic only when an intermediate enters the subnormal range. Representable results no longer become false zero; ordinary formulas, missing-value behavior, provenance and the sum of published pair values remain unchanged.
- Added 19 regressions; full Python suite 1,384 -> 1,403 passing + 30 subtests, same 17 existing warnings. Independent compatibility audit confirms 509 ordinary outputs, 13 invalid-input errors and all 32 saved snapshots unchanged; report `.tools/carbon_underflow_compatibility_v1.json`, SHA256 `d9df9c8eb7cf928fce55d76bfb2d47823e93ed656ab0f3a0a32e5adedbb22555`.
- Fresh frontend verification: initial parallel run 265/267 passing, two existing App timing failures and delayed object-URL cleanup error; unchanged serial rerun 267/267 passing including eight live API tests. Production build passes with existing large-chunk warning. All 72 checked frontend files unchanged; no visual-browser inspection claimed.
- Fixed January-February proxy preparation completed: `data/processed/wind_audits/proxy_ver_2026_janfeb_v1/`, manifest SHA256 `9cd5483ca4666999332b208c1e6143603deb013be1558b2eb5acbe7ea188ef24`. All 13 files reproduced byte for byte using copied sources; independent raw CSV/Decimal oracle checks all 11,328 price cells and nine nullable joint cells per point (`fixed_proxy_2026_janfeb_independent_v3/`).
- Each point has 1,397 jointly evaluable hours, including 1,333 event hours. Proxy-positive counts in sorted point order are 10, 148, 52, 65, 85, 120, 141, 59, all on event hours. This high-prevalence descriptive association is not a forecast, local delivery proof, or eight independent grid datasets; thresholds and source assumptions stay fixed.
- Conservative missing-evidence bounds published in `data/processed/wind_audits/frozen_winter_missingness_bounds_v1/`, report SHA256 `58552166574d5c10c58fa709616979c32b40381d65c1679c187891f9aa794f26`: exact-rational and independent Decimal endpoints agree. Full-calendar 2026 winter Brier difference versus baseline is raw [0.0040770469811791795, 0.03451175507368478], calibrated [-0.006531121052616519, 0.023656706249826076]. These are outer algebraic bounds, not confidence intervals or imputed forecasts; no new fitting or variant selection.
- Integration: all research artifacts remain ignored and require separate transfer; no API contract, HTTP wiring, canonical model or frontend changes in this checkpoint. UI source-letter tags, assumption pills and hover-open panels remain removed; click-only source details remain available.

## Numeric boundary consistency and portable winter handoff - 2026-09-13 15:32 UTC
- Fixed wind/carbon validation: finite JSON integers now behave like equivalent floats; unrepresentable values raise domain `ValueError` instead of NumPy `TypeError`/Python overflow. WindPolicy stores its validated binary64 thresholds so the actual screen matches the threshold in provenance. Carbon still rejects negative inputs that round to negative zero.
- Added 17 public-boundary regressions; full Python suite 1,403 -> 1,420 passing + 30 subtests, same 17 existing warnings. All 16 actual 2025/winter proxy summaries remain byte-identical, including refs. Frozen-model replay also reproduces all 10,176 prediction rows and both report/parquet pairs byte for byte; `.tools/finite_number_frozen_replay_compatibility_v1/report.json` records the check. No refitting or changed model output.
- Portable handoff: `data/processed/grid_impact/handoff_2026_winter_review_v1/` and sibling ZIP (308,747 bytes). Manifest SHA256 `a88bc7c9cc2335abca4fd5d2caca61b984709adf2331e93bb1891c1959bc393a`; ZIP `9673c7c29bec9a3617bdd2b5587890a93572600140852d7e8ecef0eea40dea9f`. All 25 files and ZIP reproduce from bundled inputs and frozen sources, including with ML/downloader imports blocked. Only documented packages and their normal dependencies are required.
- Each exact point retains 1,416 observed hours, 1,398 evaluable and 18 unknown. Explicit review controls are 100 MW and 0.5 upward availability; annual and shifted-carbon fields remain null. Independent Fraction audit checked all 11,328 input rows, 96 evidence hashes and 32 alternate scenarios (`wind_audits/winter_grid_handoff_independent_v1/`). Raw caches and per-row Pnodes require the separate upstream handoff; no city/site aliases or physical recoverability claim.
- Official hourly-load metadata resolves CF as conforming, NC as nonconforming, and MarketHour as hour-ending GMT. Historical CF/NC additivity and metered-versus-estimated treatment remain unverified. Reproducible source review: `data/processed/wind_audits/hourly_load_2026_schema_review_v1/`, manifest `b29fc3297b1aaf706bdeeea5983f9947cc4afa1355e2c975a8e175d86f7ca540`. No March load sum, schema expansion or new model cohort.
- Frontend follow-up for Tharun: the parallel export failure leaves its revocation timer pending when test teardown restores jsdom URL; deterministic teardown should drain it before restoring globals. The surface failure is a cold lazy-import wait, not evidence of a WebGL loop freeze. Unchanged serial suite/build pass; browser GPU behavior remains unverified. Badge-free source is confirmed served on 5174/app.
- Interface unchanged; publication is Alex only. All research bundles remain ignored and need separate transfer. Latest fetch is unchanged: main `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`; no teammate branch or frontend merge.

## Explicit footprint validation and source review - 2026-09-13 16:00 UTC
- Fixed a real nullable-string bug: missing observation/label location IDs could pass pandas `all()` and be reassigned to SPP_SYSTEM. Both classifier boundaries now reject missing IDs explicitly. The direct wind-input adapter also rejects unnamed or non-string load footprints before normalization; custom named scopes remain unchanged.
- Added 48 regressions for Python/Arrow nullable strings, invalid names, public prediction/replay paths and input/provenance preservation. Before the guards: 38 failures and ten valid controls passing. Full Python suite: 1,420 -> 1,468 passing + 30 subtests, same 17 existing warnings. No model fit, economic formula, API contract or frontend change.
- Both frozen replay outputs remain exact after the classifier guards: all 10,176 rows, report/source values and report/parquet bytes match the saved publications; all fourteen publication files retain their hashes. Audit `.tools/null_location_frozen_replay_compatibility_v1/report.json`, SHA256 `025b1dff8bfb79fa86047929e9b73d691c9b28834a629d0ca487901481dad4dc`. No refitting or promotion.
- Cache and native shift-pair lineage reviews found no additional defect: fresh-byte hashing, private return copies and distinct timestamp refs remain intact even for repeated equal quantities. Winter helper timing medians were 3.84245 ms cold, 0.94035 ms warm and 2.01645 ms for explicit scenario recomputation across 120 local calls; these are OS-warm Python measurements, not HTTP/browser timings or a latency guarantee. Raw samples: `.tools/winter_grid_helper_timing_v1.json`.
- Located SPP's current [v35 guide](https://www.spp.org/documents/75871/spp%20markets%20public%20data%20guide%20and%20samples%20v35.zip), raw SHA256 `e2e8478bc8b0fd5fba8201b4c1f4197a4983d88a2406d710cba2fe230cdebee7`, through its official current-specification index. It still does not explicitly define monthly Date/HE mapping or resolve GenMix interval/meters; Self dispatch targets and forecast Load remain qualified. No archive schema, period or prior source object was changed.
- Independent offline source verification checked six cached documents, 28 guide locators and five sample headers against the two review reports, with network blocked. Reports: `.tools/da_monthly_time_source_review_20260913_v1/report.json` and `.tools/genmix_v35_source_review_20260913_v1/report.json`; verifier `.tools/verify_spp_source_reviews_20260913_v1.py`. Original bytes are unchanged; unresolved definitions remain assumptions.
- Continuing the scoped audit on Alex. Fetch remains main `9e8a6a0`, Kristian `2d22db1`, Tharun `e1257ef`; no merge. Frontend remains at the previously verified 267 serial tests and passing build, with badge-free source served on 5174/app. Research artifacts remain ignored and require separate transfer.

## Fixed proxy-threshold sensitivity verified - 2026-09-13 16:18 UTC
- Declared a descriptive grid before computing its results: wind-share thresholds 0.4/0.5/0.6 and price ceilings -10/0/10 USD/MWh, over the unchanged 2025 and January-February 2026 calendars and eight exact price points. Protocol SHA256 `feb04d4c3478555fc39c522359963cc15de85e791b0db889983259d2f0dc8bae`. Prior baseline/target knowledge is explicit; these are perturbations, not sourced plausible bounds or a blinded validation.
- Published `data/processed/wind_audits/proxy_policy_sensitivity_2025_winter2026_v1/`: 144 records and 6,048 assumption-sourced statistics, including all nine nullable proxy/event cells and explicit conditional-ratio numerators/denominators. Full baseline Boolean arrays reproduce all 81,408 point/hour rows; all 192 selected-set monotonicity checks pass. No policy-dependent missingness, energy conversion, annualization, ranking, fitting or production change.
- Threshold choice materially changes screening counts. LES_LES spans 251-1,645 known proxy hours in 2025 versus baseline 764, and 52-304 in winter versus baseline 148. AEPM_CSWS spans 3-139 versus baseline 28, and 0-65 versus baseline ten. These are observed-period scenario counts across the fixed grid, not uncertainty intervals or measured recoverable energy. A zero selected-hour denominator correctly produces a null conditional fraction.
- Keep the context with any reported association: common-row VER event prevalence is 7,570/8,687 in 2025 and 1,333/1,397 in winter. An event-bearing hour contains at least one positive five-minute event among twelve evaluable samples; it is not an hour-long curtailment duration. High conditional association does not establish forecast skill, local delivery, or an independent dataset at each price point.
- Independent PyArrow/stdlib oracle was frozen before reading producer results. All 144 records, 6,048 sourced values, 1,296 joint cells and 576 ratio triples agree exactly; exact-rational boundary checks find no disagreements. Root reproduced all seven producer files and all five independent comparison files byte for byte. Original input/source objects remain unchanged.
- Publication manifest SHA256 `193bc5a48ac86dfd30143d5500a089f0e06b199cefcc6a7b6617a4b9dfdf6eaa`; results `7bdbe32db47541e5cefe9c739247e9f220faf4b6d2d91f04bf62f378b61c11e8`. Independent comparison: `wind_audits/proxy_policy_sensitivity_independent_comparison_v1/report.json`, SHA256 `16d7f807370ca6914a1cb71a3065fd559f320a0fd8bb434c065af6d0c8cf787e`. Scripts, protocol, source copy and exact dependencies are recorded; artifacts stay ignored for separate transfer.
- No tracked implementation change since `79af411`; latest full verification remains 1,468 Python tests + 30 subtests and the prior 267 frontend tests/build. Continuing with an inventory of actual full-fuel carbon coverage and missing factors, without inventing fuel mappings or makeup schedules. Publication remains Alex only.
