# Default: call the local FastAPI endpoint directly with CORS, no proxy required.
# Set to local to make no HTTP requests. Restart Vite after changing this value.
VITE_ESTIMATE_MODE=api
VITE_API_BASE_URL=http://127.0.0.1:8000
